# Special-die repairs and admission safeguards — 2026-09-17

User authorized all six findings from SPECIAL_DICE_OPTIMIZATION_AUDIT_2026-09-17.md, then requested reusable blockers and animation best practices for future variants.

## Runtime repairs

- Spaceship: hidden finished debris exits before geometry/paint work. Backward seek into its active interval revives it, retaining replay and hide boundaries. Actual-scene regression covers consecutive hidden frames, no node mutations, rewind and hide again.
- Honey: snapshot scale/x/y and base CSS position once per visible bee per synchronous solver update. Preserve eight relaxation passes, mutable offsets and original pair ordering. Independent old-algorithm comparison covers multiple updates and changing visibility/pose; getter count is N opacity reads plus three per visible bee. For 16 visible bees, pose getters fall from 3,264 to 48, excluding the unchanged 16 opacity reads; this is not a thermal measurement.
- Bee: guard identical CSS and diagnostic data writes; normalize opacity string values to avoid false differences. Preserve active sampled trajectories and hide/show behavior. Same-time update yields no DOM mutations in the focused test, movement and rewind still update.
- Flower: sample the shared scale easing once for X/Y. Differential test against authored SVG/prior independent sampling checks exact poses across the cycle, key boundaries and wrap/negative inputs.
- Fish preparation: remove unconditional finale-media preparation from Journey/Arcade navigation. Use existing generation-owned post-entry warmup with actual eligibility (Beach 12–20, Arcade round one, or a live restored Fish). Both retained HEVC video owners load only once per element; consumption allows a new prepared element without resetting active playback. Cold attach/fallback paths remain owned by the scene. Unused prepared media remains a bounded module cache; this patch does not implement a new media eviction system.
- Fish idle: avoid unchanged transform/style assignments while retaining shared frame geometry, depth and media visibility lifecycle.

No supplied assets, animation durations, gameplay archetypes or progression were changed. No native sync/install or device capture.

## Future-variant safeguards

- AGENTS.md and the existing artwork playbook route every special animation/variant change to SPECIAL_DICE_PERFORMANCE_CONTRACT.md.
- A registry-derived TypeScript AST admission check requires every registered variant to declare its matching archetype, real idle/finale owner paths, warmup/cleanup policy and existing regression tests in special-dice-performance-owners.json.
- `npm run qa:special-dice` is included in both fast/full QA. Positive and negative checks verify missing variant, wrong archetype and missing test rejection.
- The contract requires behavioral coverage for hidden retirement/replay, eligible/idempotent preparation, solver equivalence/read counts, shared easing, unchanged writes, and lifecycle/late loads. Admission metadata is not a substitute for those tests or physical validation.

## Validation

Fast QA PASS. Full QA exit 0, all 14 gates PASS. Full test suite: 356 suites / 2,353 tests PASS; Gameplay KING: 24 suites / 306 tests PASS. Independent reviews PASS after correcting three manifest finale-owner references. Admission checker accepts valid metadata and rejects missing variant, wrong archetype and missing-test fixtures. Complete full-gate result is recorded in CURRENT_HANDOFF.md and logs/special-dice-repair-20260917/qa-full.log. HTTP localhost:5174 serves the repaired source; this is availability verification, not visual acceptance. Source/build results are separate from web visual and phone acceptance.

Physical verdict: **NEEDS PHYSICAL TEST**. These repairs remove proven unnecessary work and a reproduced visibility defect; they do not prove all original heating, board-entry stalls or resets are resolved.
