# Background work and loading repairs — 2026-09-17

Implements the authorized findings in BACKGROUND_AND_LOADING_AUDIT_2026-09-17.md. Preserves unrelated dirty work, supplied assets, sound gain/timing and gameplay archetypes. No native synchronization, install or phone capture.

## Changes

1. **Eligible sound preparation.** `special-sound-warmup.ts` selects existing sound preload owners from canonical Forest/Area55 pools, Beach variants, registry-authored Arcade tour and restored live tiles. Journey/Arcade entry retains common ordinary/Star/poof sounds but no longer prepares ten unrelated special-family owners indiscriminately. Fresh and saved board completion use one generation/load-owned delayed preparation boundary. Optional preparation skips hidden-document callbacks; no new resume listener or replay queue is introduced. Existing playback, Settings and fallback implementations remain intact. Arcade Fish media eligibility now follows the actual registry tour (currently no Fish), rather than assuming every variant can appear in round one.
2. **Queued slider ownership.** A single cancelable request owns polling, settle timeout and fallback. Success retires fallback; new navigation, forceReady and destroy retire the previous request. A late callback cannot reset an exit-owned flag or navigate after disposal.
3. **Renderer-matched decoration preparation.** Journey bottom art uses DOM srcset preparation, sharing source assignment with the displayed image. Both densities are no longer uploaded to Pixi for a DOM-only decoration. Pending warmup has bounded timeout and retry; assets remain unchanged.
4. **Canonical ghost density and unused preload.** Startup and board consumers share one ghost texture selector. Removed unused close-button image entries from preload lists only; asset preserved.
5. **Hidden Wild Star work.** Independent GSAP orbit callback skips hidden-document mutations/calculations while keeping cleanup ownership. Delivered hidden ticks refresh the timing baseline; existing delta clamp remains protection when WebKit suspends callbacks completely.
6. **Dead FX removal.** Removed disconnected triggerJuiceMergeFizz/createMerge6Bubbles and unused fx-special-effects module, after caller/import/global compatibility search. Updated active guide and no-check baseline. This is maintainability cleanup, not a claim of measured thermal savings.
7. **Foreground play-time accounting.** New small tracker makes start idempotent and consumes elapsed time synchronously before async persistence. Visibility stops before imports, resumes only active foreground gameplay, and game-active subscription covers Journey/Arcade. Partial seconds carry between intervals. Repeated stop and old background callbacks cannot double-count or stop a newer foreground interval. Storage failures retain existing log-only behavior; no historical statistics migration.
8. **Actual startup dependency boundary.** Deferred UI gameplay boot; extracted lightweight tutorial request/storage policy with compatibility reexports for current consumers. Slider and Settings no longer import the complete tutorial/runtime to check a flag. Removed source-wide manual chunk grouping while retaining vendor/GSAP grouping. Direct repeated new-game requests now share one in-flight owner across cold loading, released on success/failure. Source AST graph and actual built graph verify app-core is absent from static startup dependencies and HTML modulepreloads, yet dynamically reachable. This is a loading boundary improvement, not proof of measured cold-launch speed or reduced sustained heat.

## Permanent safeguards

AGENTS/PROJECT_CONTEXT route new background/loading work to BACKGROUND_WORK_CONTRACT.md. Existing special-die performance contract and admission index remain. Startup dependency source audit runs in fast/full QA; full QA also checks the produced bundle graph. An isolated negative fixture confirms that reintroducing a static app-core import fails the audit.

## Validation and acceptance

Evidence folder: logs/background-work-repair-20260917. Focused tests cover actual slider owner, Wild Star visibility, clock overlap/foreground races, audio selection and entry ownership, DOM preparation/density, and tutorial request compatibility. Independent reviews were scoped to changed owners. Final `qa:full` exit 0: all 16 gates PASS, 363 suites / 2,382 tests; Gameplay KING 24 suites / 306 tests. `qa:fast` passed before the final small visibility guard; that guard then passed focused behavior tests, final TypeScript and targeted lint. Full production build includes the guard. Final source/built startup audits PASS; the separate app-core chunk is 562,707 bytes and absent from the entry static closure. HTTP localhost:5174 serves the final source. Full logs and current ownership are recorded in CURRENT_HANDOFF.md.

Source/dist and physical app are separate: installed phone remains the older 36aac656 entry. New web visual acceptance and physical FPS/thermal/cold-entry/sound acceptance remain NEEDS PHYSICAL TEST. Browser UI check was attempted but automatic permission review timed out; no browser visual acceptance is claimed. Source HTTP availability and build/test evidence do not replace that check.
