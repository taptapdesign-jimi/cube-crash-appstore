# Current Stack to Six Handoff

Updated: 2026-08-09

## Repository state

- Production promotion complete: the accepted Journey card/modal state is package `2.0.635` on canonical `main` at commit `9e70ee0`, pushed to `origin/main`. Full QA passed 115 suites / 690 tests plus every build/audit gate; `qa:ios` passed after the explicit complete-asset sync only to Stack to Six `Web.bundle`. The signed arm64 Debug app verified bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, installed over the existing app on `iPhone 13 blue` at installation URL ending `515FF1B4-C2A1-4B03-ACFC-1498CD1BB366/Stack%20to%20Six.app/`, and that exact bundle launched successfully. The first install attempt ended before installation with a CoreDevice connection reset; the safe retry succeeded. Existing app data was preserved; legacy Kockice Crash and every other device were untouched.
- Rapid re-open during the post-dismiss squash/stretch no longer captures a transient undersized card. The regular-card tap handler now synchronously cancels only the active overlay landing-bounce timeline, restores its saved inline transform styles, clears the GSAP transform, and only then acquires the portal origin lease. The next modal therefore always enters from the full settled Unit geometry; normal tap, landing bounce/smoke, flip, and return timing are unchanged. `qa:full` passes 115 suites / 690 tests and every build/audit gate. Web-only; native bundle and iPhone remain untouched.
- Journey card tap now starts the existing `680ms` spatial/flip modal enter immediately, with no preliminary impact bounce or awaited animation. After dismiss fully lands, restores the resident Unit card, and crosses the existing two-paint WebKit anti-flash barrier, that real card plays exactly one random existing squash/stretch bounce with one bounded smoke puff. Gameplay-return auto-open still keeps its lifecycle-scoped readiness marker until the exact Forest/Beach/Area 55 Unit is idle and stable or the manager is disposed. `qa:full` passes TypeScript, audit, lint, 115 suites / 690 tests, visual contracts, the 918-module production build, bundle audit, and native source guard. `dist` is current; Stack to Six `Web.bundle`, Xcode app, and iPhone remain untouched. Physical feel remains `NEEDS PHYSICAL TEST`.
- Journey modal dismiss flips in the opposite rotational direction from the earlier implementation (`-180deg → 0deg`) while preserving the accepted `680ms` spatial return and centered physical edge. The later landing-effect timing is superseded by the current in-flight stretch/smoke handoff documented above. Play/Continue return remains unchanged. Native bundle and iPhone remain untouched.
- Journey card entry no longer begins with the `0 → -0.028` spatial retreat: the first segment now moves immediately and monotonically from the tapped Unit card toward the modal while retaining the existing later cartoon overshoot/settle. Journey Close keeps its existing shared `380ms` comic bounce and is no longer hidden at exit start; because it remains physically nested in the back face, it travels/flips with the modal and retires naturally at the 90deg edge. No other modal motion changed. Web-only; focused overlay/portal tests pass 17/17 and `qa:fast` passes 5 suites / 36 tests plus visual contracts. Native bundle and iPhone remain untouched.
- The Journey overlay artwork card now receives the same warm brown depth treatment as the modal when the physical front face is visible: a scoped `drop-shadow(0 13px 17px rgba(185, 145, 119, 0.8))`. It is disabled during exit and does not change the paper shadow, flip, timing, gyro, drag, or any other motion. Web-only; the focused Journey overlay suite passes 12/12 and TypeScript passes. Native bundle and iPhone remain untouched.
- Journey card entry no longer runs the extra post-settle `impactShell` scale bounce (`1 → 1.045 → 0.988 → 1`) after the card/modal reaches its destination. Only that `300ms` landing bounce was removed; spatial entry, flip, stats/CTA timing, gyro, idle coach, drag, exit, and return remain unchanged. Web-only; the focused Journey overlay suite passes 12/12 and TypeScript passes. Native bundle and iPhone remain untouched.
- Journey modal exit no longer visibly levels itself before traveling to the Unit. The live idle/gyro/tilt pose is sampled in the close task, but its neutralization now runs linearly across the complete physical return (`680ms` dismiss, or the `600ms` Play launch+travel landing interval) and reaches identity only on the landing frame. Spatial return, reverse flip, and alignment therefore begin together as one continuous motion; the former standalone-looking `260ms` leveling phase is removed. Web-only; focused overlay/portal tests pass 17/17, `qa:fast` passes 5 suites / 36 tests, and final `qa:full` passes TypeScript, audit, lint, 115 suites / 690 tests, visual contracts, the 918-module production build, bundle audit, and native source guard. `dist` is current. Stack to Six `Web.bundle`, Xcode app, and iPhone remain untouched; physical feel verification remains `NEEDS PHYSICAL TEST`.
- Latest Journey modal correction makes dismiss the mathematical reverse of the accepted enter: both spatial flight and rotor use the same `680ms` lifecycle, retain the source face through 32%, cross the physical edge at exactly 50%, and settle the turn at 68%. Interactive artwork-to-stats turns now derive the stats/CTA trigger from the actual remaining angle (including committed partial drags) instead of a guessed 40% timer. Close/downward dismiss run the stat rows/divider forward with reverse stagger so their hidden terminal state meets the physical edge while the shared CTA runs its canonical exit. A rotor-owned `data-paint-face` changes only across the physical 90deg edge and symmetrically culls the nonphysical face; Journey gyro also removes its redundant nested perspective. This addresses the one-frame artwork takeover and card/paper texture bleed under gyro, drag, and idle without adding sensor targets or persistent animation owners. Web-only; `qa:fast` passes 5 suites / 36 tests and final `qa:full` passes TypeScript, audit, lint, 115 suites / 690 tests, visual contracts, the 918-module production build, bundle audit, and native source guard. `dist` is current. Stack to Six `Web.bundle`, Xcode app, and iPhone remain untouched; physical feel/compositor/FPS verification remains `NEEDS PHYSICAL TEST`.
- Branch: `feature/journey-card-flip-prototype` (created from the complete dirty state of `feature/v700-journey-hub`; no work was discarded).
- Latest Journey flip correction: Close is removed immediately when the automatic stats-to-artwork turn starts, and a persistent `data-face="front"` visual guard prevents WebKit from repainting the separately composited Close child in the final artwork frame. Both interactive artwork-to-stats turns and the initial spatial entry explicitly own `is-flipping-to-back`, exempting that guard only during the turn; Close remains physically nested/backface-hidden and therefore becomes visible at the same physical 90deg/50% edge as the stats face instead of waiting for settle. Web-only; native bundle and iPhone remain untouched.
- Journey flip backdrop timing now starts its `520ms` fade-in in the first visible frame immediately after card tap/overlay mount, independently of rotor progress. Close, Play, and dismiss add the backdrop-exit state in the first closing frame so its `520ms` fade-out runs concurrently with the card return instead of waiting for that return to finish. Web-only; native bundle and iPhone remain untouched.
- The Journey flip back face restores the preceding detail modal's accepted internal enter rather than revealing static content. At the physical artwork-to-stats edge, the shared CTA retains its normal `340ms back.out(1.8)` enter while both stat rows and their divider reuse the prior `detailStatPopOut` animation in reverse at the requested 2× speed: `200ms` each with `25ms` DOM-order stagger. Returning to artwork primes those nested elements hidden only after the back face is no longer visible, so every later stats reveal replays cleanly without affecting rotor, card geometry, gyro, or Close ownership. Web-only; native bundle and iPhone remain untouched.
- The two-sided Journey flip now restores the preceding modal's shared `6.8s` idle float on a dedicated owner outside rotor and gyro, so both artwork and stats faces inherit the same gentle idle without transform collisions; input/flip/close pause it and stable settle resumes it. Every overlay opening also restores the prior randomized opposing tilt profile: artwork samples `4.75–6.25deg` left/right and stats samples `2–3.25deg` in the opposite direction. Reduced Motion suppresses idle while retaining the static composition. Web-only; native bundle and iPhone remain untouched.
- The Journey flip idle coach now alternates after each five-second quiet window. The first cycle shows only `DRAG TO FLIP` with the existing hand swipe and rotor preview; the next shows only `TAP TO FLIP`, with the hand pressing once at centre and the complete card making one contained `0.965 → 1.06 → 0.988 → 1` scale bump. The modes continue alternating, share the existing `2.1s` coach lifecycle, stop immediately on input/flip/close, and remain disabled under Reduced Motion. Web-only; native bundle and iPhone remain untouched.
- Random Journey flip tilt no longer waits for landing/settle. The existing `is-flipping-to-back` / `is-flipping-to-front` direction owners select the destination face's sampled tilt in the first enter/flip frame, and the dedicated idle shell transitions toward it throughout the motion so landing arrives already aligned without a final degree correction. Web-only; native bundle and iPhone remain untouched.
- Journey flip exit removes artwork/paper/pseudo shadows in the first `is-exiting` frame, but the white stats surface itself retains full opacity and leaves exclusively through the physical flip-back; the experimental `320ms` stats fade was reverted per visual direction. The Close flip-back now begins 30 timeline percentage points earlier (`2%` instead of `32%`) and reaches its physical edge at `20%`, while entry timing is unchanged. Before stopping live motion, exit samples the current shared idle matrix plus gyro `translate/transform`, disposes their live owners in the same task, and WAAPI-neutralizes the frozen poses to identity over `260ms`; the random overlay tilt concurrently transitions to exact `0deg`. The return therefore reaches the resident Unit with no idle, gyro, or extra-rotation delta. Reduced Motion uses immediate identity. Web-only; native bundle and iPhone remain untouched.
- Direct Journey overlay dismiss now resumes World/Unit idle from the wrapper's exact live landing transform. The resume path passes `preserveInitialTransform` into a `1.8s` idle ramp, preventing `restoreJourneyBoardCardBaseTransform()` from writing the returned Unit to base Y for one frame before idle takes over; the ticker then eases continuously from the captured landing pose. Other fresh Journey idle starts retain their canonical base reset. Web-only; native bundle and iPhone remain untouched.
- Board-to-Journey return is now strictly serialized: the normal remembered-World Journey enter runs first, the overlay return gate accepts only `journeyV700Phase === 'idle'` with both screen and target Unit at `>=0.99` opacity for two consecutive paint frames, and only then the last-played Stage card opens automatically at full initial opacity. The former overlap that allowed card entry during World `entering` at `0.04/0.08` opacity is removed. Web-only; native bundle and iPhone remain untouched.
- Current online checkpoint: `polish Journey card gestures and per-world progression`, commit `91610bc` on `origin/feature/journey-card-flip-prototype`, with unchanged web package `2.0.634`.
- Rollback boundary: `v815 automate QA and finish Journey motion guards` is the last online checkpoint **without 3D Motion**. `v900` is the first consolidated online checkpoint that includes the shared gyro/3D system across Journey, Homepage, and gameplay cubes.
- Checkpoint `v901` consolidates all work after `v900`. Always run `git status --short` before editing and preserve any newer user work.

## Latest local changes

- Journey Worlds now own independent local Stage progression while preserving global board/save IDs 1-30. Forest, Beach, and Area 55 each present Stages 01-10; an empty World receives its first interim card and a partially completed World receives its next local interim card. All three World progress signs remain visible, locked and modal Stage labels use local numbering, and Beach/Area 55 collectible artwork receives a non-destructive local-number overlay over its baked global number. The Journey flip overlay also supports a downward-only, axis-locked drag dismiss through the same canonical Close lifecycle; upward/short vertical gestures do not close and horizontal drag remains flip-only. The rounded artwork artifact is fixed at all paint boundaries: the portal clone owns the real modal radius and an explicit rounded clip; stable artwork culls the complete rear face; v917 paper overscan uses only a `+12px/+16px` background-size inside a fixed pseudo box; and the remaining front-host drop-shadow is removed because that filter necessarily paints outside the PNG alpha/stroke and was the final visible rounded fringe. Paper/back descendants explicitly own WebKit backface culling. Container size, PNG, flip, paper shadow, and sibling Close remain unchanged. Commit `91610bc` is online with unchanged package `2.0.634`; `qa:full` passes 115 suites / 690 tests and the 918-module build. The exact 797-asset output was synced only to Stack to Six, `qa:ios` passed, the signed arm64 app verified bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, and install-over succeeded on `iPhone 13 blue` at installation URL ending `27A3E2F0-4491-40E2-8F89-630173B7F8FA/Stack%20to%20Six.app/`; that exact bundle launched. The first CoreDevice transfer was interrupted before install and the safe retry succeeded. Legacy Kockice Crash and all other devices were untouched. Physical verification remains `NEEDS PHYSICAL TEST`.
- `v918 prototype physical Journey card flip and stabilize Arcade lifecycle` at commit `9fd74e8` raises the web package version to `2.0.634`. It consolidates the current two-sided Journey Stage card, exact portal return and no-flash landing, Close/overlay/idle/gyro ownership, alternating Drag/Tap coach, prior-modal stats/CTA enter at 2× stats speed, strict game-return sequencing, and the complete Arcade stale-frame/continuation/exit lifecycle corrections documented below. Pre-push `qa:full` passes TypeScript, audit, lint, 114 suites / 681 tests, visual contracts, the 917-module production build, bundle audit, and native source guard. The branch is online at `origin/feature/journey-card-flip-prototype`.
- The exact v918 output was restored to 797 runtime assets (822 raw files including ignored `.DS_Store` metadata), synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed project identity, asset completeness, and source/bundle freshness. The signed arm64 Debug app built successfully with final bundle ID exactly `com.taptapdesign.stacktosix.Stack-to-Six`; final-app entrypoint and asset count match `dist`, and the v918 Journey markers are present. Install-over preserved existing data and completed only on `iPhone 13 blue` at installation URL ending `06A9382B-B843-462F-AD48-B1BA6244F357/Stack%20to%20Six.app/`; that exact bundle then launched successfully. Legacy Kockice Crash and every other device were untouched. Final animation feel remains `NEEDS PHYSICAL TEST` on this installed build.
- A web-only Journey Stage-card flip prototype now replaces the prior two-object card/paper depth swap on branch `feature/journey-card-flip-prototype`. The live Unit still leases a resident-original plus portal clone, but the overlay is now one exact-aspect card with two absolute, identical-size faces: artwork front and paper/stats/Stage-title/CTA back. Portal translation/scale/rotateZ, landing impact, one gyro target, and the `rotateY` rotor have separate transform owners. Entry turns through negative Y angles (`0→-180deg`) and crosses the physical edge at exactly 50%; Play/Continue continues that direction (`-180→-360deg`) so the artwork faces the Unit before the accepted launch/return choreography. The preceding-branch Stage-paper composition is retained inside the current flip-card dimensions with exact `32px` Stage title and centered CTA. The shared Close controller is physically mounted inside the white stats/modal back face. It reveals naturally with that back face when turning toward stats; when the automatic turn starts from stats back toward artwork, a direction-specific lifecycle class removes Close immediately so it never travels across the rotating artwork. Both faces retain brown drop shadows, and the backdrop remains transparent through the first half of entry before reaching the shared `0.56` tint. Manual drag translates with the finger up to `±44px`, now rotates opposite the preceding implementation, and previews at most `36deg`. Reaching `20%` of live card width immediately releases pointer capture and hands the current pose to the automatic bounce-flip without waiting for pointer-up; shorter releases still use the velocity flick or restore, and tap still flips. The five-second idle coach now alternates `DRAG TO FLIP` swipe and `TAP TO FLIP` press/bump demonstrations. No DOM/opacity face swap occurs at the edge, the proven two-paint resident-card restore remains, and World gyro/idle suspension remains. Final web-only validation passes `qa:fast` with 19 suites / 129 tests and `qa:full` with TypeScript, audit, lint, 114 suites / 679 tests, visual contracts, 917-module production build, bundle audit, and native source guard. `dist` is current. Deterministic verdict is `PASS`; visual/gesture feel remains `NEEDS PHYSICAL TEST`. Stack to Six `Web.bundle`, Xcode app, and iPhone were intentionally not changed.
- The physically reported Journey Unit-card tap hitch is now isolated from all background Journey motion work. Before the fix, opening the Stage overlay left the Forest World spatial owner active across `98` targets and its area idle timelines running, registered two additional modal gyro targets, synchronously sampled every Unit card's geometry at tap/next-frame boundaries, and repeatedly forced computed-style plus bounding-rect reads while the modal itself was animating. Card-overlay ownership now suspends the existing World gyro/listener and area idle timelines before the portal transition; the modal keeps its own two live gyro targets; dismissal resumes the same World target set and retained pose without replaying World enter. The forced card-geometry and `modal-axis-sample` diagnostics are removed; a lightweight five-second RAF frame window remains for the physical acceptance trace. Direct-open and gameplay-return overlays both use the same suspend/resume lifecycle, while Play intentionally hands cleanup to Journey exit. New behavior/source contracts pass with focused coverage at 48/48; `qa:fast` passes 19 suites / 127 tests, and `qa:full` passes TypeScript, audit, lint, 114 suites / 677 tests, visual contracts, the 917-module production build, bundle audit, and native source guard. Exact assets were synced only to Stack to Six; `qa:ios`, bundle identity `com.taptapdesign.stacktosix.Stack-to-Six`, signed Xcode build, and install-over on `iPhone 13 blue` pass at installation URL ending `4FF1EBC0-F6F7-420B-8433-93E6F1D838C5/Stack%20to%20Six.app/`. Existing app data was preserved and Kockice Crash was untouched. Deterministic verdict is `PASS`; final card-tap feel and captured after-fix frame window remain **NEEDS PHYSICAL TEST** on this installed build.
- The remaining physically reproduced one-frame dice flash on reused Homepage→Arcade entry was a native WebKit/Pixi compositor ownership bug beyond the already-fixed pop-in watchdog: hiding `stage`/tiles changed only the next Pixi render tree, while `showApp()` and boot could reveal the reused canvas IOSurface that still contained the previous board. Arcade Round entry now has a dedicated surface gate. The complete canvas remains `visibility:hidden/opacity:0` through the transparent Round cue; boot's immediate and `50ms` reveal paths plus `UIManager.showApp()` must respect the gate; fresh and saved-board pop-in runners release it only after `sweetPopIn()` has synchronously prepared every tile at its initial scale/alpha, explicitly render that safe pose, and cross one `requestAnimationFrame` paint barrier. Cue reset/cancel also clears ownership so a failed entry cannot strand the canvas. Three independent audits converged on this retained-framebuffer cause. Focused coverage passes 12/12, `qa:fast` passes 19 suites / 126 tests, and final `qa:full` passes TypeScript, audit, lint, 114 suites / 676 tests, visual contracts, the 917-module production build, bundle audit, and native source guard. Exact assets were synced only to Stack to Six; `qa:ios`, bundle identity `com.taptapdesign.stacktosix.Stack-to-Six`, signed Xcode build, and install-over on `iPhone 13 blue` pass. Existing data was preserved and Kockice Crash was untouched. The user physically tested the installed build on `iPhone 13 blue` and confirmed the complete Homepage → Play → Round 01 → first-board transition is clean with no premature/stale dice flash. Final verdict: **PASS**.
- The remaining physically reproduced Exit→fresh Play dice flash was not stale Pixi state: native timing showed the Round cue acquired at `473394ms`, startLevel began at `473414ms`, and gameplay layout ran at `473423ms`. `rebuildBoard` correctly hid the new tiles, but its generic `800ms` pop-in safety watchdog was armed immediately while the intentionally multi-second Round 01 cue still owned the transparent overlay. It therefore forced every cue-hidden tile to `visible/alpha/scale=1`; cue cleanup exposed them, then the real `sweetPopIn` reset them to scale zero and entered again, producing the exact `dice appear → disappear → true board enters` flash. The watchdog is now armed only after `consumeArcadeEntryCue` settles, immediately before the real pop-in; no-cue boards retain the existing immediate safety behavior. Layout also defers gameplay spatial activation while the Round cue is pending, so `arcade-stage-clear` keeps sole surface ownership until pop-in begins instead of being replaced after ~29ms. Focused cue/spatial coverage passes 45/45, `qa:fast` passes 16 suites / 123 tests, and final `qa:full` passes TypeScript, audit, lint, 113 suites / 675 tests, visual contracts, 916-module production build, bundle audit, and native source guard. `qa:ios` and signed Xcode build pass; install-over completed on `iPhone 13 blue` at installation URL ending `5E469B5E-2D99-4C41-AC6D-C1D094CB783B/Stack%20to%20Six.app/`. Existing data was preserved and Kockice Crash was untouched. Source/agent verdict is `PASS`; final Exit→Play flash acceptance remains **NEEDS PHYSICAL TEST** on this installed build.
- A refreshed physical trace exposed a separate critical Arcade lifecycle race beyond the Clean Board Exit fix: Stage/Round continuation called asynchronous `startLevel(nextStage)` without awaiting it, then ran an external destructive reset/layout while `startLevel` later performed its own reset and unawaited layout. This produced the reported pause, double exit, ghost placeholders, old-board flash on fresh Play, early input restore, and race-prone score/wild carryover. Arcade continuation now awaits one `startLevel` owner; external reset/layout/HUD updates are removed; `startLevel` awaits its sole layout before resolving; outer endgame cleanup cannot run early; and the next-Round modal holds its settled composition for `300ms` before exit. Reused PIXI stage/board/HUD are occluded synchronously before boot's first await and again before the texture boundary, so neither fresh Play nor saved Resume can paint the prior board. Saved-load paths remain hidden until restored state owns visibility; Play taps are serialized behind active `exitToMenu`; the redundant boot RAF layout is removed; and slow layout completion restores authoritative ghost visibility if pop-in completed while it was pending. A deferred-layout behavioral regression test covers that physical slow-device ordering. Both independent animation/Homepage re-reviews are `PASS`. Final `qa:fast` passes 15 suites / 117 tests; `qa:full` passes TypeScript, source audit, lint, 113 suites / 675 tests, visual contracts, 916-module production build, bundle audit, and native source guard. Exact raw assets were synced only to Stack to Six; `qa:ios` and signed Xcode build pass. Install-over completed on `iPhone 13 blue` at installation URL ending `3FF9FEB8-03A6-4A0A-85BA-64CCEC082D22/Stack%20to%20Six.app/`; existing data was preserved and Kockice Crash was untouched. Final fresh Play, Stage→Stage, Exit→Play, and Round 01 ghost/stale-frame acceptance remains **NEEDS PHYSICAL TEST** on this installed build.
- Arcade Round 02+/03+ Clean Board Exit now has one complete, non-interrupting handoff to Homepage. Physical console reproduction showed that the board exit was already resolved when the later-round summary appeared, while the modal overlay was removed only `50ms` after that resolved owner even though its paper/card/CTA exit still required roughly `1050–1350ms`; the board owner also globally killed unrelated tracked tweens, the menu watchdog could start a competing fallback, and the fast path still tore down the warm Homepage slider. Board and modal exits now remain independent and overlay retirement awaits both; board cleanup no longer kills global modal/Homepage motion; the menu watchdog observes without taking ownership; fast Arcade preserves the warm slider and runs exactly one prepared Homepage enter. Navigation/visibility cancellation cannot strand exit promises, every modal completion detaches its per-session navigation handler, and native `[CC_ARCADE_EXIT]` markers cover tap, both owners, and overlay retirement. `qa:fast` passes 14 suites / 114 tests; `qa:full` passes TypeScript, source audit, lint, 112 suites / 672 tests, visual contracts, the 916-module production build, bundle audit, and native source guard. Independent animation-ownership review is `PASS`. The exact raw assets were synced only to the authoritative Stack to Six shell; `qa:ios` and the signed Xcode build pass with bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, and install-over is verified on `iPhone 13 blue`. Existing app data was preserved and Kockice Crash was untouched. Final Round 03+ exit and Homepage slider feel remains **NEEDS PHYSICAL TEST** on this installed build.
- Journey Stage now uses the exact centered Exit Game modal exit, not a similar Journey/World approximation. The complete paper container carrying Close, title, stats, and CTA runs the same two nested Exit Game transform owners in parallel: `end-run-centered-modal-bounce-out` on the outer paper and `end-run-centered-modal-flip-out` on the inner shell, both `650ms` with `cubic-bezier(0.4, 0, 0.2, 1)`. The lifecycle wait is again sourced from the shared `GAMEPLAY_MODAL_BENCHMARK.exitDurationMs`, so cleanup cannot cut off either keyframe. During entry, the exact spatial shell still owns Unit-to-overlay geometry while the excluded nested card shell keeps the requested larger `0.82 → 1.18 → 0.94 → 1.055 → 1` landing bounce over the existing `580ms` flight. The automatic card-to-details switch remains immediate after modal settle (`0ms` extra delay), and return/no-flash, gyro, Close, drag, CTA, and reduced-motion lifecycles are unchanged. Focused Journey coverage and final `qa:fast` pass 10 suites / 94 tests; the 916-module production build passes with `SKIP_NATIVE_BUNDLE_SYNC=true`. `dist` is current, while native `Web.bundle`, Xcode, and phone were intentionally not changed. Final motion feel remains **NEEDS PHYSICAL TEST** on refreshed web/device.
- The Forest Journey Stage presentation begins with the travelling card as the foreground surface before first paint. The standard gameplay paper then bounce-enters behind it, and the web-only test now invokes the existing `beginDepthSwap` lifecycle immediately after that paper fully settles, with `0ms` additional delay. Any user pointer interaction cancels the pending switch; the automatic path suppresses haptic feedback while manual switching keeps it. Card portal/return geometry, accepted no-flash landing, standard paper bounce, gyro, Close placement, and manual gestures are unchanged. Focused coverage passes 12/12 and `qa:fast` passes 10 suites / 94 tests. A 916-module production build completed with `SKIP_NATIVE_BUNDLE_SYNC=true`; its generated JS was inspected for the exact zero-delay automatic path, and the restarted `localhost:5174` responds `200`. Native `Web.bundle`, Xcode app, and phone were intentionally not changed; the installed iPhone build remains the preceding `300ms` version at `/private/var/containers/Bundle/Application/DA1EBAFE-A8B0-4038-8959-4F7DB96320A2/Stack to Six.app`. Final web feel remains **NEEDS PHYSICAL TEST**.
- The rejected custom Forest Journey Stage paper retreat has been replaced with the existing app-standard gameplay-paper motion. Only the Journey modal paper's isolated outer shell now consumes `end-run-centered-modal-bounce-in/out` for `650ms`; its nested flip shell is held at identity so there is one clean bounce owner with no local 3D turn. The prior compact `240ms`/`30ms` stat cascade is restored. Card portal/return geometry, accepted two-paint no-flash landing, gyro owners, Close placement, and depth swapping are unchanged. Focused overlay/portal coverage passes 12/12; final `qa:fast` passes 10 suites / 94 tests, the 916-module production build and `qa:ios` pass, and the signed Stack to Six Xcode build succeeds. The built Web.bundle was inspected for both exact `.65s` standard bounce rules. Verified install-over preserving app data landed on `iPhone 13 blue` at `/private/var/containers/Bundle/Application/095735BD-786E-4018-A248-5E2A2543AEEF/Stack to Six.app` and launched successfully. Final animation feel remains **NEEDS PHYSICAL TEST**.
- The cold/hard-relaunch 3D Motion permission and listener lifecycle is now persistent and physically accepted. The web controller re-requests document-level access once from the next trusted gesture and sends the actual result through the new `motionPermissionResult` native bridge. The authoritative Stack to Six `GameViewController` persists a granted result in `UserDefaults` and implements `WKUIDelegate.requestDeviceOrientationAndMotionPermissionFor`; subsequent bundled `app://localhost` sessions receive `.grant` without another Apple popup, while all other origins retain `.prompt` and the web Settings toggle still owns whether motion is active. `qa:fast` passes 10 suites / 94 tests, the 916-module build and `qa:ios` pass, and the Swift project builds/signs successfully. The explicitly confirmed install-over landed at `/private/var/containers/Bundle/Application/202848BB-ACDB-4D93-9421-199505030AE8/Stack to Six.app`. Two fresh-process physical passes showed no Apple popup and active gyro. The second hard-relaunch trace specifically proved `Restoring persisted motion permission for app://localhost`, `granted:true`, `listener-started` on Journey Hub/World, and live Forest modal motion across both axes up to `targetX=1`, `targetY=-1`. Verdict: **PASS**. The local host still reports the known `CSSMERR_TP_NOT_TRUSTED` development-certificate warning despite device acceptance.
- Physical acceptance on the installed `C8268A57-2670-4117-A382-E165A8EB825A` build passed: the user opened/dismissed multiple Forest Stage cards and reported the former one-frame return blink is gone. Native `[CC_CARD_LANDING]` evidence confirms the resident original was decoded and visible underneath the fully opaque portal clone in the same task and both subsequent paint frames; portal/original terminal geometry differed by less than a pixel before clone retirement. Verdict for the card-return blink: **PASS**. The user clarified 3D Motion was disabled, so gyro is intentionally not part of this acceptance result.
- The user clarified that 3D Motion was disabled during the gyro checks, so gyro is out of scope for the active acceptance pass. The remaining direct Stage-modal dismiss blink is now guarded by a two-paint visual handoff: the already-resident original card is revealed underneath the still-fully-visible terminal portal clone, the modal remains mounted for two complete `requestAnimationFrame` paints, and only then is the clone/stage retired. This removes the prior same-task clone hide + original reveal + stage removal that could expose a WebKit compositor gap. Landing diagnostics now post directly through the native console bridge. `qa:fast` passes 9 suites / 80 tests, production build and `qa:ios` pass, and final app bundle identity/assets/entrypoint parity are verified. Apple confirmed install-over on `iPhone 13 blue` at installation URL ending `C8268A57-2670-4117-A382-E165A8EB825A/Stack%20to%20Six.app/`. Final blink acceptance remains `NEEDS PHYSICAL TEST`.
- After two physically failed fixes, a three-agent deep audit changed the ownership model instead of retuning the same path. The original Journey card now remains resident as an opacity-hidden, dimensionally stable card in its Forest/Beach/Area 55 Unit; a deep visual clone alone travels through the Stage modal. Dismiss hides/retires the clone and reveals the original while scroll remains locked, eliminating the WKWebView compositor-layer rebuild caused by moving the promoted card node between the Journey and modal 3D trees. The clone carries no `data-board-id`, so live-card selectors remain unique. Gyro modal mount now rebases the shared sensor baseline and emits throttled physical axis/pixel evidence (`modal-axis-sample`: raw beta/gamma, baseline, normalized/current tilt, CSS variables, computed transform, and rect); card landing emits same-task plus RAF+1/RAF+2 evidence under `[CC_CARD_LANDING]`. `qa:fast` passes 9 suites / 80 tests, including the new invariant that the live card never leaves its wrapper. Production build and `qa:ios` pass; the signed verified Stack to Six build installed over existing data on `iPhone 13 blue` at installation URL ending `F6447CD4-C562-445D-8791-D436911AB64E/Stack%20to%20Six.app/`. Physical verdict remains `NEEDS PHYSICAL TEST` for no-flash acceptance and measured pitch-axis diagnosis.
- The first refreshed physical pass confirmed the Close alignment fix, but the user still saw no meaningful modal pitch response and still saw a direct modal-dismiss card flash. Native evidence proved both modal targets were registered on the single live listener (178 orientation events), so the modal now consumes the same relaxed-wrist response curve as Journey and has stronger Journey-only Y/pitch gains. The remaining flash was a path asymmetry: only gameplay-return acquisition prepared a settled landing, while a direct Forest card tap restored its captured inline idle transform after portal landing. Direct card acquisition now prepares the same settled presentation before capturing landing geometry, preventing that one-frame transform restart. Regression coverage asserts nonzero modal Y/rotateX and settled preparation ordering. `qa:fast` passes 9 suites / 80 tests, production build and `qa:ios` pass, and the signed verified Stack to Six build installed over existing data on `iPhone 13 blue` at installation URL ending `E33A3AF9-6C8F-4FAA-8B9F-162FE9BC34C9/Stack%20to%20Six.app/`. Final physical acceptance remains `NEEDS PHYSICAL TEST`.
- A live `iPhone 13 blue` follow-up showed the Forest Journey Stage modal needed a more legible physical gyro response and that its local Close override (`-18px`) sat farther right than the shared centered-modal position (`-10px`). The Journey-only card/paper counter-motion gains are now stronger, the Close control matches the shared `top/right: -10px` placement, and narrow native diagnostics report modal-target registration/disposal without adding another sensor listener. The existing settled-style exact-card landing fix remains intact. Focused tests pass 55/55; `qa:fast` passes 9 suites / 80 tests; production build and `qa:ios` pass. The complete authoritative Stack to Six bundle was signed, verified as `com.taptapdesign.stacktosix.Stack-to-Six`, and installed over existing data on `iPhone 13 blue` at installation URL ending `04A3DBE1-B5D9-4519-811D-CF40091F9E00/Stack%20to%20Six.app/`. Final gyro strength, X alignment, and no-flash feel remain `NEEDS PHYSICAL TEST` on this build.
- The current dirty-tree follow-up to online `v917` / web `2.0.633` was freshly rebuilt, reinstalled over the existing app data, and launched on `iPhone 13 blue`. This build includes the Journey gameplay-return landing fix that strips transient World-enter motion state before restoring the exact leased card, preventing its close-time disappear/reappear flash, plus the shared Journey modal gyro shell that keeps the paper and X on one iOS compositor owner and moves X to the safer `-18px` top/right overlap. Final focused coverage passes 15/15 and `qa:fast` passes 9 suites / 80 tests with TypeScript, lint, diff hygiene, and Journey visual contracts. The latest fresh 916-module production build synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios` and the final built-app audit pass bundle identity, raw assets, source/bundle freshness, and exact final-app entrypoint parity. The signed Debug iPhone build succeeded with bundle ID exactly `com.taptapdesign.stacktosix.Stack-to-Six`; the latest Wi-Fi install-over completed at installation URL ending `B5B56C5B-5F15-4B37-9F78-E38F034599F5/Stack%20to%20Six.app/`, and the same bundle launched successfully. Existing app data was preserved and legacy Kockice Crash was untouched. The standalone host trust check reports `CSSMERR_TP_NOT_TRUSTED` for the development certificate despite successful Xcode signing/build and device acceptance. Final card-flash and close-X confirmation remains `NEEDS PHYSICAL TEST` on this installed build.
- Physical `iPhone 13 blue` reproduction confirmed a Journey gameplay-return dismiss flash: the automatic overlay leased the exact card as soon as the returning World became minimally visible, while that card still carried its transient World-enter GSAP `transform/opacity`. The lease stored that intermediate inline style and reapplied it after the reverse spatial flight, briefly hiding/shrinking the landed card before the next Journey owner restored it. Gameplay-return leases now preserve the last painted geometry as the flight source but explicitly prepare and retain a settled landing presentation before capturing the live destination; restore strips only motion/transient inline properties while preserving unrelated custom styles. Direct idle card taps retain their exact original-style restore path. The same physical pass exposed the Journey Stage X/paper compositor split: Journey now nests paper and X inside one transform-isolated gyro shell, mounts the close control on that shared owner, and moves it from `-10px` to `-18px` top/right so it overlaps the paper less while remaining inside the iPhone safe width. Focused portal/overlay/close coverage passes 15/15, final `qa:fast` passes 9 suites / 80 tests, TypeScript, lint, diff hygiene, and Journey static visual contracts; the 916-module no-native-sync production build passes and `dist` is current. Stack to Six `Web.bundle`, native shell, installed app, and phone were not changed, so the fixes remain `NEEDS PHYSICAL TEST` after a refreshed install.
- All active gameplay paper modals now consume the existing single app-level 3D Motion sensor owner instead of creating per-modal orientation listeners. End Game/Exit Stage, High Score/Combo, and Collectible Reward/New Card mount a restrained inner-paper translation plus `rotateX/rotateY` profile, then synchronously dispose it before their accepted exit begins. The Journey Stage overlay has a stronger dedicated opposing-depth pair: a new transform-isolated inner card shell and the stats paper counter-move with different translation, rotation, and Z gains while outer portal flight, drag/swap, enter/exit, and idle owners remain untouched. Its dynamic `Play` / `Continue` primary CTA also fires the accepted selection haptic from the single registered CTA activation callback before starting the existing Play exit; no duplicate click listener or timing owner was added. Modal targets can run while the underlying Journey world is spatially suspended, honor the player 3D Motion setting and Reduced Motion, clean disconnected surfaces, and share the same listener/RAF stream as Homepage, Journey, and gameplay cubes. Focused coverage passes 50/50; final `qa:full` passes type-check, source audit, lint, 112 suites / 667 tests, static Journey visual contracts, no-native-sync production build, built-bundle audit, and native source guard. The haptic follow-up additionally passes its focused 7/7 overlay suite, TypeScript, final `qa:fast` (7 suites / 72 tests), and a 916-module no-native-sync production build. `dist` is current; Stack to Six `Web.bundle`, native shell, installed app, and phone were not changed. Final strength, perceived depth, haptic feel, and iPhone compositor feel remain `NEEDS PHYSICAL TEST`.
- Online checkpoint `v917 refine Journey card presentation and shared modal motion` at commit `fe0b2df` raises the web package version to `2.0.633`. It consolidates the new exact-card Journey Stage overlay/return lifecycle, symmetric tap-and-drag card/stats depth switching, responsive centered pair composition, shared CTA sizing and modal overlay/stat animation contracts, corrected Journey World/card exit ownership, Arcade tutorial/current-Round presentation guards, and the faster randomized pooled Mushroom pollen finale. Pre-push `qa:full` passes every deterministic gate: type-check, source release audit, full lint, 112 suites / 665 tests, Journey visual contracts, no-native-sync production build, built-bundle audit, and Stack to Six native source guard. `dist` was rebuilt; Stack to Six `Web.bundle`, native shell, and phone were not changed. Physical Journey tap/drag/centering and Mushroom timing remain `NEEDS PHYSICAL TEST` on a later refreshed web/device build.
- The online `v917` / web `2.0.633` checkpoint is now installed over the existing app data on `iPhone 13 blue` in bundled mode. A fresh 915-module production build was restored to 822 raw asset files, synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed bundle identity, asset completeness, and source/bundle freshness. The signed Debug iPhone app built successfully; its final `Info.plist` bundle ID is exactly `com.taptapdesign.stacktosix.Stack-to-Six`, its signature verifies, and both native-bundle/final-app entrypoints match `dist` with the expected TapTap and character launch assets. Wi-Fi install-over completed at installation URL ending `FD55C43A-825C-4E98-9365-EE37FCEAE429/Stack%20to%20Six.app/`, and that exact bundle ID launched successfully. Existing app data was preserved and legacy Kockice Crash was untouched. Physical confirmation of Journey tap/drag/centering and Mushroom timing remains `NEEDS PHYSICAL TEST` on this installed build.
- Journey Stage card and stats paper are now vertically centered as one combined composition instead of centering only the foreground surface while the rear peek pulls the pair upward. A single responsive `--journey-card-composition-y: clamp(40px, 5.69dvh, 52px)` contributes about `48px` on the iPhone 13 viewport and is composed into both settled orientations plus every tap/drag swap intermediate, so the entire pair moves down together without changing its internal peek distance, opposing rotations, depth scales, or producing a settle snap. Focused overlay coverage passes 7/7 and final `qa:fast` passes 13 suites / 99 tests with TypeScript, changed-file lint, diff hygiene, and static visual contracts; the 915-module web-only production build passes. `dist` was rebuilt with native sync explicitly skipped; Stack to Six `Web.bundle`, native shell, and phone were not changed. Final optical centering remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- The Journey Stage stats modal no longer looks disproportionately small when it moves behind the foreground card. Its settled rear scale is softened from `0.96` to `0.985`; the existing upward/left peek position, opposing rotation, `z: -80px` depth, drag following, and swap transitions remain unchanged, and the rear card intentionally retains its accepted `0.96` scale. Focused overlay coverage passes 7/7 and final `qa:fast` passes 13 suites / 99 tests with TypeScript, changed-file lint, diff hygiene, and static visual contracts; the 915-module web-only production build passes. `dist` was rebuilt with native sync explicitly skipped; Stack to Six `Web.bundle`, native shell, and phone were not changed. Final depth proportion remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Superseding both incomplete tap attempts: every visible Journey Stage card/stats surface now toggles depth on a short tap, whether that surface is foreground or the exposed rear peek. The shared geometry-aware captured-pointer owner commits `beginDepthSwap` on every valid no-movement release; it no longer rejects `dragStartedOnRear`. The old rear-card `boing only` click branch, timer, class, and keyframes were removed, and the card's click fallback now also always commits the same swap. CTA/form controls and the true outer backdrop remain excluded, while post-pointer synthetic clicks stay suppressed so one tap cannot toggle twice. Drag distance, drag-release switching, swap animation, modal/card state, and exit behavior are unchanged. Focused overlay coverage passes 7/7 and final `qa:fast` passes 13 suites / 99 tests with TypeScript, changed-file lint, diff hygiene, and static visual contracts; the 915-module web-only production build passes. `dist` was rebuilt with native sync explicitly skipped; Stack to Six `Web.bundle`, native shell, and phone were not changed. Final physical hit testing remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Journey Stage `Play` / `Continue` no longer owns a modal-specific `249px` width. It now uses the same shared standard-width modifier as Homepage CTAs: `250px` on wider layouts and `226px` on phone, while retaining the canonical shared CTA visual's `28px` internal horizontal padding, primary styling, motion, input ownership, and centered placement. The modal paper/container width and all Journey card/modal animation owners are unchanged. Focused Journey overlay/shared CTA coverage passes 18/18, `qa:fast` passes 13 suites / 99 tests, TypeScript, changed-file lint, diff hygiene, and static visual contracts; the 915-module web-only production build passes. `dist` was rebuilt with native sync explicitly skipped; Stack to Six `Web.bundle`, native shell, and phone were not changed. Final visual width remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Mushroom finale pollen now completes roughly one second faster and reads as a filled rising field instead of one simultaneous launch. Its existing pooled flock owner is shortened from `7s` to `6s` (safety `7200ms` to `6200ms`); all 72 particles are distributed through shuffled stratified slots across the lower `30%` of the viewport, then assigned to 12 randomized groups of six with independent birth and arrival ordering. Small per-particle timing jitter keeps the clusters organic, while every particle begins its existing arrival flash/fade on the exact frame it reaches its randomized target, so the ending clears in randomized groups without adding another tween, ticker, emitter, texture, or cleanup owner. Focused Mushroom/Beach Ball coverage passes 8/8, final `qa:fast` passes 13 suites / 99 tests, TypeScript, changed-file lint, diff hygiene, and static visual contracts; the 915-module production build passes. `dist` and the Stack to Six native `Web.bundle` were rebuilt by the standard postbuild; the native shell was not built or installed and the phone was not changed. Final density and one-second-faster feel remain `NEEDS PHYSICAL TEST` on refreshed web/device.
- The Journey Stage depth coach no longer says the ambiguous `Swipe to switch`, which could imply navigation to another Stage. It now derives copy from the existing single `cardFront` source of truth: stats in front shows `Swipe to view your card`, and the card in front shows `Swipe to view stats`. Visible uppercase copy is split into two centered 32px lines (`SWIPE TO VIEW / YOUR CARD` or `SWIPE TO VIEW / STATS`) so it remains unclipped at the iPhone 13 width while retaining the accepted per-letter board-game enter/hold/exit and the same five-second hand/drag demonstration. The copy updates at the real depth-swap commit and is revalidated before each coach run; no gesture, Stage, card, timer, or navigation owner was added. Focused overlay coverage passes 7/7, `qa:fast` passes 12 suites / 93 tests, and the 915-module web-only production build passes. `dist` was rebuilt; native `Web.bundle` and phone were not changed. Final copy spacing and motion feel remain `NEEDS PHYSICAL TEST` on refreshed web/device.
- All centered paper/gameplay modals now consume one shared Journey-strength screen-overlay token: `rgba(230, 196, 177, 0.56)`. Journey Stage (including its spatial card-entry tint), board-game High Score/Combo, End Game, collectible reward/New Card, and the 3D Motion intro therefore use the same full-screen peach veil while retaining their existing modal-specific enter, exit, input, and cleanup lifecycles. The prior duplicated `0.24` overlay recipes were removed from those families, and focused source contracts prevent visual drift. `qa:fast` passes 12 suites / 92 tests, TypeScript, changed-file lint, diff hygiene, and static visual contracts; the 915-module web-only production build passes. `dist` was rebuilt, while the native `Web.bundle` and phone were not changed. Browser control was unavailable in this session, so final overlay feel remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Board-game HUD `High Score` and `Combo` modals now reuse the same shared stats motion contract as the new Journey Stage modal. Direct stat-container children are primed before the first visible modal frame, then enter in DOM order with the accepted reverse `detailStatPopOut` beat (`400ms` each, `50ms` stagger); Arcade High Score therefore animates `High score → divider → Rounds cleared`, while Journey High Score and Combo animate their single visible stat without an artificial empty delay. Closing restarts the identical keyframe forward, including when exit interrupts enter, and all tracked classes, animation delays, RAFs, and cleanup timers are cleared by the existing score-modal lifecycle. Reduced Motion exposes stats immediately. Focused shared-modal/stats coverage passes 12/12, final `qa:fast` passes 12 suites / 91 tests, TypeScript and the web-only production build pass. `dist` and the live Vite source contain the change; native `Web.bundle` and phone were not changed. Final visual feel remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Superseding the immediately older exact-v910 coordinator-only attempt: the repeated web repro proved Stage 01 could still read as a lone card at the terminal Journey frame. The full ownership audit found two missing invariants. Card wrappers now receive canonical `data-board-id` and `data-journey-area-id="board-N"` at render time, grouping reads wrapper provenance first, and the World snapshot repairs/logs any zero-or-multiple non-selected membership before animation; selecting Stage 02 therefore cannot orphan Stage 01. Inside the same Unit coordinator timeline and same reverse-cascade start, each ordinary card's inner visual now disables its CSS transform transition/shimmer and completes an opaque `400ms back.in(1.7)` scale-to-zero while its structural island/stump/stars/clouds retain the canonical `480ms back.in(1.25)` World exit. The wrapper only follows the Unit's y motion and becomes hidden after the card reaches scale zero, so the high-contrast card finishes 80ms before its softer structure and cannot remain alone for final viewport cleanup. Complete and parent/child interruption paths share idempotent cleanup; the selected portaled card remains excluded and unchanged. Focused Journey coverage passes 21/21, final `qa:fast` passes 12 suites / 90 tests, TypeScript and the web-only production build pass. `dist` only is rebuilt; native `Web.bundle` and phone remain unchanged. Independent review verdict is `NEEDS PHYSICAL TEST` on refreshed web/device.
- Superseding the two immediately older experiments that split ordinary Journey cards from their Unit structures: three independent audits compared the current exit path with the authoritative `v910` implementation at commit `43cd5e9` and found that split ownership was the regression. Every non-selected Unit is again animated by exactly one coordinator tween over the full `unit.targets` group—card wrapper, island, stump, stars, and clouds together—with the v910 contract (`480ms`, `back.in(1.25)`, `y:28`, `scale:0.65`, `opacity:0`) and the reverse cascade capped inside `130ms`. The extra card-only scale-to-zero timeline, terminal visibility write, and coordinator ownership flag were removed, so no second timeline or cleanup can leave a card after its cjelina or cut it before the grouped exit completes. The selected/active portaled card remains excluded from this coordinator and keeps its already-accepted dedicated exit unchanged; atomic Journey viewport finalization also remains, preventing the removed legacy child-card cascade from resurrecting a random card. Focused Journey coverage passes 20/20, `qa:fast` passes 11 suites / 78 tests, TypeScript and the web-only production build pass. `dist` contains the change; native `Web.bundle` and phone were not changed. Final grouped visual timing remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Ordinary Journey Unit cards now complete their entire exit instead of appearing cut off near the terminal frame. The prior first-class card tween stopped at `scale:0.5` while fading opacity to zero over `≤340ms`, then applied `visibility:hidden`; that was deterministic but visually read as a lifecycle interruption because the bounce never reached its geometric endpoint. Each ordinary card wrapper now keeps opacity at `1`, runs the complete `scale 1 → 0` `400ms back.in(1.7)` bounce-out (with its existing Unit-relative y motion), and only in `onComplete` commits `opacity:0` plus `visibility:hidden`. The structural Unit continues for roughly `480ms`, so the card finishes fully and remains among the first Unit elements without being clipped by viewport finalization. Existing World-exit ownership flags and stale-restore protection remain intact. Focused Journey coverage passes 20/20, `qa:fast` passes 11 suites / 78 tests, TypeScript and web-only production build pass. Vite receives HMR and `dist` contains the change; native `Web.bundle` and phone were not changed. Final terminal-frame smoothness remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Fixed ordinary Journey cards lingering after their island/Unit disappeared. The deeper clash was that World coordinator groups contained card wrappers and structural Unit art, but the coordinator did not mark those targets as authoritative game-exit owners; stale enter/restore helpers could therefore reset a non-selected card after its Unit exit had begun. World exit now sets `__ccJourneyToGameExitTween` on every Unit target, and the central card restore protection recognizes both selected-card and whole-Unit exit ownership. On the next legitimate World enter the coordinator clears that flag before preparing targets. Within every exiting Unit, card wrappers are now explicit first-class targets: they start at the same Unit stagger position, complete a bounded `≤340ms` opacity/scale exit before the `480ms` structural Unit exit, and receive terminal `visibility:hidden`. Structural island/stump/cloud targets retain the existing duration/ease/order. Thus a card cannot remain visible after its own cjelina either through timing or a stale reset. Focused Journey/overlay/motion coverage passes 20/20, `qa:fast` passes 11 suites / 78 tests, TypeScript and web-only production build pass. Vite receives HMR and `dist` contains the change; native `Web.bundle` and phone were not changed. Final grouped card/Unit ordering remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Fixed the lone/random Journey card that could remain on the paper after every real World Unit had already exited. The portaled-card coordinator was incorrectly followed by the legacy `startJourneyExitAnimation()` child-target cascade; that cascade uses hidden/prepared Journey card targets and can explicitly make an already-exited card visible again for its own stagger, producing the screenshot where Stage 01 remained alone. After selected Unit, other Units, nav, and portaled card have completed, the overlay route now calls `finalizeJourneyViewportAfterCoordinatedWorldExit(boardId)` and never re-owns Journey child cards a second time. A scoped source regression asserts that the portaled coordinator contains atomic viewport finalization and cannot call the legacy viewport cascade. Card ordering, new landing bounce, Unit/nav timing, and all other flows are unchanged. Focused test passes 6/6, `qa:fast` passes 11 suites / 78 tests, TypeScript and web-only production build pass. Vite receives HMR and `dist` contains the fix; native `Web.bundle` and phone were not changed. Final absence of resurrected cards remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Journey Play/Continue card exit was professionally refactored to remove the entire unreliable visible reparent/RAF→GSAP ownership boundary after repeated web reports of flight followed by an apparent instant disappearance. For Play only, the exact leased DOM card now remains inside the existing overlay spatial portal for one uninterrupted `1120ms` timeline: `100ms` launch boing, `500ms` curved flight to the frozen Unit geometry, `120ms` full-opacity landing punch `1 → 1.14`, then a `400ms` `back.in(1.7)`-style collapse `1.14 → 0` at that exact Unit position. Opacity remains `1` until the terminal frame; only when fully invisible is the same node discarded. There is no Play `restoreNow`, global re-query, second GSAP card owner, historical inline-style restoration, or opportunity for idle/World-enter cleanup to interrupt visible card pixels. At collapse start, `startOverlayPortaledCardJourneyExit` owns only the selected Unit remainder (`skipCard:true`), other Units with selected excluded, nav, and subsequent viewport exit, and it awaits the portal card's terminal completion before handoff. Dismiss/X still uses the established visible restore path. Direct-tap and gameplay-return overlays share the same two semantic callbacks. Focused overlay/portal tests pass 10/10, `qa:fast` passes 11 suites / 78 tests, TypeScript and web-only production build pass. Vite receives HMR and `dist` contains the refactor; native `Web.bundle` and phone were not changed. Final visual acceptance remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Deep Journey ownership audit found the concrete remaining missing-bounce clash, especially on the gameplay-return auto-open route: a still-finishing asynchronous World-enter completion iterates every card wrapper and calls `restoreJourneyBoardCardVisualTarget` plus `restoreJourneyBoardCardInnerVisual`; both helpers previously executed `gsap.killTweensOf(...)` and reset scale/opacity even when `animateJourneyCardTapExit` had already marked that wrapper `__ccJourneyCardTapExitActive`. Idle cleanup honored this protection, but these shared restore helpers did not. Both helpers now enforce the protection invariant before any kill/reset, closing the central async ownership hole for all callers. After the already-added post-overlay double-RAF paint boundary, the local card exit now has an unmistakable isolated two-beat visual owner: `1 → 1.14` for `120ms` with `back.out(2.4)`, then `1.14 → 0` for `400ms` with `back.in(1.7)`. The scoped `journey-card-tapping` state pauses shimmer/CSS transition during the tween and is cleaned on complete/interrupt; GSAP uses `overwrite:'auto'` for sole ownership. Modal, spatial flight, other Units, nav, and viewport timing are unchanged. Focused overlay/portal tests pass 10/10, `qa:fast` passes 11 suites / 78 tests, TypeScript and web-only production build pass. Vite receives the source via HMR and `dist` contains the change; native `Web.bundle` and phone were not changed. Final card bounce visibility remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Fixed the remaining Journey overlay landing-card disappearance without changing modal, World, Unit, nav, or viewport choreography. Three focused reviews agreed that the exact leased card was restored correctly, but `restoreNow()`, overlay removal, and the v900 GSAP `400ms back.in(1.7)` local exit all began inside one render task. WebKit could therefore coalesce reparent and shrink before committing a single Journey-layer frame, making the card appear to vanish instantly. Play now restores and explicitly primes the exact card/wrapper visible, completes `settle('play')` so the overlay stage is removed, forces layout, and crosses a deterministic double-`requestAnimationFrame` presentation barrier before releasing `onPlayCardLanded`. Only then does the unchanged v900 card exit begin. Focused overlay/portal tests pass 10/10, `qa:fast` passes 11 suites / 78 tests, TypeScript and the web-only production build pass. Vite received the source reload and `dist` contains the change; native `Web.bundle` and phone were not changed. Final landing-card visibility remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- The Journey overlay CTA handoff is now structurally matched to the actual `v900` exit at commit `993ab3b`, rather than the previous visually different hybrid. The overlay reverse-FLIP remains only a prelude: immediately before its landing target is captured, the selected Unit's idle owners are stopped so card and Unit cannot drift to different frames. After the exact card is restored and all overlay transforms/classes are released, the v900 sequence begins: selected card shrinks `1 → 0` for `400ms` with `back.in(1.7)` and full opacity; at `+100ms`, the selected Unit remainder, every other Unit (with selected excluded), and navigation begin together; after those finish, the original full Journey viewport exit is awaited instead of atomically finalizing the screen. The experimental extra `1.12` punch was removed because it does not exist in v900. Both direct-tap and gameplay-return overlays use this same sequence. Three parallel code reviews compared exact v900 constants, selectors, owners, and ordering against current source. Focused overlay/portal tests pass 10/10, `qa:fast` passes 11 suites / 78 tests, TypeScript and the web-only production build pass. The live Vite server on port 5174 receives the source through HMR and `dist` contains the build; native `Web.bundle` and the phone were not changed. Final v900 visual match remains `NEEDS PHYSICAL TEST` on refreshed web/device.
- Follow-up runtime audit found why the new Journey Play/Continue choreography looked unchanged on web: the overlay opened by a direct Unit tap had the new landing callbacks, but the automatically reopened overlay after returning from Journey gameplay did not, so that important route still fell back to the old `startBoardAreaThenJourneyExit` path. Both overlay entry routes now create the same landing deferred, start `startOverlayCardLandingJourneyExit` at the 200ms lead callback, resolve it on exact-card restore, and pass the resulting early exit promise into `startJourneyBoardFromOverlay`. The selected card's terminal Unit animation is also now visibly standard/cartoon rather than the former plain shrink: it punches `1 → 1.12` for `100ms` with `back.out(2.4)`, then collapses `1.12 → 0` for `400ms` with `back.in(1.7)` while the selected Unit remains last. Two source assertions prevent the direct-tap and gameplay-return callback wiring from diverging again. The stale Vite process on port 5174 was safely restarted with forced dependency re-optimization; the current DEV server is live at `http://localhost:5174` / `http://192.168.1.189:5174`. Focused tests pass 6/6, `qa:fast` passes 11 suites / 78 tests, TypeScript and the web-only production build pass. `dist` contains the fix; native `Web.bundle` and the phone were not changed. Final visual timing remains `NEEDS PHYSICAL TEST` in the refreshed web runtime and later on device.
- Journey Stage Play/Continue now uses a dedicated two-phase selected-Unit exit instead of hiding the portaled card inside one early terminal collapse. The Stage modal keeps its standard foreground/rear ordering; the card gets a `100ms` launch boing followed by a `500ms` curved return to a frozen Unit target. Exactly `200ms` before landing, every non-selected Unit plus Journey navigation starts the coordinated World exit while the selected Unit remains excluded and stable. On the landing frame the same leased DOM card is restored into its original Unit and `runClickedJourneyBoardUnitExit(..., 'regular-card')` performs the canonical local `400ms back.in(1.7)` second bounce-out, with the selected Unit remainder following and completing last. This removes the prior invisible/discarded arrival and preserves one transform owner per phase. A stationary tap on the foreground card once again invokes the existing depth swap to bring the Stage modal forward; rear-card taps while the modal is already foreground remain a harmless boing. Three independent reviews agreed on the selected-last ownership model. Focused overlay/portal tests pass 10/10, `qa:fast` passes 11 suites / 78 tests, TypeScript and the web-only production build pass. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` still contain the previously installed build and were not changed. Final timing/feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- The complete current Journey card/modal and Board Fail star iteration is now installed over Wi‑Fi on `iPhone 13 blue`. Before native mutation, `qa:full` passed 112 suites / 661 tests plus every type/lint/audit/visual/build/native-source gate. Complete raw assets were restored into `dist`, the exact build was synced only to `/Users/user/Stack to Six/Stack to Six/Web.bundle`, and `qa:ios` passed both before and after the signed build/install. The final `.app` bundle ID is exactly `com.taptapdesign.stacktosix.Stack-to-Six`; `dist`, native `Web.bundle`, and final-app `index.html` share SHA‑256 `74ba643f5fbfbc2e65fcff5eddd8c67f5143634de2f292226e2980e2eba9eb1`, and the runtime raw asset guard passed. After one host-side CoreDeviceService initialization timeout, only that service was restarted; the paired target resolved as `iPhone 13 blue` at CoreDevice ID `0F62B71E-0B04-53C3-906E-EC28F5D2390B`. The long-running Wi‑Fi install session was preserved through tunnel/usage-assertion acquisition and returned explicit `App installed` at installation URL ending `C45F2863-BC67-4B35-B56B-24D09C981401/Stack to Six.app`; that exact bundle launched successfully at 17:25:23. Existing app data was preserved, and no fallback device, Capacitor shell, or legacy Kockice Crash project was touched. Physical Play/Continue arc/bounce and Board Fail star feel now remain `NEEDS PHYSICAL TEST` by the user.
- Three independent agents traced the reported unchanged Journey Play/Continue exit and confirmed the CTA reaches the new path in both depth orders; the failure was phase composition, not routing or CSS specificity. The prior shrink began at 270ms while travel was only about 56% complete, hiding most of the arc, and modal-front delayed the card another 128ms. Play now starts one card return immediately in both depth orders and uses three explicit non-overlapping beats inside exactly 500ms: `0–80ms` whole-composition launch boing while spatial progress stays zero, `80–400ms` full-size travel along the bounded `14–34px` tangential arc, and `400–500ms` `1 → 1.06 → 0` bounce-out only after spatial progress reaches the frozen Unit target. World exit is released at the landing beat. Scale/opacity ownership moved from the inner leased image host to the complete preview flip shell so shimmer/burn/card read as one object. The card stays opaque through 90% of terminal collapse, remains portaled until invisible, and X/backdrop keeps its accepted 620ms restore. Focused overlay/portal tests pass 10/10, `qa:fast` passes 11 suites / 78 tests, TypeScript and web-only production build pass. `dist` contains the change; native `Web.bundle` and phone were not changed. Final physical feel remains `NEEDS PHYSICAL TEST` after installation.
- Board Fail's three empty stars no longer run nested opacity-zero enters that made the later third star appear to vanish or fail rendering. The stars row is now the sole visibility/enter owner; each star remains at its authored `0.9` opacity and receives only a compact staggered scale bounce (`1 → 1.12 → 0.98 → 1`) after the row begins settling. This preserves the left-to-right cartoon beat without any individual disappearance, while existing fail-modal exit ownership and cleanup remain unchanged. A focused source regression locks out per-star opacity priming and covers the new bounce. Focused CTA/fail coverage passes 11/11, `qa:fast` passes 11 suites / 77 tests, TypeScript and web-only production build pass. `dist` contains the change; native `Web.bundle` and phone were not changed. Final bounce feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey gameplay return now uses the exact same card-then-modal enter cadence as a direct Unit tap. The return-only `modalEnterStartProgress: 0.24` override (about 139ms) and its configurable API were removed; every presentation now shares the single benchmark `JOURNEY_CARD_MODAL_ENTER_START_PROGRESS = 0.55` (about 319ms). This prevents the Stage paper/stats/CTA from appearing almost simultaneously with the enlarging return card and removes the reported overlapping ghost composition. Early World/Unit enter eligibility and the card's inherited opacity ramp remain unchanged. Focused overlay tests pass 6/6, `qa:fast` passes 10 suites / 66 tests, TypeScript and web-only production build pass. `dist` contains the fix; native `Web.bundle` and the phone were not changed. Final return sequencing remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Play/Continue card return no longer has the physically choppy mid-exit owner handoff. The Play path snapshots the Unit landing geometry before World exit, uses a monotonic spatial curve, and keeps the exact card portaled while one continuous RAF timeline owns both the return flight and the overlapping `1 → 1.08 → 0` landing bounce-out. World exit is released only when the card reaches the 68%/near-Unit beat; the card therefore never chases an already-moving anchor. The node is discarded only after its terminal invisible frame, eliminating the former visible reparent at roughly 45% scale, CSS-variable transform removal, and second WAAPI continuation. X/backdrop dismiss retains the accepted visible reverse-FLIP/restore bounce. Three independent motion/lifecycle/handoff reviews identified and agreed on this ownership fix. Focused overlay/portal tests pass 9/9 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; no phone install was performed. Final iOS feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey `Swipe to switch` coach color treatment is now white-only: `SWIPE` animates at full white opacity, while `TO SWITCH` uses the same white at a capped 90% visible opacity through the enter overshoot, hold, and exit anticipation. Both return to zero opacity in their shared staggered terminal exit. Animation ownership, neutral gameplay shadow, bottom position, recurring idle timing, hand, and card/modal gesture remain unchanged. Focused overlay tests pass 6/6 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed.
- Journey Stage card taps no longer invoke depth swap. A stationary tap on either the foreground or rear card now only restarts a scoped 240ms gentle boing on the existing flip/idle owner (`1 → 1.045 → .99 → 1`); it does not change `cardFront`, z-order, modal state, or focus. Both card and modal therefore switch only after the established committed drag/flick threshold. Drag-generated synthetic clicks remain suppressed, and enter/swap/close states reject the tap effect. The timer/class has complete cleanup and Reduced Motion continues to disable the animation. Focused overlay tests pass 6/6 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Final tap-boing feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Fixed the invisible recurring `Swipe to switch` copy. Its per-letter lifecycle was previously attached while the coach layer was hidden, so the complete enter/hold/exit ran during the initial five-second idle delay and left every letter at its terminal zero-scale state before presentation. Letters are now statically primed hidden and acquire the board-game splash animation only from `.is-swipe-coach-active`, guaranteeing a fresh restart on every recurring coach cycle. The requested two-tone addon is explicit: `SWIPE` uses canonical orange `#E8744A` with its dark-orange shadow, while `TO SWITCH` is white with the gameplay neutral shadow. Sequential elastic enter, readable hold, anticipation, staggered scale exit, bottom position, random gesture, hand, and real surface stretching remain unchanged. Focused overlay tests pass 6/6 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the fix; native `Web.bundle` and `iPhone 13 blue` were not changed. Final on-device legibility remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Stage Play/Continue card return now has a distinct terminal landing lifecycle. The same live card still reverse-FLIPs for 620ms to the exact moving Unit geometry and is reinserted on the terminal frame, but a successful Play landing immediately continues into a 320ms local card bounce-out (`1 → 1.08 → 0`, `back.in`-style easing) on the real card inside its cjelina. Opacity stays full through 92% of the shrink, then reaches zero; the card is explicitly left `visibility:hidden`, `opacity:0`, and `scale(0)` until the already-running Journey/World cleanup, so it cannot sit visibly on the Unit after landing. X/backdrop dismiss keeps the existing visible card restoration. The Web Animation owner is tracked, cancellable, releases its fill effect after baking the hidden terminal styles, and safe fallback/Reduced Motion paths hide immediately. Focused overlay/portal tests pass 9/9 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Final landing/bounce overlap remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Stage modal paper no longer has any tap/keyboard depth-switch action in either depth position. A stationary pointer sequence on the foreground or exposed rear modal now returns to rest and does nothing; only an intentional modal drag/flick can swap it, while tapping the card itself retains the existing card-owned switch behavior. X and CTA remain normal controls, backdrop dismissal remains scoped to the true outside stage, and the rear modal keeps its complete geometric drag hit area with a grab cursor. Obsolete modal click/pointerup/keydown listeners, button role/tabindex/aria-label, cleanup calls, and focus-loop membership were removed. Focused overlay tests pass 6/6 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Final modal drag/tap feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey `Swipe to switch` copy now uses the board-game splash text lifecycle instead of appearing and then being hidden with its parent. Every Baloo2 letter enters sequentially with an elastic zero-scale pop and overshoot, holds readable during the hand gesture, anticipates briefly, then exits sequentially by bouncing/scaling back into itself before the 1.9s coach owner completes. The complete phrase now uses the canonical orange `#E8744A` with the darker orange gameplay shadow; copy size, bottom 40px safe-area anchor, recurring five-second idle schedule, randomized 100–140% gesture, hand, and actual card/modal stretching are unchanged. Focused overlay tests pass 6/6 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Final splash timing remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey gameplay return no longer waits for the viewport/World to reach idle plus six stable geometry frames before reopening the selected Stage composition. The single return owner now becomes eligible during the canonical World/Unit `entering` phase and leases the exact selected card as soon as its wrapper and Journey viewport have measurable geometry and begin painting (`>= .08` wrapper and `>= .04` screen opacity). The Unit wrapper continues its normal enter while that same card departs immediately into the 580ms spatial flight. The portal inherits the card's current effective World×viewport opacity and ramps it to one during the first 38% of flight, preventing a low-opacity Unit card from flashing fully opaque when reparented. Return-only modal overlap begins at 24% of card flight instead of the standard 55%, so the Stage paper follows about 139ms after launch while ordinary card taps keep their accepted timing. The overlay still remains open until explicit player input. Focused overlay/portal tests pass 9/9 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the combined change; native `Web.bundle` and `iPhone 13 blue` were not changed. Overlapping World/card/modal feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- `Swipe to switch` is now a recurring idle coach rather than one-shot. After every uninterrupted five-second idle window it chooses a fresh horizontal direction and random amplitude from 100–140% of the base gesture, then uses the real foreground/rear drag variables for the same 1.9s hand/card demonstration and rear 20% counter-follow. Completion schedules the next five-second idle window; any real pointer input cancels the active timer/RAF/motion and restarts the countdown. Copy is now anchored exactly `40px + bottom safe area` from the physical bottom. Reduced Motion continues to suppress it, and close/route/dispose cleanup remains symmetric. `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Final random gesture/text placement remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Stage rear-surface drag no longer depends solely on WebKit's hit target for transformed 3D shells. A stage-level capture fallback now resolves pointer coordinates against the live foreground and rear `getBoundingClientRect()` values: foreground pixels retain priority, while every exposed pixel of the visible rear card or paper (including the blank paper below the Stage title) starts the same rear drag/switch gesture even when iOS reports the backdrop as the event target. CTA, X, and other controls remain excluded, overlapping foreground pixels cannot be stolen, and the added listener has symmetric cleanup. Focused overlay tests pass 6/6 and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Transformed iOS hit-area feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Stage depth drag now works from either surface at either depth, not only the foreground. The card preview and modal shell resolve whether the gesture began on the front or rear surface; a rear drag receives the full established axis-reduced visual travel while the foreground follows at 20%, with counter-rotation/tilt. On committed release/flick, the choreography preserves that ownership: the dragged rear surface continues in the hand's direction and becomes foreground while the former foreground counters behind it. Sub-threshold release still uses the existing overshooting snapback. The rear modal's outer shell supplies the hit area while its inert children remain noninteractive; CTA/X controls remain excluded. The additional pointerdown listener is removed in every overlay cleanup path. Enter, exit, thresholds, positions, tap-to-switch, and front drag are unchanged. Focused overlay tests pass 6/6, `qa:fast` passes 10 suites / 66 tests, and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Rear-drag feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Stage Play/Continue now returns the exact live card to its Unit instead of running the former centered `950ms` card scale-out and discarding it. Both depth orders use the same `620ms` reverse spatial FLIP as dismiss: a foreground card starts returning immediately; a rear card starts when the second exit surface begins. For the modal-front case the card captures its stable source before `onPlaySecondSurfaceExitStart` begins the Journey screen exit, then follows the Unit's live geometry while that World moves. The card lands/restores into the Unit and proven landings are preserved through `play` cleanup; only a failed/unlanded Play return is discarded safely. The duration-aware 40% overlap now uses `620ms` for CTA returns too, keeping the fast `320ms` modal exit complete before card landing. Modal/stats exit, early Journey handoff, CTA behavior, and dismiss behavior otherwise remain unchanged. Focused overlay/portal tests pass 9/9, `qa:fast` passes 10 suites / 66 tests, and `qa:full` passes 112 suites / 660 tests plus all build/audit gates. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Moving-Unit landing feel remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey gameplay Exit Game now completes a deliberate full-circle return instead of restoring the Stage overlay instantly. The containing Forest/Beach/Area 55 screen and every Unit complete their normal coordinated enter first, with the selected board card visible inside its Unit throughout that enter. The existing return owner then waits for World idle, no viewport preparation/animation, the exact expected World, and six stable card-wrapper geometry frames. It automatically acquires that same live card, pauses its idle exactly like a real tap, captures its stable landing geometry, and runs the standard exact-card spatial enter followed by the Stage modal/CTA/stats enter. The obsolete `restored-open` presentation, hidden newly-rendered return placeholder, and 500ms auto-dismiss were removed, so the opened card/modal composition now remains interactive until the player explicitly closes it or chooses Play/Continue. The return marker remains pending until a proven card landing or safe cancellation. Focused overlay/portal/origin tests pass 17/17, `qa:fast` passes 10 suites / 66 tests, and `qa:full` passes 112 suites / 660 tests plus all build/audit gates. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed. Final seamless timing remains `NEEDS PHYSICAL TEST` when installation is requested.
- Journey Stage modal stats now animate out whenever that modal surface actually begins its exit, whether the modal is foreground or rear and whether dismissal comes from X/backdrop or Play/Continue. The exit reuses the legacy forward `detailStatPopOut` keyframe in DOM order `High score → divider → Longest combo`, compacted to `240ms` per item with `30ms` stagger so the full stats cascade finishes at `300ms`, 20ms before the Stage paper's `320ms` exit. If exit interrupts the stats enter, the enter owner/timer is cleared and the exit keyframe is deterministically restarted after one layout flush. Card exit/return, modal timing, positions, haptics, and Reduced Motion are unchanged. Focused overlay tests pass 6/6, `qa:fast` passes 10 suites / 66 tests, and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed.
- First-play Arcade tutorial runs no longer arm or display the regular `Round 01` entry cue. `startNewGame()` now uses the existing `beginFirstPlayTutorialRun('arcade')` decision as the single gate: an accepted tutorial explicitly clears any stale `__ccArcadeContinuationCueRound`, while every non-tutorial fresh Arcade run still arms Round 01 exactly as before. Saved Arcade continuation cues, board pop-in ordering, Journey, and post-tutorial runs are unchanged. Focused Arcade/tutorial lifecycle tests pass 14/14, `qa:fast` passes 10 suites / 66 tests, and `qa:full` passes 112 suites / 660 tests plus all build/audit gates. `dist` contains the change; native `Web.bundle` and `iPhone 13 blue` were not changed.
- The Journey Stage modal no longer paints its former full-paper warm radial burn/mix-blend lighting when the modal settles. Only that modal-content lighting layer, its `is-modal-impact` class, and its cleanup timer were removed; the card's own shimmer/burn, modal 3D enter, legacy stat cascade, light screen impact, haptic, layout, drag, and exit remain unchanged. The focused overlay contract passes 6/6 and `qa:fast` passes 9 suites / 60 tests. Native `Web.bundle` and `iPhone 13 blue` were not changed.
- The new Journey Stage card/modal now reuses the accepted legacy detail-stat enter contract when its modal surface begins the 3D enter. Its direct children animate in DOM order `High score → divider → Longest combo` with the shared reverse `detailStatPopOut` keyframes: `400ms` per element and `50ms` stagger (`0 / 50 / 100ms`). All three are primed hidden before the first visible modal frame, the cascade begins in the same lifecycle tick as `cc-gameplay-modal-entering`, and its scoped classes/animation delays clean up after the `534ms` complete sequence. Restored-open gameplay returns remain immediately populated, while Reduced Motion exposes the three elements without animation. No duplicate timing/easing helper was added: Stage consumes `detail-modal-stats-enter-motion.ts`, the same contract as the legacy Journey detail modal. Focused overlay/detail-stat tests pass 11/11, `qa:fast` passes 9 suites / 60 tests, and `qa:full` passes 112 suites / 660 tests plus every build/audit gate. `dist` contains the iteration; native `Web.bundle` and `iPhone 13 blue` were not changed. Physical stagger/3D composition remains `NEEDS PHYSICAL TEST` when the user requests installation.
- Centered paper/gameplay modal headlines now use one modular typography contract: `--cc-gameplay-modal-title-size: 32px` plus the semantic `.cc-gameplay-modal-title` class. End Run, Score, Collectible Reward, launch 3D Motion, and Journey Stage all consume that same token at every viewport width. Their former local `40px` declarations and the redundant mobile/short-height overrides were removed, so later stylesheet order cannot make Stage or another paper modal diverge again. Modal-specific color, weight, line-height, spacing, and motion remain local; intentionally large full-screen celebration titles such as New Reward and Arcade Stage Clear are outside this paper-modal contract and unchanged. Focused modal contracts pass 26/26, `qa:fast` passes 8 suites / 56 tests, and final `qa:full` passes 112 suites / 659 tests plus all build/audit gates. `dist` contains the shared 32px title contract; native `Web.bundle` and `iPhone 13 blue` were not changed. Physical typography balance remains `NEEDS PHYSICAL TEST` when the user requests installation.
- Journey Stage modal enter uses the complete accepted 3D Motion permission-modal profile on its existing nested motion owners: the `580ms` Stage lifecycle reproduces the deep `translateZ(-180px) / rotateX(17deg) / rotateY(-88deg)` arrival and restrained counter-tilt settle. Its exit deliberately does **not** use the 3D Motion tilt-away: the nested flip owner stays at identity while the outer shell alone runs the accepted `end-run-centered-modal-bounce-out`, now in a dedicated fast `320ms` lifecycle. Exit overlap is derived from the surface actually in front instead of the former global `950ms` guess. On dismiss with the card in front, the modal starts at `248ms` and finishes at `568ms`, while the exact card reverse-FLIP lands at `620ms`; with the modal in front, it finishes at `320ms` and the card then completes its return. The backdrop fade receives the calculated remaining lifecycle so cleanup cannot cut it short. The card retains its existing `950ms` non-spatial Play exit and `620ms` exact dismiss return. A timing regression covers both depth orders. Focused overlay tests pass 6/6, `qa:fast` passes 9 suites / 60 tests, and `qa:full` passes 112 suites / 660 tests plus all build/audit gates. `dist` contains this fast, duration-aware exit; native `Web.bundle` and `iPhone 13 blue` were not changed. Final motion feel remains `NEEDS PHYSICAL TEST` when the user requests installation.
- Every Journey Stage card/modal presentation now creates one stable randomized opposing tilt pair when it opens. One orientation puts the card at `-4.75…-6.25deg` and the modal at `+2…+3.25deg`; the other mirrors both signs. The choice and small magnitude variation are sampled once per presentation, stored as stage-scoped CSS variables, and reused across initial exact-card entry, modal reveal, drag, snapback, depth exchange, restored-open gameplay return, staged exit, and exact-card return so no frame can re-randomize or jerk. The existing swap-only separation rotations remain independent. Focused overlay/portal tests pass 8/8, `qa:fast` passes 7 suites / 44 tests, and `qa:full` passes 112 suites / 658 tests plus all build/audit gates. `dist` contains this randomized opposing tilt; native `Web.bundle` and `iPhone 13 blue` were not changed. Final visual balance remains `NEEDS PHYSICAL TEST` when the user requests installation.
- Journey card/modal paper now paints 16px farther down inside its existing rounded container, with 6px horizontal overdraw to prevent transparent side slivers from the torn-edge PNG. The scoped `.journey-card-overlay-paper::after` override enlarges only the background paint box; modal dimensions, centering, CTA position, 40px layout padding, clipping, burn layer, drag, and enter/exit transforms are unchanged. The focused overlay contract passes 4/4, `qa:fast` passes 7 suites / 43 tests, and `qa:full` passes 112 suites / 657 tests plus all build/audit gates. `dist` contains this paper extension; native `Web.bundle` and `iPhone 13 blue` were not changed.
- The Journey card/modal overlay is now completely smoke-free. Its smoke-only lazy import, preload promise, 300ms lead timer, particle emission, and teardown cleanup were removed instead of merely hiding the particles; the shared Journey/New Card/gameplay smoke engine was restored to its prior implementation and remains available to its existing non-overlay owners. The 580ms exact-card entry, modal overlap, shimmer, burn-in, screen impact, haptics, drag/depth swap, 950ms staged exit, and 620ms exact return are unchanged. A focused source contract prevents the overlay from importing or calling the smoke engine. Focused overlay/portal/gameplay-FX tests pass 11/11, `qa:fast` passes 7 suites / 43 tests, and `qa:full` passes 112 suites / 657 tests plus all build/audit gates. `dist` contains this smoke-free overlay iteration; native `Web.bundle` and `iPhone 13 blue` were not changed. Final animation feel remains `NEEDS PHYSICAL TEST` when the user requests installation.
- Journey exact-card reverse FLIP now preserves the card's captured visual offset from its live `.journey-board-card-wrapper` instead of targeting the wrapper center. The offset is scaled with the moving World/Unit wrapper on every return frame, so the terminal overlay pixels already match the reparented card pixels and there is no former roughly 24px upward correction after landing. A focused regression reproduces a translated card inside a moving/scaling wrapper and proves the live target retains the offset; focused Journey tests pass 15/15, `qa:fast` passes 7 suites / 43 tests, and `qa:full` passes 112 suites / 657 tests plus all build/audit gates. `dist` contains the fix; Stack to Six native `Web.bundle` and `iPhone 13 blue` were not changed.
- Journey card/modal now has one general full-circle portal lifecycle for Forest, Beach, and Area 55 (`Stage 01…30`). A regular Unit tap captures the exact pre-pause card pose, reparents that same DOM card into a dedicated spatial shell, and flies it directly to the established rear-card/modal-front composition; there is no cloned image or center-origin spawn. Entry and return use separate X/Y FLIP scale, a projection-free card plane and its true visual transform origin; the overlay frame inherits the source card aspect ratio while retaining the same 40px side gutters/modal width, the New ribbon remains through entry and is removed only after landing, and the post-pause base pose is captured independently for the reverse destination. Dismiss preserves benchmark modal exit timing, samples the final painted idle pose before neutralizing it, starts the exact-card reverse flight at the configured overlap, follows the live Unit rect, and reinserts the same node on the terminal frame before overlay cleanup. The obsolete centered preview-enter keyframe is removed so it cannot reactivate after spatial landing, and transient one-shot shimmer state is not restored onto the Unit card. Gameplay exit returns to the exact saved World/scroll, waits for both viewport and coordinated World enter to finish plus six stable geometry frames, restores modal-front/card-rear for 500ms, then auto-closes and lands the card. The return intent is peek/ack rather than consumed early, is scoped through a single in-flight manager owner, supports boards 26–30, acknowledges only a proven connected landing, and reveals/clears safely on timeout, interruption, navigation, pagehide, or target loss. Reduced Motion removes the spatial delays as well as CSS motion. Focused portal/overlay/origin tests pass 14/14; `qa:fast` passes 7 changed suites / 42 tests and final `qa:full` passes all 112 suites / 656 tests plus type-check, lint, release/bundle audits, static visual contracts, no-native-sync production build, and the native source guard. `dist` contains this source iteration. Native `Web.bundle` and `iPhone 13 blue` were intentionally not synced or installed; first-frame/landing feel and sustained iOS smoothness remain `NEEDS PHYSICAL TEST` when the user requests installation.
- Journey overlay visual drag is now axis-specific: horizontal travel is reduced another 40% from `0.36×` to `0.216×` of the original implementation, while vertical travel is reduced another 30% to `0.252×`. Tilt and rear-surface follow derive from those reduced painted deltas; raw distance/flick recognition, snapback, depth exchange, and idle ownership remain unchanged. This remains web/source-only and is not installed.
- The Journey overlay card now gains the New Card quiet idle language after its reveal settles: a dedicated inner owner runs the same `3s` ease-in-out `-8px / 1deg / 1.02×` float-bounce without changing the card's position, drag, depth, enter, or exit owners. Idle pauses during active drag, snapback, depth exchange, and exit, and Reduced Motion still removes it. Card and modal drag rendering is reduced to exactly `60%` of finger travel (including proportional tilt and the rear surface's follow), while the established raw-gesture `100px` distance and `0.35px/ms` flick thresholds remain unchanged so only visual reach—not switch intent—changes. This remains web/source-only and is not installed.
- Card-front Journey overlay composition is lowered by exactly `32px`: when the card is active in the foreground, its centered destination moves down by `32px` and the modal in its rear peekaboo position moves down by the same amount, preserving their spacing as one depth composition. Modal-front positioning, drag deltas, switch choreography, enter/exit timing, shimmer, burn, and haptic ownership are unchanged. This remains web/source-only and is not installed.
- Journey Stage overlay reveal keeps the New Card visual-impact language without its smoke particles. The card artwork uses the same `1.7s` white shimmer cadence, alpha-masked with its own PNG so neither shimmer nor the warm burn-in pulse can paint outside the card silhouette. Card settle and modal settle still independently emit their lifecycle-tracked light screen impacts and medium/light haptics; the modal burn remains clipped by its paper radius. Reduced Motion disables shimmer, burn, and shake while preserving the static composition.
- Modal-drag follow-up makes both foreground surfaces explicit gesture starters: the card preview and the non-control Journey modal paper each bind the same Pointer Events drag owner directly, while stage-level move/release/cancel retains one shared flick/snapback/depth-swap lifecycle. The modal paper disables its otherwise inherited vertical overflow/overscroll gesture ownership and uses `touch-action: none`, preventing iOS from cancelling the card-style drag as a scroll. CTA/X remain excluded controls and keep their own interaction. This remains source/`dist` only and is not installed.
- The Journey overlay Play/Continue CTA no longer fills the modal paper width. Its registered `.cc-cta` root now uses the canonical centered Exit Game measure `249px × 64px` with the shared 28px internal visual padding; primary color, shadow, shimmer, press, enter, and exit remain owned by the shared CTA system. This remains source/`dist` only and is not installed.
- Card-art containment follow-up removes the rectangular `box-shadow` from `.journey-card-overlay-preview`. With the preview intentionally unclipped and the PNG using `object-fit: contain`, no container-sized glow/shadow now paints outside the actual Journey card artwork. Card size, 40px gutters, modal paper shadow, depth/drag motion, and native/phone state are unchanged.
- Latest width/crop follow-up supersedes the 48px-gutter values below: the Journey overlay card and modal now share `min(calc(100vw - 80px), 390px)`, giving 40px side gutters. The card keeps that outer size but no longer crops its artwork: the preview uses `overflow: visible` and its image uses `object-fit: contain`. This remains source/`dist` only; native `Web.bundle` and the phone were not changed.
- Journey overlay source/web iteration after the `A1D5E86D-06BF-44CF-A825-6AE4D60E0B4C` phone install is intentionally **not synced or installed**. Both the foreground card and modal now use the identical `min(calc(100vw - 96px), 390px)` width, giving 48px gutters on each side of the iPhone 13 viewport. The `Stage NN` title has a dedicated title section whose font family, 40px/900 weight, `#AD8675` color, line height, centered alignment, and top position reproduce the Exit Game title benchmark exactly. Paper bottom padding is 40px below Play/Continue, including the short-height media profile, and the stats text column flexes within the narrower paper instead of overflowing. The active card or non-control modal paper now owns one cleaned-up Pointer Events gesture lifecycle: horizontal or vertical drag follows the finger with bounded tilt while the rear surface follows at 20%; a release below the Homepage-aligned 100px / 0.35px-per-ms intent threshold performs a 320ms overshooting snapback, while distance or fast flick continues in the release direction through the existing 110ms split, 280ms depth settle, z-order exchange, and 240ms foreground boing. Pausing more than 80ms before release clears stale flick velocity, drag-generated clicks are suppressed, CTA/X remain independent controls, and all listeners/RAF/timers/styles clean up on every modal exit. `qa:fast` passes 6 suites / 39 tests plus TypeScript, changed-file lint, diff hygiene, and Journey visual contracts. A fresh production build passes with native sync explicitly skipped; `dist` contains this iteration, while the Stack to Six `Web.bundle` and phone remain on the prior installed build. Gesture feel and final visual proportions remain `NEEDS PHYSICAL TEST` when the user later requests installation.
- 2026-08-07 the current Journey overlay return-card iteration passed `qa:full`, a fresh production build, raw-asset copy, sync only to the authoritative Stack to Six `Web.bundle`, `qa:ios`, and a signed Debug Xcode build. The final app bundle ID is exactly `com.taptapdesign.stacktosix.Stack-to-Six`; `dist`, native `Web.bundle`, and final-app `index.html` have the identical SHA-256 `e2c08e3934fdd1638a21504b4997499815939d8808a16af2831900311db9b6cc`, and the Journey overlay/return markers plus `assets/tile.png` are present in the final `.app`. `iPhone 13 blue` resolved as paired/available at CoreDevice ID `0F62B71E-0B04-53C3-906E-EC28F5D2390B`. Two initial Wi-Fi install-over attempts failed with `CoreDeviceError 3002 / IXRemoteErrorDomain 6 / Connection interrupted`; after restarting only the host-side `CoreDeviceService`, one attempt was manually cancelled while still waiting, then the final uninterrupted Wi-Fi retry completed successfully. Apple reported bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `A1D5E86D-06BF-44CF-A825-6AE4D60E0B4C/Stack to Six.app`; the exact bundle launched successfully at 03:19:58. Existing app data was preserved and no fallback device or legacy shell was touched. Independent animation review found that the return-card FLIP can currently start before the asynchronously scheduled Journey World/Unit enter has actually settled, and that the one-shot marker is consumed before the flight successfully completes. Those lifecycle races are plausible causes of the remaining landing jerk. Verdict: installed successfully; animation behavior remains `NEEDS PHYSICAL TEST`.
- Journey boards launched from the new overlay no longer set the legacy `fromDetailModal` return origin. They set a dedicated one-shot `__ccJourneyCardOverlayReturnBoardId`, so end-run routing and app-zone routing return directly to the containing Forest/Beach/Area 55 World and its canonical coordinated Unit enter instead of opening the old Journey detail/modal-card slider screen. The selected card DOM inside its floating-island Unit is marked as a hidden placeholder before the visible World return render/enter, so the cjelina genuinely has no duplicate card. After `showCollectibles()` completes, the same artwork runs as a centered active card over a fully transparent, noninteractive stage: the strong 580ms card enter, a precise 500ms hold, then a 700ms frame-tracked FLIP flight into the live placeholder. Every flight frame re-reads the destination center and current wrapper scale and interpolates translation/depth/scale/base rotation with zero-velocity smootherstep easing. Landing no longer hard-cuts between unlike surfaces: preview and real Unit card share a 160ms smoothstep crossfade while live position tracking continues, both use the exact 12px destination radius and `contain` image fit, and cleanup occurs one aligned frame after the real card reaches full opacity. Inline target opacity/transition/will-change state is restored exactly on completion or interruption. The transition cancels safely on navigation/pagehide/replacement and never affects legacy detail-origin or interim routes. No build, native sync, or phone install has been performed.
- A source-only Journey regular-card modal experiment now replaces the legacy full-page detail presentation behind `JOURNEY_CARD_OVERLAY_MODAL_EXPERIMENT_ENABLED`. Tapping an unlocked regular card keeps the active Forest/Beach/Area 55 World mounted and opens a minimal `Stage NN` modal with only High score, Longest combo, and Play/Continue; the card preview and modal can repeatedly exchange foreground depth, while the canonical Journey Unit/World/nav exit and existing saved/fresh gameplay handoff begin only after the CTA. The latest local visual iteration uses the exact Exit Game title typography (`Baloo2 40/900`, `#AD8675`, line-height `1`), the Score modal's stat typography/layout (`80px` icons, `32/700` values, `20/500` labels and divider), and the Exit Game CTA measure (`250×64`). Card and modal share the exact same responsive width (`100vw - 48px`, capped at `390px`), and each active surface independently centers from its own height at the exact viewport midpoint (`top:50%` plus `-50%` self-translation). Enter now targets the final depth composition from its first frame: the card enters directly into its raised rear `-6deg / .96` peekaboo pose rather than first becoming straight and centered, using a slightly slower 580ms tilt/bounce; the modal plus CTA begin their centered 580ms foreground enter at 55% of that card motion (319ms). On depth swap the active card is straight (`rotate(0)`) and centered with the same 24px side gutters as the modal, while only the inactive surface moves to the rear pose; specifically, the rear modal sits an additional 24px above the rear-card peek position. The background modal's exposed edge accepts pointer/touch/click/keyboard return input while its nested CTA/X are inert, so tapping it restores the modal foreground without a double toggle. Lifecycle cleanup covers replacement, route/page teardown, scroll/inert/focus restoration, and pending exit. Focused modal tests pass 8/8; full fast QA is rerun for every completed timing iteration. Per the user's instruction, this corrected iteration has not been built, synced, or installed on the phone; `dist` and Stack to Six `Web.bundle` still represent the earlier first experiment iteration, and physical appearance remains `NEEDS PHYSICAL TEST` only when the user explicitly requests another device install.
- Latest overlay interaction refinement: when the card is foreground, tapping the true backdrop outside both surfaces now dismisses through the same `beginClose('dismiss')` lifecycle as the modal close button; tapping the exposed rear modal remains reserved for bringing it forward. The rear modal is now raised another 24px, for a total 48px additional vertical offset above the base rear peek position. The card's dedicated enter keyframe has exactly 40% more 3D depth and X/Y tilt than the shared modal enter (`-252px`, `23.8deg`, `-123.2deg`) without changing the modal, timing, bounce, final rear pose, or exit; Reduced Motion removes both custom card animation owners. Repeated card/modal depth swaps now use a physical two-phase exchange: a fast 110ms lateral split moves the two surfaces in opposite directions with extra opposing `5deg/-11deg` rotation, then z-order changes and both settle into their new front/rear positions over 280ms with a restrained bounce; interaction is guarded for the 390ms transaction and all timers share overlay cleanup. A settle-phase hit-test race is closed: the visible rear-modal edge retains its pointer surface once the card takes foreground z-order, and backdrop dismissal is explicitly suppressed for the entire swap, so a tap can no longer fall through and close the overlay. This remains source-only and has not been built, synced, or installed.
- Modal-front interaction now treats every non-control point on the paper/modal surface as a depth-swap gesture: only real controls (`button`, link, input/select/textarea), including X and CTA, retain their own action. The physical switch choreography is horizontally mirrored from its preceding iteration: the card now passes in the opposite lateral direction with opposite rotation (`+5deg` when coming forward, `-11deg` when yielding), and the modal separates counter to it before the same 110ms split / 280ms settle. Whichever surface becomes foreground receives a dedicated gentle 240ms scale boing (`.975 → 1.025 → 1`) on an inner motion owner, without disturbing the positioning/depth transform. Backdrop dismissal and the settle hit-test guard remain unchanged.
- Play/Continue now prestarts the existing canonical Forest/World exit at the exact overlay phase boundary where the second rear surface begins its exit (40% / 380ms into the first surface), rather than waiting for both 950ms paper exits to finish. The modal controller emits this handoff only for the `play` result; the manager retains and awaits the single returned Journey exit promise after overlay cleanup, so World/Unit/nav animation cannot start twice. X/backdrop dismissals never trigger the early Journey exit.
- The experiment exit has no root opacity fade and is deliberately 300ms slower than the shared End Game benchmark: each foreground/background surface uses the same flip/bounce shape over 950ms. The background surface begins when the foreground reaches exactly 40% (380ms); in that exact handoff frame the overlay backdrop begins its own 950ms background-color fade to transparent while both paper surfaces retain full independent opacity. Foreground identity is dynamic, modal→card and card→modal orders both work, cleanup waits through the rear surface's full exit (1330ms total), and the current depth arrangement is preserved instead of snapping back.
- Tilted modal exits are now viewport-bounded across End Run/Exit Game/Exit Stage, Score/Combo, Collectible Reward, and the 3D Motion intro. The two nested exit owners no longer compound the old `1.055 × 1.018` scale with an overshooting easing; they retain a small anticipation at `1.012 × 1.008` (about `1.02×` total), reduced upward travel/depth/tilt, and a bounded `cubic-bezier(0.4, 0, 0.2, 1)`, while enter motion, 650ms lifecycle, final flip, CTA exit, cleanup, and Reduced Motion remain unchanged. Focused modal contracts pass 20/20; `qa:fast` and `qa:full` pass, with 110 suites / 648 tests plus build/audit gates. Exact raw assets were synced only to Stack to Six; `qa:ios`, signed Xcode build, final bundle ID/entrypoint/CSS marker checks, install-over, and launch on `iPhone 13 blue` passed. Apple reported installation URL ending `C0504B13-692D-4F90-BB64-7FEDE4240E73/Stack to Six.app`. The user physically confirmed the modal stays inside the phone viewport and the revised exit height/feel is correct: `PASS`.
- Online checkpoint `v916 stabilize Arcade navigation and HUD lifecycle` is commit `dae1c6c` on `origin/feature/v700-journey-hub`. It contains the complete accepted dirty tree after `v915`: Arcade Homepage navigation lifecycle ownership, the physically accepted HUD exit-flash fix, shared modal parallel-exit/idle polish, Mushroom density/speed tuning, and their regression contracts. `qa:full` passes 110 suites / 648 tests plus all static/build/audit gates; the exact build is physically accepted on `iPhone 13 blue` for Arcade navigation and HUD return behavior.
- Arcade HUD exit flash is fixed and physically accepted on `iPhone 13 blue`. The root race was `playHudRise()` leaving `HUD_ROOT._dropped` true during its tween, allowing a layout pass to force the HUD back to visible, while delayed gameplay-entry RAF callbacks could also reveal it after `exitingToMenu` began. HUD rise now revokes dropped ownership and marks exit synchronously before tweening; layout and every delayed HUD reveal path share a gameplay-zone/exiting policy guard. Two focused contract suites cover the policy and all guarded owners. `qa:fast` passes 10 suites / 67 tests; `qa:full` passes 110 suites / 648 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard. Exact raw assets were synced only to the authoritative Stack to Six shell; `qa:ios`, signed Xcode build, exact bundle/content/runtime-marker verification, install-over, and bundled `app://localhost/index.html` launch passed. Apple reported installation URL ending `E7B1E47A-3008-4C54-9D18-94CCEF24FB61/Stack to Six.app`. Multiple captured Arcade exit cycles completed gameplay → Homepage normally, and the user physically confirmed the HUD exits without reappearing or flashing: `PASS`.
- Arcade Homepage navigation now follows the same single-owner enter/exit contract as the accepted Journey path. Arcade launch records home origin without leaving the `home` zone until `animateSliderExit()` completes, so navigation performs its full accepted bounce-out before `board-arcade` hides it. Return no longer acquires Homepage in `requestExitToMenu`, destroys the app-lifetime navigation observer, runs competing `showHomepage()/forceReady()` subscribers, or waits up to 3.5s for warm first paint. The final physical follow-up removed the remaining return flash by separating route acquisition from navigation paint: `prepareHomeMenuEnter()` moves to the home zone while nav remains inactive, the enter owner applies deterministic `scale(0)`, and only then primes the tree for its existing bounce-in. Stale async `hideHomepage()` cleanup also aborts after ownership returns home. Focused lifecycle tests pass 24/24; `qa:fast` passes 52 changed-file tests; `qa:full` passes 108 suites / 633 tests plus TypeScript, lint, audits, visual contracts, 912-module no-sync production build, bundle audit, and native guard. The complete raw-asset output was synced only to Stack to Six; `qa:ios`, signed Xcode build, exact bundle ID, final-app source markers, install-over, and bundled launch passed. Apple returned `App installed` only for `iPhone 13 blue` at installation URL ending `31B60E77-716F-4BBA-A3EA-AB99590027F2/Stack to Six.app`. Multiple captured Arcade return cycles progressed `hidden-sync → before-animation` in roughly 70–75ms and finalized one enter owner in roughly 1.08s. The user physically confirmed the navigation icons are now “super”; both Homepage → Arcade exit and Arcade → Homepage enter are accepted `PASS` on this installed build.
- The complete current dirty-tree build is installed and launched on `iPhone 13 blue`. It passed `qa:full` (108 suites / 631 tests plus TypeScript, lint, source/release audit, visual contracts, no-native-sync production build, built-bundle audit, and native source guard), exact raw-asset Stack to Six sync, `qa:ios`, signed Xcode build, and final `.app` bundle/content checks. The first Wi-Fi install ended with the known `IXRemoteErrorDomain 6 / Connection interrupted`; the persistent retry completed with `App installed`, preserving existing data. Apple verified bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `BADC6402-FDBD-4040-A8B1-57EABC0A7154/Stack to Six.app`; launch then succeeded. No fallback device or legacy shell was touched. Git was not changed; physical modal/Mushroom feel remains `NEEDS PHYSICAL TEST`.
- Shared CTA gameplay modals now use one explicit parallel-exit contract: `runGameplayModalParallelExit()` starts the CTA pair/group and modal/backdrop surface in the same JavaScript turn, then holds cleanup behind both owners. This applies to 3D Motion, End Run, and Collectible Reward; it replaces the sequential CTA-then-surface gap while preserving companion CTA stagger, each surface's accepted exit motion, duplicate-choice guards, synchronous iOS permission gesture ownership, and cancellation cleanup. This is included in the installed build recorded above; final simultaneous visual feel remains `NEEDS PHYSICAL TEST`.
- Shared gameplay-modal idle now moves more visibly during both quiet float arcs before its periodic boing: the first lift is `-3px / z3 / scale 1.003` and the second `-3.75px / z4 / scale 1.004`, roughly 50% more vertical travel than the prior `-2px/-2.5px` values. The 6.8s cadence, boing poses, shadow owner, press pause, Reduced Motion handling, enter/exit animation, and every modal call site are unchanged. This is included in the installed build recorded above; final shared modal feel remains `NEEDS PHYSICAL TEST`.
- Mushroom finale birth is locally retuned without changing its Juice archetype, spores, text, exit, or shared cleanup. The pile drops exactly 30% from 30 to 21 pooled Sprites, reducing its peak Mushroom finale owner from 102 to 93 active display objects when combined with the unchanged 72 spores. The 21 births sample the full existing 30-slot silhouette from front row through inverted-V cap instead of truncating the upper rows. Appearance is 40% shorter throughout: stagger `42ms → 25ms`, and all four per-Mushroom rise/squash/stretch/settle durations use one `0.6×` speed profile. Reverse exit remains at the accepted 50ms cadence. This is included in the installed build recorded above; final density/speed remains `NEEDS PHYSICAL TEST`.
- Online checkpoint `v915 standardize gameplay modals and polish CTA motion` is commit `a0201d7` on `origin/feature/v700-journey-hub`. It consolidates the accepted layered 3D Motion presentation, centered End Run benchmark, shared gameplay paper close, modular Score/Combo and Collectible Reward modal migration, coordinated CTA timing/style corrections, Arcade `Rounds cleared` copy, and the final non-rotating floating-paper/occasional-boing idle. Package version is `2.0.632`. `qa:full` passes 108 suites / 631 tests plus TypeScript, full lint, source/release audit, visual contracts, no-native-sync production build (912 modules), built-bundle audit, and native source guard. The checkpoint does not imply a phone installation; final modal/idle feel remains `NEEDS PHYSICAL TEST`.
- The rejected continuous side-to-side idle tilt has been removed from all shared gameplay modals. Its dedicated nested owner now runs a 6.8s `floating paper + occasional boing` cycle: two quiet `-3…-3.75px` hover breaths, then one short `-4px / scale 1.015 → +1px / scale .995 → -1px / scale 1.006` cartoon settle before returning to rest. A separate rounded pseudo-shadow varies only composite opacity with the same cadence, adding depth without animating layout or the paper's painted shadow. There is no rotation, horizontal motion, or squeeze; paper, content, CTAs, and X remain one composition. CTA/close press pauses both owners, Reduced Motion disables transform and ambient shadow animation, and no JS timer/RAF/GSAP owner was added. Native bundle/app/phone and Git were not changed; final float/boing feel remains `NEEDS PHYSICAL TEST`.
- The stronger shared modal idle is now a pure side-to-side 3D flip instead of a diagonal squeeze: its two extrema retain `rotateY -10.5deg/+11.5deg` and Z depth, but `rotateX`, `rotateZ`, and all Y-axis translation are exactly zero. Only a restrained `-4px…+4px` horizontal X shift remains, so left/right paper edges travel through depth without vertical lean, roll, or squash-style composition. Lifecycle ownership, CTA/close pause, exit composition, and Reduced Motion are unchanged. Native bundle/app/phone and Git were not changed; final side-flip feel remains `NEEDS PHYSICAL TEST`.
- Shared gameplay-modal idle tilt is retuned from the initially near-invisible `rotateY -1.35deg…+1.5deg` to the requested 10–15% of the accepted 88-degree enter tilt: `-10.5deg…+11.5deg` Y rotation, `+2.1deg…-2.3deg` X counter-tilt, and bounded `18–22px` depth at the two extremes. The 7.8s asymmetric loop uses one continuous symmetric ease, retains the dedicated nested idle owner, CTA/close press pause, exit composition, and Reduced Motion behavior, and adds no runtime animation object. Native bundle/app/phone and Git were not changed; stronger idle feel remains `NEEDS PHYSICAL TEST`.
- Arcade High Score modal copy now labels its progression statistic `Rounds cleared` instead of `Rounds opened`; the underlying `highestStageOpened` value and all scoring/progression behavior are unchanged. Native bundle/app/phone and Git were not changed.
- Shared centered gameplay modals now gain one subtle post-enter idle tilt without competing with the accepted benchmark enter/exit. End Run/Exit Game/Exit Stage, Score/Combo, and Collectible Reward each wrap paper, content, CTAs, and the protruding X inside a dedicated `.cc-gameplay-modal-idle-shell`; the existing outer bounce and flip shells retain exclusive ownership of enter/exit. Only after the 650ms enter plus 34ms cleanup does the stage acquire `cc-gameplay-modal-idle`, starting an asymmetric 8.4s 3D drift bounded to roughly `rotateY -1.35deg…+1.5deg`, `rotateX ±0.4deg`, and 1px/4px depth movement. Because idle is nested, it can continue through outer exit with no identity snap; CTA press and close press pause it, and Reduced Motion disables it. The owner is recreated/removed with each modal DOM lifecycle and adds no JS timer, RAF, listener, GSAP tween, or permanent `will-change`. `qa:fast` passes 9 suites / 61 changed-file tests plus TypeScript, lint, diff hygiene, and visual contracts; the no-native-sync production build passes (912 modules). `dist` is current, while Stack to Six `Web.bundle`, native app, phone, and Git were not changed by this follow-up. Final idle feel remains `NEEDS PHYSICAL TEST` on web/device.
- The accepted centered Exit Game/Exit Stage presentation is now the shared benchmark for every active dismissible gameplay sheet family. New `gameplay-modal-benchmark.ts` owns the common `650ms` enter/exit, `34ms` cleanup buffer, CTA start at 20% (`130ms`), and `70ms` companion stagger. Score/Combo and Collectible Reward now use the same full-screen centered stage, separate bounce and 3D tilt owners, rounded paper texture, `#B99177`-family shadow, protruding shared paper close, backdrop fade, Reduced Motion fallback, and exact benchmark cleanup. Their legacy translateY entrance/drag owners were removed from the active paths; Score's dead drag/timeline code was deleted, and Reward's duplicate nested sheet plus competing 300ms slide lifecycle were replaced by one modal owner. Reward CTAs begin during the modal at 20% rather than after it finishes and still exit as a coordinated pair; early dismissal cancels scheduled animation callbacks, and overlay state is now actually reset after cleanup. The first-play tutorial instruction panel remains intentionally excluded because it is spatially anchored to its gameplay target, while legacy Resume Game remains excluded as removed product code. TypeScript, production build, and 3 focused contract suites (10/10 tests) pass. The production postbuild synced current web output to the authoritative Stack to Six `Web.bundle`; no Xcode build, signing, phone install, or online Git checkpoint was performed. Final Score/Combo and Collectible Reward visual feel remains `NEEDS PHYSICAL TEST` in the web/device renderer.
- End Game/Exit Stage no longer performs any secondary rotational settle at the end of enter. Its inner 3D owner follows one uninterrupted `cubic-bezier(0.16, 1, 0.3, 1)` ease-out from the accepted side tilt directly to the identity transform; the separate outer owner retains the existing cartoon bounce, so depth remains playful without a final door-spring or counter-rotation. Both owners retain matching explicit resting matrices, and enter-class cleanup remains one frame-safe `34ms` after the unchanged 650ms visual motion, preventing compositor teardown from producing a last-frame snap. End Run primary CTA starts at exactly 20% of the modal lifecycle (`130ms` at the current 650ms duration), derived from the duration rather than hardcoded; secondary follows through the existing `70ms` stagger at `200ms`. Both remain hidden/non-interactive until their tracked controller start, which is cancelled by normal close/route cleanup. 3D Motion, exit, backdrop, and Reduced Motion behavior are unchanged. Native bundle/app/phone and online Git were not changed.
- The Journey `Exit Stage` modal no longer exposes the obsolete `New Card` action. Its button markup, controller registration, haptic callback, delayed Journey lookup, and dynamic New Card screen handoff were removed together, leaving exactly `Restart` and `Exit Stage`; Arcade remains exactly `New Game` and `Exit Game`. Shared CTA group exit behavior, close control, modal motion, save/resume behavior, and the real card-reward flow elsewhere are unchanged. Native bundle/app/phone and online Git were not changed.
- Board gameplay sheets share one modular paper close control through `gameplay-sheet-close.ts`. End Run/Exit Game has restored its tilted 3D presentation: `.end-run-modal-bounce-shell` owns the comic scale accent, while its nested `.end-run-sheet-flip-shell` owns perspective/depth rotation and contains both `.end-run-paper-clip-shell` and the close badge. Paper and X therefore remain one physical 3D composition throughout enter and exit, without foreground/background separation. The paper shell exclusively owns texture, rounded clipping, scroll, padding, and the requested `#B99177` shadow; the flip shell remains overflow-visible so the 52px circular badge can protrude at `top:-10px/right:-10px`. Score/Combo continues to mount the same shared control on its sheet surface. The badge retains the smooth circular paper texture, existing 27px X asset, guarded one-shot activation, and board X's 380ms local tap bounce. It remains excluded from 3D Motion and other non-board modals. Native bundle/app/phone and online Git were not changed; exact tilt feel remains `NEEDS PHYSICAL TEST` on web.
- Both centered modal presentations now use their intended tilted 3D lifecycle again. End Game runs the restored nested flip enter/exit behind `END_RUN_BOTTOM_SHEET_3D_FLIP_TEST_ENABLED`, including perspective, depth translation, `rotateX`/`rotateY`, and its separate comic bounce accent over the existing 650ms surface window. The 3D Motion startup modal likewise has `SPATIAL_MOTION_MODAL_3D_FLIP_TEST_ENABLED` enabled and its `.journey-spatial-permission-flip-shell` restored. CTA motion, backdrop fade, paper clipping/overscan, close behavior, seven-day permission dismissal, and the removal of Journey's obsolete `New Card` action are unchanged. Reduced Motion disables both experimental 3D motions. Focused modal contracts pass 42/42 and `qa:fast` passes 8 suites / 56 changed-file tests plus TypeScript, lint, diff, and visual contracts. Native bundle/app/phone and online Git were not changed; final tilt feel remains `NEEDS PHYSICAL TEST` on web.
- Shared CTA shimmer is now primary-only. The orange visual retains the accepted two-pass `cc-cta-shimmer`, while every registered white/secondary CTA explicitly suppresses both its inner visual pseudo-element and any legacy root `::before/::after` residue with no content, display, animation, or opacity. Press/release, enter/exit, focus ring, 3D stroke/shadow, sizing, and CTA group ordering are unchanged. Native bundle/app/phone and online Git were not changed.
- Both local/web-only centered modal experiments reuse the exact End Game bottom-sheet tint `rgba(233, 210, 200, 0.24)` as a full-viewport background beneath the rounded paper. The tint is painted by an independent pseudo-layer and now has its own explicit lifecycle class: modal paper/content become fully opaque in their first animated frame, then one actual RAF later the backdrop eases from opacity 0 to 1 over `0.5s ease-in-out`. This prevents Chrome/WebKit from coalescing insertion and backdrop activation into an apparently instant brown frame. Exit removes the backdrop class before cleanup and retains the `0.2s` fade, without blur, rectangular sub-container, or opacity coupling to paper/CTA transforms. Focused modal contracts pass 26/26. Native bundle/app/phone and online Git were not changed; final tint strength/cadence remains `NEEDS PHYSICAL TEST` on web.
- Local/web-only End Game remains a true centered modal behind `END_RUN_CENTERED_MODAL_TEST_ENABLED`. The full-viewport outer owner performs centering, visibility, pause/unfreeze, CTA ordering, X toggle, outside dismissal, and cleanup; its bounce shell owns only the comic scale accent, its nested flip shell owns only 3D motion, and its paper shell owns only paint/clipping. Bottom-sheet RAF rise and drag/swipe registration are bypassed while the centered flag is enabled, the obsolete handle is hidden, and dialog/aria-modal/title semantics remain present. One shared duration selector updates restart, exit, and final cleanup to the centered modal's 650ms close, preventing routing from cutting off the animation; disabling the centered flag restores the intact 400ms bottom-sheet fallback. Paper is painted once, so there is no static duplicate underneath. No native Web.bundle/app/phone or online checkpoint was changed; final centered tilt, outside dismissal, and CTA handoff remain `NEEDS PHYSICAL TEST` on web.
- Local/web-only reversible 3D Motion modal experiment adds a nested `.journey-spatial-permission-flip-shell` behind one explicit `SPATIAL_MOTION_MODAL_3D_FLIP_TEST_ENABLED` flag. The accepted outer card remains the sole owner of modal centering, comic bounce, opacity, CTA lifecycle, focus, permission gesture, cooldown, launch blocking, and 650ms exit completion; only the nested paper layer receives `920px` perspective. Its revised flip-in is one uninterrupted depth/rotation/scale arc from the visible side profile at `rotateY(-88deg)/rotateX(17deg)/translateZ(-180px)` to one restrained `rotateY(7deg)` settle and the frontal resting pose; the staged hidden-backface/intermediate-pose movement was removed because it felt like motion started before rotation. Entrance opacity is now paint-only: overlay/card/content stay at zero before the visible start, then become fully opaque in the first animated frame; no opacity interpolation runs through the motion timeline. The centered End Game experiment follows the same immediate-opacity entrance rule, while both exit fades remain intact. Title, tilt-art wrapper, subtitle, and CTA-group wrapper enter from independently staggered `150–280px` Z depths and settle within the same 0.88s flip; child tilt/star/CTA transforms remain independently owned, and CTA exit is intentionally not wrapped so its accepted separate exit remains visible. Title/art/copy peel outward during paper exit. The old rectangular `.card::before` ambient plane is disabled under the experiment, removing the pale square behind the rounded modal; the shell's rounded shadow is now the only depth surface. The flip shell is also the rounded clipping owner while the inner paper carries 18px of invisible bottom overscan (`54px` padding with `-18px` crop), removing the doubled paper-texture seam below Later without moving CTA/content spacing. This avoids competing transforms and makes rollback a one-constant operation. Reduced-motion removes all experimental flip/layer motion, and the test adds no timer, listener, GSAP owner, or persistent `will-change`. Focused modal/launch contracts pass 23/23; `qa:fast` passes 2 suites / 14 changed-file tests plus TypeScript/lint/diff/visual contracts; the no-native-sync production build passes (910 modules). `localhost:5174` serves the enabled test. Native Web.bundle/app/phone and online `v914` were not changed; final layered 3D feel and bottom crop remain `NEEDS PHYSICAL TEST` in the web renderer before acceptance or removal.
- Online checkpoint `v914 modularize Homepage navigation and polish Mushroom finale` is commit `bebdbdc` on `origin/feature/v700-journey-hub` with web package `2.0.631`. Before checkpointing, `qa:full` passed 105 suites / 618 tests plus TypeScript, full lint, release/source audit, visual contracts, no-native-sync production build, built-bundle audit, and native source guard. The exact checkpoint was rebuilt with the complete raw asset tree, synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed. The signed Debug app was verified as `com.taptapdesign.stacktosix.Stack-to-Six`, bundled mode, valid signature, matching `dist`/Web.bundle/final-app entrypoints, and 797 raw assets including `mushroom@2x.png` plus `mushroom1…mushroom5`. Wi-Fi install-over completed on the paired `iPhone 13 blue`; Apple returned `App installed` at installation URL ending `BD35742C-B0C9-4305-9198-93C46F0509D1/Stack to Six.app`, and that exact bundle ID launched successfully. Existing app data was preserved; no other device, Capacitor shell, or legacy Kockice Crash project was touched. Final navigation cold-start and Mushroom animation feel remain `NEEDS PHYSICAL TEST` on this installed build.
- Mushroom finale growth rotation now includes the original Mushroom die illustration via its crisp `mushroom@2x.png` source alongside `mushroom1…mushroom5`. The bounded composition remains exactly 30 pooled Sprites, so six textures cycle evenly five times each with no extra display objects, ticker, timer family, or cleanup owner. Sequential pile birth cadence is reduced exactly 40% from `70ms` to `42ms`, making the complete packing phase finish in about `1.22s` instead of `2.03s` while retaining each mushroom's accepted boing. The third row is lifted by 1% and the cap row by 2% of viewport height, revealing a small amount of stem without changing horizontal density, Sprite size, reverse exit, spores, or gameplay/input lifecycle. Focused Mushroom/registry/Beach Ball tests pass 19/19; `qa:fast` passes 19 suites / 208 tests plus TypeScript/lint/diff/visual contracts; the no-native-sync production build passes (910 modules). Native Web.bundle/app/phone were not changed; exact density, stem reveal, and faster cadence remain `NEEDS PHYSICAL TEST` in the browser/device renderer.
- Homepage bottom navigation cold/warm visibility is now controlled by one route-aware, generation-owned lifecycle rather than five competing DOM writers. `navigation-control.ts` is the sole authority for `inactive -> primed -> entering -> interactive`; every activation phase rejects non-Homepage zones, every release invalidates old callbacks, stale enter/commit generations cannot affect a newer route, and duplicate prime/enter calls cannot regress an already-interactive Homepage. UIManager, SliderManager, AppZoneManager, collectibles/Journey handoffs, and Homepage animation cleanup no longer independently reveal or hide the nav tree. Active/inactive owner CSS makes both visibility and input paint-proof against delayed inline/animation writes, while cold startup acquires the owner before icon decode/readiness and warm shell return now performs one prime and one final commit without phase churn. Behavioral coverage explicitly exercises cold start, stale release/reacquire, duplicate callbacks, DOM mutation reconciliation, and activation rejection in `journey`, `board-arcade`, `board-journey`, and `fail-screen`. Focused navigation tests pass 18/18; `qa:fast` passes 19 suites / 208 tests plus TypeScript/lint/diff/visual contracts; the no-native-sync production build passes (910 modules) and did not sync or modify the native bundle/app. Fresh uncached web cold launch remains `NEEDS PHYSICAL TEST` before closing the visible issue.
- Homepage navigation cold start now follows the same ownership/readiness order as warm Journey returns. The supplied trace proved `#independent-nav` itself received the expected enter classes and transition, but cold boot kept the complete tree under `data-homepage-owner="inactive"` (`display:none!important`) throughout `forceReady()` and first-paint preparation, acquiring Homepage ownership only in the same task that started the scale transition. The startup handoff now acquires the `home` zone while `#home` is still safely transparent, before nav priming, `forceReady()`, image decode, and the two-frame paint barrier. Homepage readiness now explicitly decodes all `.independent-nav-button img` assets as critical first-paint images. Warm return behavior and the inactive-owner gameplay/Journey isolation remain unchanged. A focused contract locks the required owner → forceReady → readiness → enter ordering and nav-image selector. `qa:fast` passes 17 suites / 196 tests plus TypeScript/lint/diff/visual contracts, and the production build passes. The standard postbuild synced the generated `dist` output into Stack to Six `Web.bundle`; no Xcode build, signing, app install, launch, or legacy shell operation was performed. Fresh uncached web cold launch remains `NEEDS PHYSICAL TEST`.
- Mushroom finale now owns the foreground above the lower gameplay UI for its exact bounded lifecycle. When the Mushroom drop profile starts, one scoped `cc-mushroom-finale-foreground` class lifts the Pixi canvas above Journey's bottom decor and keeps the DOM Round indicator below the `#app` stacking context, so the growing pile and its spores visibly pass in front of the lower HUD. The top DOM HUD remains above the canvas, other Juice-family/Beach Ball effects do not receive the class, and shared cleanup removes the class on normal completion, forced stop, retry cleanup, route cleanup, and error fallback. No Sprite, ticker, timeline, texture, or pool count changed. Native bundle/app/phone were not changed; final overlap appearance remains `NEEDS PHYSICAL TEST` after web verification.
- Homepage bottom navigation is now lifecycle-owned exclusively by the Homepage surface. The supplied Journey → Homepage → Arcade reproduction exposed that `UIManager.hideNavigation()` queried a nonexistent semantic `<nav>` even though the real tree is `<div id="independent-nav">`; hiding therefore depended on MutationObserver timing and allowed the three icon buttons to remain over the Pixi board. `navigation-control.ts` now provides one synchronous ownership switch for the complete tree, `app-zone-manager` releases it before any gameplay/Journey cleanup and acquires it only for Homepage, and `UIManager.showNavigation()` rejects calls outside the `home` zone. An inactive `data-homepage-owner` CSS invariant uses `!important` display/visibility/input isolation, so stale enter/finalize inline writes cannot paint even one gameplay frame; descendant Web Animations are cancelled on release. The first startup follow-up was insufficient because the legacy inline boot CSS still applied `visibility:hidden` to every `#independent-nav` after enter removed its temporary inline override, while the empty initial `#app` DOM node could also be misclassified as active gameplay. Boot CSS is now owner-scoped with `:not([data-homepage-owner='active'])`, and the DOM gameplay fallback is ignored while the authoritative app zone is `home`. This restores cold-start navigation without weakening Journey/gameplay isolation. Native Web.bundle/app/phone were not changed; fresh web cold launch plus Journey→Homepage→Arcade remain the acceptance paths.
- Mushroom spores now occupy five explicit depth bands with approximately 20% of the 72 pooled Graphics in each: foreground `z=140`, behind Mushroom row 1 at `z=88`, behind row 2 at `z=68`, behind row 3 at `z=49`, and behind the rear row at `z=30`. Rear bands add `105ms` progressive birth delay so they visibly begin after the foreground wave. Every spore still rises from below the viewport at `25–32%` screen height/sec with independent wild sway and continuous Honey-highlight shimmer, but its endpoint is now independently randomized across a full 20%-screen band (`y=50–70%`). The prior shared last-cap exit owner is removed: each spore detects its own arrival, flashes once near `24%` of a randomized `300–520ms` terminal window, then pulses/fades to zero and becomes invisible individually. Only after all individual arrivals finish are the shared rise tween and pooled Graphics released outside GSAP render. This reduces the effect from two owners to one and removes synchronized top disappearance, gravity, and all stale exit state. No ticker, listener, texture, or unbounded emitter was added. Native Web.bundle/app/phone were not changed; final depth illusion/arrival rhythm remains `NEEDS PHYSICAL TEST` on fresh web replay.
- Mushroom pollen is now a dense bounded spore field rather than 36 similarly large grains. It uses 72 pooled Graphics particles: 72% are fine `0.85–2.2px` spores and the remainder are `2.25–4px`, rendered as narrow grains, dots, vertical spores, or tiny two-speck clusters. Their birth zone spans `1–99%` screen width and `66–97%` height so they cover the complete lower Mushroom pile instead of drifting sparsely across unrelated upper board space. Each retains the visible single-owner `55–85%` alpha and z-index 140, but adds independent vertical flutter to its existing three-segment lateral S-wind path; emission tightens to `22ms` intervals so many different sizes coexist on screen. The effect remains hard-bounded at 72 Graphics plus 30 Mushroom Sprites (102 active objects, below the existing 150-Graphics pool ceiling), owns no ticker/new texture, and defers pool release beyond GSAP render. Focused tests pass and `qa:fast` passes 16 suites / 189 tests plus TypeScript/lint/diff/visual contracts; the no-native-sync production web build passes. Native Web.bundle/app/phone were not changed; final spore density/feel remain `NEEDS PHYSICAL TEST` on fresh web replay.
- Mushroom pile timing is lightly accelerated without changing gameplay ownership: birth stagger moves `82→70ms` and reverse exit stagger `58→50ms` (roughly 15% faster), while individual boing shapes remain unchanged. The previously invisible floating pollen had two concrete composition bugs: its fill and display object both used the same translucent alpha, effectively squaring `38–72%` into a much fainter result, and its `zIndex=100` could sort behind the pile's front depth `101–102`. Pollen now draws opaque geometry with one `55–85%` display alpha, owns `zIndex=140`, and follows a three-segment S-shaped wind path with alternating lateral bends instead of one straight drift. Its pool release is also deferred beyond the GSAP render frame through the tracked lifecycle, matching the Mushroom Sprite crash fix. Count, size, palette, Graphics pool, no-ticker design, and cleanup remain bounded. Focused Mushroom/Beach Ball/registry tests, TypeScript, diff hygiene, `qa:fast`, and the no-native-sync production web build pass. Native Web.bundle/app/phone were not changed; final speed/pollen visibility remain `NEEDS PHYSICAL TEST` on fresh web replay.
- Mushroom density is retuned after the first enlarged visual replay still exposed board gaps. Inspection confirmed all five sources are transparent 1024×1024 canvases, so overlap based only on canvas bounds was insufficient. The same bounded 30-Sprite pool is now arranged as four extremely interlocked front-to-back rows (`8+8+7+7`) with centers only about 15–16% of screen width apart, while display size is tightened from `135–200px` to `160–200px`. The former tall apex (`y=0.685`) is removed; the shallow top contour settles at `y=0.895–0.945`, keeping the visible pile around the lower 30% rather than approaching mid-screen. Rotation is now independently randomized to `8–15°` in either direction per birth. Existing safe post-GSAP repool boundary, fixed Sprite count, five textures, reverse exit, pollen, and cleanup are unchanged. `qa:fast` passes 16 suites / 189 tests plus TypeScript/lint/diff/visual contracts, and the no-native-sync production web build passes. Native Web.bundle/app/phone were not changed; actual alpha-overlap density remains `NEEDS PHYSICAL TEST` on a fresh web replay.
- Mushroom's first enlarged-pile web replay exposed a high-severity GSAP/Pixi lifecycle crash: repeated `Cannot read properties of null (reading 'x')` came from a pooled Pixi target being reset/destroyed during GSAP's own render stack. The visual completion callback previously killed/repooled its Sprite from inside one track while a sibling `bubble.scale` track was completing, and the final owner then redundantly called `container.destroy({children:true})` after manually releasing pooled children; Pixi destruction nulls `_position/_scale`, leaving lazy GSAP property initialization to read `null.x`. Bubble release is now idempotently scheduled on the first safe task after the GSAP frame, Mushroom calls it only after both parallel exit tracks share a timeline end, and the container destroys only itself with `children:false` after every child has been explicitly detached/repooled. A focused contract locks all three lifecycle boundaries and forbids `children:true` from returning. `qa:fast` passes 16 suites / 189 tests plus TypeScript/lint/diff/visual contracts, and the no-native-sync production web build passes. Native Web.bundle/app/phone were not changed; a fresh-reload Mushroom web replay remains `NEEDS PHYSICAL TEST` before declaring the runtime crash closed.
- Mushroom growth pile now follows the supplied dense reference more closely: the same fixed pool of 30 Sprites is resized from `100–150px` to `135–200px` and authored into five tightly overlapping front-to-back layers. The first full-width layer owns the highest z-index; every later/higher birth sorts behind it, so the pile visibly grows upward without exposing board paper between stems. The two rear layers narrow toward a centered apex to form the requested inverted-V/pyramid silhouette, with cropped edge slots and center spacing substantially smaller than sprite width. Existing staggered boing birth, reverse boing exit, five shared textures, pollen, timers, pooling, and cleanup remain unchanged. Focused Mushroom/Beach Ball/registry tests pass, `qa:fast` passes 16 suites / 189 tests plus TypeScript/lint/diff/visual contracts, and the no-native-sync production web build passes. Native Web.bundle/app/phone were not changed; final density and silhouette remain `NEEDS PHYSICAL TEST` on web.
- Beach Ball's v800 finale physics were still present but unreachable after the later Juice-profile modularization: both merge entry points changed from the old explicit `variant.id === 'beach-ball'` down-route to `getSpecialDiceJuiceDropProfile(...)`, while the Beach Ball registry definition never received that profile. It therefore entered core Juice's upward branch instead of falling/bouncing. Beach Ball now explicitly owns `juiceDropProfile: 'beach-ball'`; both final-merge starters consequently pass `direction:'down'`, its six ball textures, and the dedicated profile into the unchanged v800 custom-down branch. The restored path is spawn above viewport → accelerated floor fall → `1.26×/0.70×` impact squash → high `power2.out` rebound with lateral travel → below-screen fall/cleanup. A focused two-test contract locks both routing call sites and the complete v800 motion markers separately from Mushroom. `qa:fast` passes 16 suites / 189 tests, type-check/diff/lint/visual contracts pass, the no-native-sync production build/raw-asset restore passes, and release bundle audit passes. Native Web.bundle/app/phone were not changed; final Beach Ball feel remains `NEEDS PHYSICAL TEST` on web first.
- Latest Mushroom crown follow-up supersedes the 25-slot/count statements below. The pile now contains 30 pooled Sprites so each of `mushroom1…5.png` appears exactly six times. Its upper silhouette is no longer four sparse full mushrooms: the highest layer is seven tightly overlapping caps across/beyond the full width, backed by a six-mushroom foreground canopy. Inspection of the actual 1024px art showed the stem occupies roughly the lower 40%; the canopy baselines are therefore raised to `y=0.755–0.765` in front of the crown at `y=0.705–0.715`, and higher z-depth lets canopy caps physically occlude crown stems without a Pixi mask, extra render pass, new ticker, or new texture. Focused contracts, type-check, `qa:fast` (15 suites / 187 tests), no-native-sync production build/raw-asset restore, and release bundle audit pass. Native Web.bundle/app/phone were not changed; final crown illusion remains `NEEDS PHYSICAL TEST` on web first.
- Mushroom finale no longer repeats `mushroom.png` as placeholder art. Its modular growth source now contains the supplied `mushroom1.png…mushroom5.png`; the 25 pile slots cycle deterministically through them, so every finale shows each illustration exactly five times while all Sprites share only five loaded textures. The 1024px sources are intentionally used at the existing 100–150px display range; supplied 2048px `@2x` files remain raw assets but are not decoded for this effect, avoiding roughly four-times-per-texture GPU allocation. All runtime/critical/comprehensive preload registries include the five 1x sources. Focused contracts, type-check, `qa:fast` (15 suites / 187 tests), no-native-sync production build, explicit raw-asset restore, and release bundle audit pass; all five PNGs are verified in `dist/assets/shop/mushroom`. Native Web.bundle/app/phone were not changed.
- Latest Mushroom density follow-up supersedes the 20-sprite composition in the next bullet: the pooled pile now contains 25 placeholders in five interlocked layers (`6 + 6 + 5 + 4 + 4`) confined to the lower ~30% of the viewport. Layer spacing is smaller than sprite diameter, the two lower layers extend beyond both side edges, and alternating offsets ensure another mushroom—not paper—is visible through the pile. It adds only five already-pooled Sprites and no texture, ticker, listener, or persistent owner; reverse boing-out cleanup remains unchanged. Focused contracts, `qa:fast` (15 suites / 187 tests), type-check, diff hygiene, visual contracts, and the no-native-sync production web build pass. Native bundle/app/phone were not changed; final density/feel remains `NEEDS PHYSICAL TEST` on web first.
- Mushroom's local/web-only Juice-archetype finale now uses the requested bottom-up “new world” growth composition and supersedes the rejected `part1…part6` ballistic burst documented below. Twenty pooled `mushroom.png` placeholders grow sequentially bottom-to-top through an authored irregular pile matching the supplied visual reference rather than a grid. Every instance is randomized between `100–150px`; edge mushrooms are intentionally cropped, foreground mushrooms are larger/layer above the smaller upper growth, and the first mushrooms remain visible until the complete world has formed. Exit runs in exact reverse birth order with a `58ms` cascade: each mushroom anticipates, contracts to `6%`, and drops below the viewport through a tracked back-in boing. Each birth owns a squash/stretch `back.out(2.5)` boing and alternating poses settle around `-8°/+8°`. The registry keeps an array-backed `mushroomGrowthSources` seam so the planned four art variants can be added without changing the animation owner. Thirty-six pooled irregular Flower-palette pollen grains are 50% enlarged, distributed across the lower field, and use translucent `38–72%` peaks with owned drift/fade timelines. Old Mushroom `part1…part6` runtime and preload references are removed, while raw source files remain untouched. The temporary Flower → Honey → Mushroom spawn sequence is now strictly scoped to Journey Forest Cjelina 02; an overly broad Forest `2–10` branch and its persisted Flower fallback previously leaked the test sequence into every later Forest level. Focused coverage checks spawn counts `0/1/2` across Forest, Beach, and Area 55 board numbers and forbids that leak. Juice gameplay/finality/input, `SHROOMY`, Mushroom idle, Flower, Honey, Beach Ball, and Arcade selection are otherwise unchanged. `qa:fast` passes 15 suites / 187 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts; `SKIP_NATIVE_BUNDLE_SYNC=true npm run build` passes. `dist` contains the change; Stack to Six native bundle/app/phone were not changed. Visual timing/composition remains `NEEDS PHYSICAL TEST` on web first.
- Honey/Magnet's long post-animation input freeze is now fixed and physically verified end-to-end. Native transaction tracing first proved the original symptom: the Magnet/Honey handler threw before board commit, rollback cleared `magnet-pull` but left the shared `special-transaction` token alive until its 15s watchdog, so every later drag was rejected. All Magnet abort/error/timeout exits now release only their own immutable token after rollback, preventing any exception from freezing the board or a stale callback from clearing a newer transaction. The root exception was then captured exactly as `Can't find variable: usedSpawnLockedTilesWithPop`: `app-merge.ts` retained a dead legacy branch flag even though that spawn path no longer existed. The stale condition is removed and the six reserved placeholders are synchronized unconditionally before board commit; a regression contract forbids that identifier from returning. `qa:full` passes 102 suites / 598 tests plus TypeScript, lint, source/bundle audits, visual contracts, production build, and native guard. The exact output was synced only to Stack to Six, signed, and installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed bundle `com.taptapdesign.stacktosix.Stack-to-Six` at installation URL ending `B91FCA10-C386-4F59-A9EB-26912A475259/Stack to Six.app`. Final physical Honey replay is `PASS`: token 3 claimed at `56887ms`, all `5/5` replacements and board-commit callback confirmed at `58330ms`, exact ownership released at `58331ms`, no merge error or later transaction-blocked drag occurred, and many subsequent drags completed. Representative post-commit drags processed moves around `0.7–1.4ms` with no move over `8ms`; most ticker maxima were `21–32ms`. The preceding Flower/TNT-heavy merge trace contained one isolated `76ms` FX spike, so correctness/unlock is PASS while further Flower finale frame-spike optimization remains separate performance work.
- Special-dice board mutation is now serialized by one reference-free `SpecialDiceTransactionOwner` shared by Star, Juice-family, Magnet-family, and TNT-family dice. Every external special merge claims an immutable monotonically increasing token before the first grid mutation; `canDrop`, drag rebind, and merge entry reject competing transactions, while Magnet's owner-internal pulled-tile merge is explicitly permitted without claiming a second token. Release is exact-token-only, so a delayed callback from an older special cannot clear a newer owner or its input gate. Normal special spawns release from their real async spawn `finally`; the legacy 2.5s spawn timeout cannot release ownership; run/route reset and the independent 15s reference-free watchdog provide abort recovery. Flower/TNT uses a dual barrier at visible Bush completion plus completed bonus replacement spawns. The first physical Honey replay then exposed a separate Magnet timing error: all `5/5` replacements had committed, but the owner remained held through app-merge's fixed `1200ms` endgame settle plus optional `600ms/500ms` retries. Magnet/Honey now signals `onMagnetBoardCommit` only after replacements, consumed-Honey-to-regular conversion, and placeholder synchronization; that callback immediately clears both `magnet-pull` and the exact special token, while later endgame verification continues without freezing input. Consumed Honey identity and tile-local resolution ownership are cleared atomically before the fresh regular value is committed. Focused owner/input/Honey/Magnet tests pass 29/29 and `qa:full` passes 102 suites / 597 tests plus TypeScript, lint, release audits, visual contracts, production build, bundle audit, and native source guard. The complete bundle passed `qa:ios`, signed build, final bundle-ID/content verification, and Wi-Fi install-over on `iPhone 13 blue`; Apple confirmed `com.taptapdesign.stacktosix.Stack-to-Six`, installation URL ending `2FDB5154-0BD9-4C35-B9A0-D2B4E255925C/Stack to Six.app`, and successful launch. Deterministic verdict is `PASS`; immediate post-Honey-board-commit interaction remains `NEEDS PHYSICAL TEST` in this exact build.
- Honey's physically reproduced repeat-activation/stuck-tile regression is fixed at both ownership boundaries. The first replay disproved the initial Honey-local fix as sufficient and exposed the upstream race: although Flower/TNT is declared `after-gameplay-resolve`, `tnt-animation.ts` released the all-input gate at the sprite-10 visual lead and again after BOOM-letter exit, exactly while Flower's bonus break/spawn began mutating the board. This allowed a Honey drop to enter an overlapping special transaction. Those visual-tail releases are removed; Flower/TNT now retains input ownership until its sole complete cleanup, so Honey cannot start while any bush/bonus mutation remains. Independently, every gameplay-resolving Honey tile carries a resolution-owner flag rejected by visual-tail rebind, drag-start, gameplay-candidate selection, `canDrop`, and `merge`; magnet conversion atomically clears `_ccWildSpecial`, variant/archetype metadata, and live wild flags before painting the fresh regular cube, invalidating late texture resurrection, and releases ownership only after `setValue` commits. Focused Flower/Honey/input tests pass 23/23; `qa:fast` passes 15 suites / 184 tests. The preceding lifecycle patch passed `qa:full` with 100 suites / 590 tests plus all static gates, and the follow-up passes production build, `qa:ios`, and signed Xcode build. The exact complete output was synced only to Stack to Six and installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed installation URL ending `DEEEA822-38AD-498F-9DDC-C70FCAD4570B/Stack to Six.app`, and that exact bundle launched with live console. The corrected Flower→Honey physical replay remains `NEEDS PHYSICAL TEST`.
- Fixed the physically observed frozen regular value-6 when the same merge filled the wild meter at zero moves. The merge callback previously deferred `No Moves` for the pending reward and then returned from the entire merge-6 transaction, skipping its normal replacement spawn and destination cleanup; the locked 6 survived until the 1.2s watchdog while the special reward could begin concurrently. The deferred terminal branch now continues the owning merge-6 cleanup/spawn, while true no-continuation positions still run the fail flow. Wild reward entry is additionally blocked whenever a regular merge-6 destination cleanup claim exists, then remains serialized behind the existing merge spawn guard, so Flower/Honey/Mushroom cannot race the still-owned merge cell. Cleanup ownership now exposes a non-retaining `hasClaim()` query; a modular terminal-handoff decision plus focused behavioral/source-order regressions cover continuation, fail, retry, and ownership ordering. `qa:fast` passes 22 suites / 224 tests and `qa:full` passes 100 suites / 589 tests plus TypeScript, release audit, lint, visual contracts, production build, bundle audit, and native source guard. The exact complete build was synced only into the authoritative Stack to Six `Web.bundle`, passed `qa:ios` before and after signed build, and was installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed installation URL ending `5F66B402-132D-4C49-BFCA-19503D01535B/Stack to Six.app`, and the exact bundle launched with live console. The physical replay captured regular merge-6 `5 + 1`; the board then accepted many subsequent drag/merge operations with no frozen input owner, terminal fail race, or JS error. This confirms the stranded-six lifecycle regression is fixed on-device. The Flower/TNT boundary still produces its known temporary GSAP/timeline workload rise, but it no longer aborts gameplay ownership.
- The complete current build was successfully installed over Wi-Fi on `iPhone 13 blue` after keeping the long-running transfer session alive beyond the initial usage assertion. Apple confirmed `App installed` at installation URL ending `9C4F6E33-9E29-4F8B-B116-322F421CFD09/Stack to Six.app`; the exact bundle `com.taptapdesign.stacktosix.Stack-to-Six` launched in bundled mode with live console. A 30-second Journey Forest Stage 02 trace proved ordinary gameplay is stable: representative one-second windows averaged `16.66–16.67ms`, most worst frames were `17–30ms`, ordinary merges averaged about `15.5–16.5ms`, and drag processing stayed `0.4–2.2ms` with zero moves over `8ms`. Hidden Journey remained only `11 children / 2 images`; no hidden-screen rebuild was present. GSAP work rose during merges but repeatedly returned downward (`592→121→44`, later `767→406→132→76`), so the trace does not show monotonic lifecycle buildup. One real hitch was captured at the special-FX boundary: a one-second window averaged `18.18ms` with a `78ms` worst frame, `2` frames over `34ms`, and `1` over `50ms`; concurrently `will-change` rose `1→10`, GSAP children `76→445`, active timelines `28→94`, and timeouts `5→15`, after which board protection correctly set `reducedFx:true`. The immediately following haptic sequence was heavy then repeated light impacts, which matches the TNT-archetype finale used by Stage 02's first Flower special; this makes Flower/TNT burst creation the strong evidence-based suspect, not Journey idle, touch handling, or ordinary merges. The console tunnel then invalidated, so absolute memory and a second confirming Flower trace remain unavailable. Verdict: `NEEDS PHYSICAL TEST`; next work should add/name Flower finale milestones around bush/flower emission and measure the same activation again before optimizing.
- Journey no longer has a visible dead pause before idle. On the Journey Worlds Hub, the standard enter remains on each outer World card while the independent nested World/cloud CSS idle owner starts on the first real visible enter tick, so motion is already alive throughout the Forest → Beach → Area 55 cascade without transform contention. On Forest/Beach/Area 55 screens, each Unit finalizes and starts its existing idle immediately when that Unit's own enter tween completes instead of waiting for every later Unit; the coordinator's seamless amplitude ramp is shortened from `0.8s` to `0.18s`. Exit, ordering, enter/exit values, Unit grouping, spatial motion, interaction cleanup, and Reduced Motion behavior are unchanged. The Journey animation contract now records this earliest transform-safe idle handoff. Focused Journey tests pass 30/30, `qa:fast` passes 19 suites / 217 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts, and the no-native-sync production web build passes. Current `dist` includes this Journey fix plus the latest Mushroom copy/birth-zone follow-ups; native bundle/app/phone were not changed in this task. Physical animation feel remains `NEEDS PHYSICAL TEST`.
- Mushroom finale has been redesigned from the rejected two-wave floor/wall physics into one Wild-Star-like centre burst. It now emits 24 pooled `part1…part6` sprites from a centre-owned horizontal birth zone spanning 50% of the viewport width, rather than one point, with a short 22ms cascade; every part owns one uninterrupted `ease:none` ballistic path, rises/spreads independently, rotates `0.75–1.75` turns, then falls under gravity and is released only below the viewport. Parts remain fully opaque for the visible lifecycle with no bounce, collision solver, wall reflection, fade-out, or direction-change pause. Each begins at exactly 50% of its randomized normal scale and continuously reaches 150% near its below-screen exit. Its splash copy is now exactly `SHROOMY` with no exclamation mark, split, or secondary color; every glyph uses `#FD7D5F`. Juice, Beach Ball, Flower, Honey, gameplay resolution, and Mushroom idle are unchanged. The focused Mushroom contract and TypeScript pass after the birth-zone follow-up; the broader `qa:fast` previously passed 19 suites / 216 tests plus visual contracts, and the no-native-sync production web build passed before the copy/birth-zone follow-ups. `dist` requires a fresh build to include those latest follow-ups. Stack to Six `Web.bundle`, Xcode app, and phone were not changed by this redesign. Animation feel remains `NEEDS PHYSICAL TEST`.
- First cold Homepage → Journey Worlds entry now starts the V700 Hub lifecycle from the already prepared `journeyBoardsManager` in the same first visible RAF as the viewport enter, instead of making both visible motion owners wait behind a second dynamic import. The import remains a guarded fallback only. This closes the physical symptom where Worlds rendered on first entry but the Forest progress banner and all Hub idle motion were absent until leaving and re-entering Journey. Native diagnostics distinguish `hub-enter-started-from-prepared-manager` from the fallback path, and a transition-ownership contract locks release → prepared-manager start → fallback-import order. `qa:fast` passes 19 suites / 216 tests; `qa:full` passes 98 suites / 585 tests plus TypeScript, release audit, lint, visual contracts, production build, bundle audit, and native source guard. Full raw assets were restored, the exact output was synced only to Stack to Six `Web.bundle`, `qa:ios` passed before and after the signed Xcode build, and the final app verifies bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` with a matching entrypoint. Wi-Fi installation has **not** been confirmed: four attempts acquired the tunnel/usage assertion but returned without `App installed`; a read-only device query proves the phone still owns the prior installation URL ending `4842F310-868D-41D6-A5E0-A692B3254938/Stack to Six.app`. The ready signed build is `/tmp/stack-to-six-derived/Build/Products/Debug-iphoneos/Stack to Six.app`; physical first-entry verification remains `NEEDS PHYSICAL TEST`.
- The same latest verified Stack to Six app was reinstalled on request over the existing app on `iPhone 13 blue`, with final bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, matching `dist`/final-app entrypoints, Mushroom runtime artwork, and Journey banner asset present. `devicectl` returned explicit install success at installation URL ending `4842F310-868D-41D6-A5E0-A692B3254938/Stack to Six.app`, and that exact bundle launched successfully. Player data was preserved; no rebuild/source change, other device, or legacy shell was involved.
- Mushroom `SHROOMY!` was absent despite correct registry copy because the Mushroom two-wave spawn branch returned before reaching the shared Juice bubbly-text trigger. The trigger now runs once before any profile-specific branch, so Mushroom, Beach Ball, and core Juice share one text lifecycle without duplicate overlays; a focused contract locks ordering before the Mushroom early return. The same installed build includes the Mushroom cold-preload/variant-restore fix, latest 36ms/360–720° motion tuning, and first-entry Journey banner preload fix (`natpis.png` plus `natpis@2x.png` are critical and present in every preload registry). `qa:full` passed immediately before the banner/Mushroom-text follow-up (98 suites / 584 tests plus TypeScript, release audit, lint, visual contracts, no-native-sync production build, bundle audit, and native source guard); after the final text-owner change, focused Mushroom tests pass (4/4), `qa:fast` passes (19 suites / 215 tests), the production build and `qa:ios` pass, and the final signed app has bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` with matching `dist`/final-app entrypoints. Wi-Fi install-over completed on `iPhone 13 blue` at installation URL ending `14E80807-F2B8-43C3-998D-4E208F42F0A6/Stack to Six.app`, and that exact bundle launched successfully. This supersedes the intermediate install ending `221BB950-AA1D-41B4-B51E-0992A1F4FA9A`, which completed while the text fix was being made. Existing data was preserved; no other device or legacy shell was touched. `SHROOMY!`, Mushroom cold texture, and first-entry Forest banner visibility remain `NEEDS PHYSICAL TEST`.
- Mushroom identity/texture cold-start regression is fixed at both ownership boundaries. Runtime registry requests `./assets/shop/mushroom/mushroom.png`, but critical preload previously included only `mushroom@2x.png`; the exact non-2x runtime key is now critical so iOS cannot initially retain the Juice fallback while Mushroom decoding is deferred. `applyWildSkinLocalCore` now immediately applies the cached texture and, for registered special visuals, awaits the authoritative `Assets.load()` decode, then reapplies only if the tile still exists and still requests the same variant path, preventing stale async replacement after reuse/removal. Separately, the alternate `gameState.tiles → grid` restore migration now preserves `_ccSpecialDiceVariant/specialDiceVariant`; it previously dropped Mushroom identity while retaining core `wild-juice`, which deterministically restored the Juice artwork. Focused Mushroom/registry contracts pass (14/14), and `qa:fast` passes (18 suites / 198 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This fix and the preceding 36ms/360–720° Mushroom motion tuning are included in the verified final install above; cold-start/save-restore behavior remains `NEEDS PHYSICAL TEST`.
- Post-install Mushroom source tuning makes every part visibly individual: per-part wave stagger doubles from `18ms` to `36ms`, spreading each 18-part wave across about `612ms`, and wave two begins at `840ms` so it cannot collapse visually into wave one. Floor rebound increases moderately from `25–48%` to `34–58%` viewport-height upward velocity. Rotation is now lifecycle-normalized rather than speed-normalized: every part completes a randomized `1–2` full turns (`360–720°`) between spawn and below-screen cleanup regardless of its individual gravity duration. Collision response, scale range, continuous linear-time physics, pooling, and cleanup are unchanged. The focused Mushroom contract passes (3/3), and `qa:fast` passes (18 suites / 197 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This newest tuning is source/web-only and is not included in the physical install below; feel remains `NEEDS PHYSICAL TEST`.
- The current Flower/Honey/Mushroom source build, including the latest two-wave Mushroom timing and physics, passed `qa:full` (TypeScript, release audit, full lint, full tests, visual contracts, no-native-sync production build, bundle audit, and native source guard). The complete raw asset tree was restored into `dist`, synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed with bundled mode (`useDevServer = false`), matching entrypoints, assets, project identity, and freshness. The signed Stack to Six Debug build succeeded with final bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`; the large Wi-Fi install-over was held through explicit `App installed` success on `iPhone 13 blue` at installation URL ending `1997C714-D2A0-4B2B-A2BE-1E876E308AC0/Stack to Six.app`, and that exact bundle ID launched successfully. Existing app data was preserved and no other device or legacy shell was touched. Physical Mushroom wave/collision feel remains `NEEDS PHYSICAL TEST`.
- Mushroom finale emits two readable waves of `18` pooled parts without same-frame clumping. Within each wave, every part starts `18ms` after the previous one, spreading each wave across roughly `306ms`; the second wave begins at `520ms`, leaving a short visible breath after the first wave's final spawn while keeping both inside one finale. Each sprite's initial gravity fall remains `0.72–0.98s`, and all 36 remain inside the active cap with no overflow wave. Scale remains `0.72–1.008` (exactly 40% maximum difference); restrained floor impulses, proximity collision/deflection, continuous gravity/rotation, side-wall reflection, below-screen no-alpha-fade cleanup, pooling, interruption ownership, and `SHROOMY!` colors remain unchanged. Core Juice and Beach Ball are unchanged. The focused Mushroom contract passes (3/3), and `qa:fast` passes (18 suites / 197 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This change is included in the verified physical install recorded above; wave/collision feel remains `NEEDS PHYSICAL TEST`.
- Flower bush frames now use the requested intentional cartoon profile rather than perfect source aspect ratio: their base scale increases by exactly 15% (`0.85 → 0.9775`) and a Flower-only `0.84` horizontal factor squeezes width by 16%. The shared TNT frame owner now accepts independent per-variant horizontal and vertical factors, and applies them consistently through enter overshoot, settle, idle bounce, exit bounce, and the optional final-frame path, preventing any mid-animation proportion snap. Flower keeps `explosionVerticalStretch: 1`; core TNT keeps horizontal `1` and its established `1.4` vertical cartoon stretch. Flower particles, depth layering, timing, text, and cleanup are unchanged. The focused Flower contract passes (2/2), and `qa:fast` passes (18 suites / 197 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This source/web change has not been synced or installed natively yet; final bush proportions remain `NEEDS PHYSICAL TEST`.
- New local/web-only `mushroom` special die is the third wild-meter reward in Forest Stage 02 (and the same Forest `2–10` sequence): Flower at spawn count `0`, Honey at `1`, Mushroom at `2`. Mushroom reuses the complete `wild-juice` gameplay/finality/input lifecycle while owning `mushroom(.@2x).png`, `SHROOM POP!` split after `SHROOM` in `#FD7D5F/#FFAB8C`, trail `#FFE1C8/#FFEDD9/#FF9A80/#FF7D61`, and shards `#E7B392/#FF7B60`. Its six `part1–part6@2x` fragments use a dedicated Juice down-drop profile: staggered fall from above, one restrained `5.5–11%` screen-height bounce, return to the floor, then fade/scale away in place; Beach Ball retains its larger multi-axis bounce/exit profile and core Juice still rises. Mushroom suppresses inherited Juice idle bubbles and instead owns a small interim-card-like squeeze/stretch jump plus three behind-art smoke puffs, all through tracked GSAP timelines with complete interruption cleanup and container destruction. Registry helpers now modularly derive Juice text colors and drop profile, preserving Beach Ball's accepted per-letter rainbow. All Mushroom assets are in the preloader lists and production `dist`; save/load continues through the established special variant ID. Focused tests pass (13/13), `qa:fast` passes (18 suites / 197 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts), and the no-native-sync production build passes with Mushroom assets present. No native bundle or phone was changed; idle/finale feel remains `NEEDS PHYSICAL TEST` on web first.
- The complete current Flower/Honey build passed `qa:full` (97 suites / 579 tests plus TypeScript, lint, production build, release/bundle audits, visual contracts, and native source guard), exact raw-asset Stack to Six sync, `qa:ios`, signed Xcode build, final entrypoint/bundle-ID/content checks, Wi-Fi install-over, and launch on `iPhone 13 blue`. The verified app is `com.taptapdesign.stacktosix.Stack-to-Six`, contains 773 raw assets including `bee7.png` and `bee7@2x.png`, and Apple confirmed installation URL ending `EFEDA28A-87B1-47EE-B4DE-70D15270CF04/Stack to Six.app`. The first Wi-Fi transfer ended with `IXRemoteErrorDomain 6 / Connection interrupted`; the persistent retry completed with `App installed`, preserving existing app data. No fallback device or legacy shell was touched. Universal artwork-alpha shimmer clipping, revised pollen, balanced seven-bee paths/pair exits, and 60% bee spacing are now installed and launched; physical animation/clipping feel remains `NEEDS PHYSICAL TEST`.
- The artwork-alpha shimmer mask is now universal for every Wild and special die rather than Flower/Honey-only. Whenever a live `tile.base.texture` exists, the shimmer mask clones that artwork's alpha silhouette, so Star, Juice, TNT, Magnet, Flower, Honey, and future registry variants illuminate only their visible PNG pixels and never the surrounding cube wall/container. A rectangular mask remains solely as a safe missing/empty-texture fallback. Existing shared-texture-safe mask disposal and shimmer timing are unchanged. This change is included in the installed build recorded above; visual clipping remains `NEEDS PHYSICAL TEST`.
- Honey now includes the supplied `bee7(.@2x).png` across the runtime registry and every web/native preload list. Bee7 is parsed as its own sprite identity and owns a southwest/down-left destination. Honey source selection now uses a shuffled balanced cycle of all seven bee identities instead of independent random picks, guaranteeing every 16-bee finale contains upper, lower, left, and right exits while retaining random order. Every bee computes the first physical viewport edge along its authored direction, advances monotonically through `20%/38%/56%/72%/86%` of that individual edge vector, and completes bounce-out beyond that same edge; it no longer shares a generic diagonal distance that can bias the visible exit. `qa:fast` passes (17 suites / 194 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This source/web change has not been synced or installed natively yet; animation feel remains `NEEDS PHYSICAL TEST`.
- Flower idle pollen no longer uses three separately eased travel/fade segments that visibly paused before exit. Every grain now has one `ease: none` progress owner for its complete `1.55–2.20s` lifecycle; continuous formulas combine independent horizontal drift, a randomized wind wave, lift arc, and vertical travel ranging from upward motion to a slight fall, while opacity fades smoothly at the lifecycle edges. The former identical circles are replaced by a randomized mix of narrow ellipses, irregular five-to-seven-point grains, and small two-speck clusters, with random rotation, while the accepted count, size scale, palette, `60–95%` peak opacity, `760ms` emission cadence, Graphics pooling, tracked timeline ownership, and interruption cleanup remain intact. `qa:fast` passes (17 suites / 194 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This source/web change has not been synced or installed natively yet; animation feel remains `NEEDS PHYSICAL TEST`.
- Honey's 16-bee finale now launches/exits as eight uneven pairs instead of one near-simultaneous wave. Pair gaps consume the full stagger window with the requested `20%/20%/10%/5%/10%/20%` rhythm plus the remaining `15%` final interval, and paired bees retain only an `18ms` natural offset. The Honey-only separation solver increases clear edge spacing from `20%` to `60%` (an additional 40 percentage points), while the overlay cleanup tail increases from `0.6s` to `0.9s` so the last pair always completes its own edge bounce-out before pooled release. `qa:fast` passes (17 suites / 194 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This source/web change has not been synced or installed natively yet; animation feel remains `NEEDS PHYSICAL TEST`.
- Flower pollen density/size is rebalanced after visual review: each `760ms` emission now creates exactly twice as many grains (`4` or `6` instead of `2` or `3`), while each grain is exactly half the previous size (`1.625×` original radius instead of `3.25×`, approximately `2.65–5.61px` after its existing start-scale range). Colors, opacity, cadence, drift, lifetime, Graphics pool ownership, and cleanup are unchanged. This source/web change has not been synced or installed natively yet.
- Flower depth is redistributed through three explicit nine-particle profiles rather than four repeating slots. Since bush1…bush9 own z-indices `0…8`, flowers now originate across `0.5…7.5` (behind bush2…bush9), weighted toward bush5…bush9 for realistic canopy density; the final wave deliberately begins at `2.5`, behind bush4, before continuing through later depths. Every particle still promotes to `8.5` at 28% flight. This source/web change has not been synced or installed natively yet.
- Repeated Honey centrifugal-circle diagnosis found the actual cause in spawn geometry, not waypoint randomness: every bee previously started around `theta + π`, deliberately opposite its destination, forcing the whole swarm through one common center where collision separation fanned it outward like a spinning wheel. Honey now spawns each bee already in its own destination-facing half-plane using independent `18–106px` forward and `±95px` lateral offsets; it then follows its independent random waypoints without crossing a shared central choke point. The mandatory 20% visible-edge solver remains active at eight passes, with existing cleanup unchanged. `flowr1…flowr6` emission is extended another `0.300s` through waves `0.10s/0.905s/1.71s`. These source/web changes have not been synced or installed natively yet.
- Honey bee flight removes the recognizable circular mid-path pattern. The former shared alternating `curveA/curveB` construction is replaced by five independently randomized lateral waypoints and five independent subtle rotations per bee in its own outward/perpendicular basis, so paths can cross naturally without every sprite tracing the same authored circle. Mandatory visible-edge spacing remains `20%`; its bounded no-layout-read relaxation is strengthened from four to eight passes per Honey ticker frame to keep independently crossing bees separated. Spawn order, authored destination sectors, calmer buzz, growth, edge bounce-out, ticker removal, translate reset, pooling, and cleanup are unchanged. This source/web change has not been synced or installed natively yet.
- Flower `flowr1…flowr6` depth flight no longer chains independently eased movement/pop/exit segments. Each particle now has one uninterrupted linear progress owner for its full `1.12 × speedScale` lifecycle: constant outward travel is combined with a smooth broad wind bend and randomized `1.15–2.0`-turn sinusoidal swirl, while rotation, alpha, scale, and the existing bush-depth foreground promotion are derived continuously from the same progress. This removes direction-change stalls and repeated boing beats while preserving current wave timing, particle size, depth layering, total lifetime, Sprite pooling, tracked timeline ownership, and idempotent cleanup. This source/web change has not been synced or installed natively yet.
- Flower's idle pollen grains are exactly 30% larger through a Flower-only `1.3×` radius scale (`2–3.45px` source radius becomes `2.6–4.485px`). Count, `760ms` cadence, yellow palette, `60–95%` opacity, wandering path, lifetime, Graphics pooling, and stop/release cleanup are unchanged. This source/web change has not been synced or installed natively yet.
- No-moves/wild-meter race is closed for the reported `[3,2,5]` → `3+2` case. Decimal accumulation now uses one shared `isWildMeterReady()` threshold with a narrow `1e-6` floating-point tolerance across progress normalization, spawn permission, retries, continuation detection, and stack-fill crossing; it does not provide a broad near-full gameplay grace. More importantly, the central `runNoMovesFailFlow` owner now performs a final wild-continuation preflight before setting terminal locks, covering a charge/drop that becomes ready while an earlier caller awaits tutorial or FX work. Focused coverage proves `[3,2,5]` is classified non-final, effectively-full decimal meters spawn/defer correctly, and `0.999` remains not ready. This source/web fix has not been synced or installed natively yet.
- Flower's latest emission extension is finalized at `+0.250s`, not `+0.500s`: depth waves now begin at `0.10s/0.755s/1.41s`. Honey bee flight is also slightly calmer through a Honey-only positional jitter reduction (`1.30 → 1.05`) and narrower alternating rotation (`7–10deg → 6–8.5deg`); authored paths, size growth, 20% collision separation, sequential launch, and bounce exit remain unchanged. These source/web changes have not been synced or installed natively yet.
- Honey bee flights now have a Honey-only per-frame separation owner layered additively over their independent authored routes. Four bounded pair-relaxation passes maintain a minimum center distance equal to the two current visual radii plus a 20% gap, so bees no longer travel together or overlap while retaining randomized crossing directions. It performs no layout reads, is registered only for `beeFlight`, and removes its GSAP ticker callback plus all additive translates during pooled-sprite cleanup. This source change has not been synced or installed natively yet.
- The complete current Flower-depth/Honey build passed `qa:full`, exact raw-asset Stack to Six sync, `qa:ios`, signed Xcode build, final bundle-ID/entrypoint/runtime-marker checks, Wi-Fi install-over, and launch on `iPhone 13 blue`. The first transfer reached Apple's install service but ended with `IXRemoteErrorDomain 6 / Connection interrupted`; the immediate persistent retry completed with `App installed`. The installed app is `com.taptapdesign.stacktosix.Stack-to-Six`, contains 771 raw assets and the current `1.428×` Flower depth particles, `BLOOMING!`, `BUZZING!`, and Honey bubble profile, and Apple reported installation URL ending `99D31A10-9D61-4A63-92ED-78BF143ED04A/Stack to Six.app`. Existing data was preserved and no legacy shell or fallback device was touched. Final animation feel remains `NEEDS PHYSICAL TEST` on this installed build.
- Flower depth-particle size is reduced by exactly 15% from `1.68×` to `1.428×`; bush scale, depth slots, wave timing, paths, foreground promotion, and cleanup remain unchanged.
- Flower depth emission keeps its immediate `0.10s` start but extends the overall release window by exactly another `0.5s`: depth waves now start at `0.10s/0.63s/1.16s`. Honey idle bubbles move from the pale `#FEE4B8/#FAC968` pair to the warmer, visibly yellower `#F7D58A/#F2BB4F`, and Honey alone applies a `1.3×` motion/spawn-delay scale so both rise and emission cadence are 30% slower; core Juice bubbles remain unchanged.
- Flower depth-burst timing now begins with the bush at `0.10s` instead of waiting for late finale beats. Its three depth waves start at `0.10s/0.38s/0.66s`, and per-particle stagger is tightened to `35–45ms`; with the unchanged flight durations, the final depth flower completes within the requested roughly `0.5s` tail after the bush sequence. True bush2/4/6/8 interleaving, mid-flight foreground promotion, size, paths, pooling, and cleanup are unchanged.
- Flower foreground particles now use true Pixi interleaving instead of a flat DOM-above-bush overlay. A Flower-only depth profile distributes every randomized `flowr1…flowr6` particle across z-slots `0.5/2.5/4.5/6.5`, placing origins below bush2, bush4, bush6, and bush8 with different local offsets. Each flower begins partially occluded within its assigned bush depth, follows its own curved radial route, then promotes to `zIndex 8.5` mid-flight so it visibly emerges in front before completing the accepted exit. The same preloaded textures, tracked timelines, shared Sprite pool (raised to a bounded 40-item cap), idempotent disposer, and TNT cleanup owner are reused; core TNT and all non-depth burst variants keep their DOM owner unchanged.
- Honey crossing-path polish increases bee birth size from `50–65px` to a moderate randomized `58–75px`. Each bee now spawns roughly opposite its authored destination with an additional random lateral angular offset, while its movement vector still points toward the destination assigned to that bee PNG. The resulting independent paths cross through the central play space before diverging toward their own edges; stagger, buzz rotation, monotonic growth, edge-owned bounce-out, and cleanup timing remain unchanged.
- Honey finale copy/direction retune changes `STICKY` to `BUZZING!`, split after `BUZZ`: `#FFC14F` for `BUZZ` and `#D0784D` for `ING!`. Bee sprites now begin at a normalized random `50–65px`, then grow monotonically along their path (`1.03× → 1.08× → 1.13× → 1.18× → 1.24×`) before the existing edge overshoot/out. Flight lasts exactly `0.5s` longer (`1.2–1.32s`), and the magnetic overlay safety cleanup gains a Honey-only `0.6s` tail. Direction is authored by sprite identity with small angular randomness: bee1 southeast, bee2 northeast, bee3 west, bee4 northwest, bee5 mostly north with a slight west bias, and bee6 east with a slight southeast bias.
- The complete current Flower/Honey dirty-tree build passed `qa:full` (all TypeScript, lint, tests, visual contracts, production build, release bundle audit, and native source guard), exact Stack to Six raw-asset sync, `qa:ios`, signed Xcode build, final bundle/content verification, Wi-Fi install-over, and launch on `iPhone 13 blue`. The final app is `com.taptapdesign.stacktosix.Stack-to-Six`, contains 771 raw assets plus the current `BLOOMING!`, shuffled bee-sector, and `STICKY` runtime markers, and Apple confirmed installation URL ending `49891F5F-3F74-4070-9DDB-D3EA34F34006/Stack to Six.app`. Existing app data was preserved; no legacy shell or fallback device was touched. Physical Flower/Honey animation feel remains `NEEDS PHYSICAL TEST` on this installed build.
- Flower finale copy changes from `FLOWERY` to `BLOOMING!`; its existing two-color split, typography, motion, bush sequence, and flower sprites are unchanged.
- Honey bee path correction removes any remaining flock-like direction order. The complete radial sector list is shuffled before each finale, so sequentially appearing bees independently cover N/NE/E/SE/S/SW/W/NW plus intercardinal directions without neighboring bees sharing the same launch direction. Each one pop-enters already moving, alternates its own `7–10deg` buzz rotations and positional deviations across the path, reaches roughly 96% of its own edge vector, performs its cartoony overshoot there, then continues beyond the viewport while scaling out with `back.in(2.4)`. STICKY letter exit no longer interrupts bees mid-screen; each bee owns its edge exit, and the shared overlay cleanup remains the final safety owner.
- Honey swarm spacing follow-up makes the 16 bees visibly sequential (`45ms` index stagger plus only `0–25ms` jitter) instead of appearing as one centered batch. Their headings use near-even radial sectors with small random angular offsets, birth radii expand across three wider rings, and early path points move decisively away from center before continuing to all screen edges. Every bee independently alternates randomized `7–10deg` clockwise/counter-clockwise buzz rotations and small positional offsets while preserving constant settled scale and opacity; the Honey-only pop-in/pop-out and shared pooled cleanup remain unchanged.
- Honey bee finale replaces the inherited bolt-like repeat/strobe motion with a Honey-only one-shot flight lifecycle. Each randomized `bee1…bee6` sprite now performs one cartoony `back.out(2.8)` pop/settle, keeps a constant settled scale and full opacity while following its own four-point curved center-to-edge path with small positional/rotational buzz changes, and never repeats a fade or scale pulse. The existing Magnet overlay's `startExit` signal now kills any remaining bee flight and gives every bee a short overshoot plus `back.in(2.2)` directional pop-out before the shared cleanup returns images to `domElementPool`; core energy bolts retain their established looping behavior.
- Flower finale size/timing retune adds a variant-only `0.85×` bush frame scale, leaving core TNT sizing unchanged. The `flowr1…flowr6` foreground waves return to their original longer `0.12s/0.96s/1.78s` schedule, `0.92×` travel timing, and full internal stagger; their accepted `1.4×` size is increased by another 20% to `1.68×`. Pooling and cleanup ownership are unchanged.
- Local/web-only Honey special-die implementation guarantees Honey as the second Forest wild-meter drop (`wildSpawnCount === 1`) immediately after Flower on Forest boards 2–10. Honey reuses the complete `wild-magnet` gameplay/pull/input/endgame lifecycle while owning `honey(.@2x).png`, `STICKY` split evenly across `#FFC223/#FFAC28`, randomized `bee1…bee6` (1x/2x) flight sprites instead of energy bolts, bee-specific normal blending and nervous flight jitter, idle Juice-style pooled bubbles in `#FEE4B8/#FAC968`, shard colors `#F1BA79/#FDCF58`, and trail colors `#FBD099/#FEE4B8/#FDCD55/#F9AF3A`. The existing Magnet overlay, DOM image pool, tracked GSAP timelines, bubble Graphics pool, drag stop/restart, save/load variant ID, and cleanup owners are reused; core Magnet remains visually unchanged. `qa:fast` passes (16 suites / 182 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts), and the no-native-sync production build contains all 14 Honey/bee 1x/2x assets. No native bundle, install, commit, or push was performed; bee flight and idle feel remain `NEEDS PHYSICAL TEST` in web gameplay.
- Flower-only foreground burst timing is tightened to live inside the bush presentation instead of trailing it: its three waves now begin at `0.02s`, `0.25s`, and `0.50s`, use `0.48×` travel duration and `0.20×` internal stagger span, while retaining the accepted random six-flower selection and `1.4×` size. The shared default Wild Star timing is unchanged. Each wave still owns an idempotent auto-cleanup, every image returns to `domElementPool`, all GSAP owners are killed on normal or interrupted TNT cleanup, and only three disposer closures exist per active Flower finale.
- Flower finale particles are now flower-only across both visual owners: the bush foreground burst selects exclusively from `flowr1…flowr6`, and Flower's TNT-archetype bonus collection replaces the inherited `small-star.png` particle with a randomized sprite from the same six-image set. Both presentations use a true `1.4×` base-size multiplier; core TNT and Wild Star keep their existing particles and sizing.
- Flower `FLOWERY` finale text now keeps its existing 3.5-letter split but uses the requested near-white pair: `#FFFEFA` for the opening portion and `#FEF8EA` for the remainder. Flower trail and shard palettes are unchanged.
- Flower bush sequence correction removes `bush10` from the registered runtime frame list entirely. Flower now loads and animates only `bush1–bush9`, so `bush10` cannot appear as an exit remnant or final PNG; the TNT archetype lifecycle and core TNT's own frame sequence remain unchanged. This supersedes the earlier exit-only `bush10` experiment. `qa:fast` passes (15 suites / 180 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts); this remains web/source-only and `NEEDS PHYSICAL TEST` for animation feel.
- Flower merge-6 foreground follow-up reuses the established Wild Star center-burst motion with the six supplied `flowr1…flowr6` sprites. Three independently randomized waves eject nine flower sprites in front of the bush while it grows; all source images are prewarmed with the Flower finale and the shared burst owner cancels/returns every element and tween on interruption. At the frame-6 exit boundary, `bush1` and `bush2` are now killed and hidden before the remaining bush frames exit, so neither can flash back in. `qa:fast` passes (15 suites / 180 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts), and the no-native-sync production build contains the runtime markers plus all twelve 1x/2x flower-burst files. No native bundle, phone install, commit, or push was performed; final animation feel remains `NEEDS PHYSICAL TEST` in the web app.
- Flower pollen visibility follow-up corrects an over-subtle first tuning: each slower `760ms` release now emits 2–3 still-small `2–3.45px` grains, starts 2px above the flower center on a tile-relative foreground z-index, reaches its randomized `60–95%` opacity within roughly `240–340ms`, then spends about `1.5–1.9s` wandering widely left/right and gently upward before fading. There is still no balloon scale or TNT idle motion. TypeScript and the focused Flower contract pass; this remains web/source-only.
- Flower finale follow-up keeps `bush10` out of the center grow/hold stack. Only `bush1–bush9` build and hold the flowering bush; once their exit has visually completed, `bush10` appears level as the final ground-remnant frame, holds briefly, and performs its own short exit. This is a per-variant TNT-visual profile flag, so core TNT timing/art is unchanged. Focused Flower/registry tests pass (12/12) and TypeScript passes; no native sync/install or Git checkpoint was performed.
- Local/web-only Flower special-die implementation adds `assets/shop/bush/flower(.@2x).png` as the die and the ten real `bush1…bush10` frames (plus `@2x`) as a TNT-archetype finale without duplicating TNT gameplay resolution. Flower is the first wild-meter special in Forest Stage 2; completing that Stage replaces the old Forest Juice reveal with the Flower unlock, and unlocked Flower remains the first special across later Forest Stages 3–10. Its merge-6 finale uses the same TNT input/endgame/board-blast lifecycle with the bush sequence instead of TNT smoke art, `FLOWERY` split after 3.5 letters from `#FFCDC7` to `#FC8C75`, trail palette `#FFE4D4/#FFBBAD/#F9999F/#FFD257`, and merge shards `#FFCDC7/#FC8C75`; core TNT remains visually unchanged. Flower no longer inherits TNT idle particles, TNT shake, or generic special-die float: its sole idle owner emits mostly one tiny pollen dot from the flower center every `620ms`, with a wider randomized left/right drift, a slow three-phase upward wander, no balloon-like scale bounce, and randomized peak opacity from `60–95%` in `#FFF16B/#FFE246/#FFCE34`. The emitter reuses the existing Graphics pool and tracked intervals/timelines, stops during drag, restarts after snap-back, and fully releases particles on merge/exit/rebuild. The TNT animation owner accepts variable-length registered frame/text profiles, caches preloads per profile, retries failed loads, reuses its existing Pixi pool/timelines/cleanup, and prewarms Flower frames when the die spawns. Save/load keeps using the established special-variant ID. Focused Flower/registry tests pass (12/12), `qa:fast` passes (15 suites / 180 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts), and a no-native-sync production build contains the `FLOWERY` runtime marker. No native bundle, Xcode build, phone install, commit, or push was performed; Flower animation feel remains `NEEDS PHYSICAL TEST` after web acceptance.
- `v911 stabilize Homepage motion and isolate Fail navigation` raises the web package version to `2.0.630`. It consolidates the accepted current-Round cold-entry owner, completed-World banner persistence, Promise-owned Homepage enter/exit lifecycle, immediate `430ms` Homepage navigation entrance, and the Journey `No moves` → Fail navigation-isolation fix documented below. `qa:full` passes (95 suites / 570 tests plus TypeScript, full lint, source/bundle audits, visual contracts, no-native-sync production build, and native source guard). The exact build is physically installed and launched on `iPhone 13 blue` at installation URL ending `B649D7FC-2352-48FA-AF33-F6CD0E0494A3/Stack to Six.app`; the user confirmed both Homepage navigation timing and removal of the Fail-frame flicker as perfect. Existing data was preserved and no legacy shell or fallback device was touched.
- Journey `No moves` → Fail handoff no longer exposes Homepage navigation for a frame. The root cause was a legacy `navigation-control` Fail branch that explicitly changed the complete `#independent-nav` Unit (icons plus divider) to `display:block`, `opacity:1`, and a z-index above the still-transparent first Fail-overlay frame; `app-core` also invoked that branch immediately before creating the Fail DOM. Fail now owns its own terminal UI from its first frame, Homepage navigation remains fully hidden/non-interactive throughout active gameplay and Fail, and the premature navigation update was removed. Focused regression coverage passes (2/2), and `qa:fast` passes (8 suites / 48 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). Production build, exact 747-asset Stack to Six `Web.bundle` sync, `qa:ios`, signing, exact bundle-ID verification, source marker, and matching `dist`/native/final-app entrypoints pass. After four interrupted Wi-Fi attempts, the next persistent install-over completed with Apple's `App installed` confirmation only on `iPhone 13 blue`, at installation URL ending `B649D7FC-2352-48FA-AF33-F6CD0E0494A3/Stack to Six.app`; the exact bundle ID then launched successfully. Existing app data was preserved and no legacy shell or fallback device was touched. Physical `No moves` reproduction remains `NEEDS PHYSICAL TEST` on this installed build.
- Homepage return enter now starts the complete `independent-nav` Unit—icons plus its owned bottom line/shadow—immediately at `0ms` and settles it over `430ms`, replacing the previous late `155ms` start with the shared `650ms` duration. This makes navigation visibly establish during the primary hero/logo beat rather than at the end. Hero, logo, content, CTA, exit lifecycle, easing, cleanup, and interaction unlock are unchanged; the existing enter owner still owns all cleanup and no new timer/tween owner was added. `qa:fast` passes (7 suites / 46 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This follow-up is included in the `B649D7FC-2352-48FA-AF33-F6CD0E0494A3` physical install documented above.
- Homepage slider motion now has one canonical Promise-based exit lifecycle across Arcade Play, Journey, Settings, and Journey-board Homepage starts/resumes. Modern browsers reuse the established zero-safe Homepage→Journey WAAPI owner (`back.in(1.25)` approximation), route only after actual target completion, freeze the currently painted transform when a fast exit interrupts enter, and normalize transforms only after Homepage is hidden; the old CSS/770ms path remains solely as a no-WAAPI fallback. Homepage return enter now exposes and settles a real Promise at final cleanup instead of a separate 1120ms route guess, and Homepage-scoped enter/exit curves use direction-correct `back.out`/`back.in` approximations without crossing through scale zero. Legacy overlay/bottom-sheet `.animate-*` behavior is untouched. Focused Homepage/Journey contracts pass (21/21), and `qa:full` passes (94 suites / 567 tests plus type-check, full lint, audits, visual contracts, no-native-sync production build, bundle audit, and native source guard). The complete 723-asset web tree was then synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios`, signing, exact bundle-ID verification, entrypoint comparison, install-over, and launch all passed on `iPhone 13 blue`. Apple confirmed installation URL ending `CA8592B1-F2AD-4F2F-AE34-751954440D4B/Stack to Six.app`. Existing app data was preserved and no legacy shell or fallback device was touched. Physical animation feel remains `NEEDS PHYSICAL TEST`.
- Journey Hub World banners now preserve completed progress history: a World renders its banner when it owns the current interim card **or** when all ten regular cards are unlocked. Completing Forest therefore keeps `10/10` visible while Beach independently shows `0/10`; future Worlds without an interim card and without completion still have no banner. Count calculation, unlock/interim progression, banner motion, and World navigation are unchanged. The focused Hub contract passes (16/16), and `qa:fast` passes (6 suites / 41 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This change is local/web-only and is not installed, committed, or pushed.
- Arcade current-Round entry latency now has an explicit runtime policy. The first local attempt started the cue before `bootGame`; boot cleanup killed that GSAP owner while its Promise remained pending, leaving both cue and cubes blocked. Three independent cold-entry audits then confirmed that first Play uniquely creates the PIXI/WebGL renderer and warms HUD/core textures while later Play reuses them. In native `app://`, the one-shot cue now starts immediately after destructive GSAP cleanup and before cold `Application.init`, covering that work; the user physically confirmed the installed native behavior is good. Desktop `http(s)` does not overlap because synchronous WebGL initialization froze GSAP mid-letter/number; web now starts the same cue immediately after boot and before saved layout/restore, keeping the animation fluid. Fresh/load pop-in consumes the exact same owner before cubes enter, recovery remains post-pop-in, and boot failure explicitly cancels the owner/overlay. Focused tests pass (10/10), and `qa:fast` passes (5 suites / 25 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). The native-overlap build passed production sync, `qa:ios`, signing, exact bundle-ID verification, install-over and launch only on `iPhone 13 blue` at installation URL ending `8450E21F-FD92-4B03-BB47-982245631DAE/Stack to Six.app`; existing data was preserved and legacy Kockice Crash was untouched. The subsequent web-only runtime split is local/dev-server-only and is not yet rebuilt into or installed on native, committed, or pushed.
- `v910 serialize Arcade launch and show current Round cue` raises the web package version to `2.0.629`. It combines the physical cold-launch Homepage ownership fix with the complete Arcade current-Round intro contract: an early Slide 1 hero tap cancels unfinished Homepage enter callbacks and has one async route owner, while every valid Arcade entry shows its actual current `Round 0N` before cubes enter—including fresh Round 01 and saved Round 01. This visual-only cue never emits `Nice/thumb`, advances progression, or replaces the established clean-board transition. `qa:full` passes (92 suites / 559 tests plus type-check, full lint, audits, visual contracts, no-native-sync production build, bundle audit, and native source guard). The final production output was restored to 723 raw assets, synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed. The signed final app has exact bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`; `dist`, native bundle, and final-app entrypoints match. Install-over completed on `iPhone 13 blue` at installation URL ending `0E214E38-AFAD-481D-A823-196BBDEE7625/Stack to Six.app`, and that exact bundle launched successfully. Existing data was preserved and legacy Kockice Crash was untouched. Physical confirmation of fresh/saved Round 01 presentation remains `NEEDS PHYSICAL TEST` on this installed build.
- Fresh Arcade now shows the same pure current-Round number cue as resume: a brand-new run displays `Round 01` after Homepage exit and before any new cube or ghost can paint, then begins the unchanged `sweetPopIn` for Round 01. It does not reuse clear-phase `Nice/thumb`, does not advance progression, and does not affect the established clean-board Round transition. `startNewGame` arms a one-shot Round 01 cue; `rebuildBoard` captures/clears it, hides the freshly constructed cubes, awaits the shared visual-only cue through the pop-in runner, then releases the original board entrance. The web browser commonly routes Play through saved-state resume even while the save is still Round 01, so the resume gate now accepts every valid saved Round `01+` rather than only `02+`; saved Round 01 therefore receives the same intro before its exact saved cubes enter. Focused ordering/continuation coverage and `qa:fast` pass (6 suites / 23 tests plus TypeScript, changed-file lint, diff hygiene, and Journey visual contracts), and a production web build passes. Included in the `v910` native bundle/install documented above.
- Physical cold-launch trace on `v909` isolated a Homepage ownership race when the user taps Slide 1's hero image while its startup enter is still animating. The accepted Arcade route previously left `animateSliderEnter()` delayed callbacks alive, allowing them to reveal Slide 1 again during exit, and the route itself had no one-shot owner, so the same early gesture could produce duplicate `gameplay → arcade-stage-clear` activations. `triggerGameStartSequence` now synchronously cancels both Homepage enter owners before the unchanged exit begins, rejects duplicate triggers for the complete async handoff, awaits the existing 770ms exit envelope, hides Homepage, and awaits exactly one fresh/resumed game start before releasing its lock. Focused cold-launch/continuation tests pass (6/6), and `qa:fast` plus `qa:ios` pass. The rebuilt 723-asset signed Stack to Six app has exact bundle ID/entrypoint and was installed over existing data on `iPhone 13 blue` at installation URL ending `6DCF4D09-D324-4899-BF19-AAFFFADC564A/Stack to Six.app`. Same cold-launch hero-tap retest remains `NEEDS PHYSICAL TEST`; this follow-up is local-only and not committed/pushed.
- `v909 stabilize Arcade continuation and terminal lifecycle` raises the web package version to `2.0.628`. It includes the supplied-console Magnet rollback/terminal lifecycle fix and the corrected Homepage Arcade continuation contract: saved Round 02+ shows only its current `Round 0N` visual, keeps the exact saved board/progression, waits for that cue before saved cubes enter, and cannot run recovery/endgame validation while entrance tiles are hidden. `qa:full` passes (90 suites / 556 tests plus type-check, full lint, audits, visual contracts, no-native-sync production build, bundle audit, and native source guard); after the final continuation race fix, `qa:fast` passes (21 suites / 216 tests). The final `2.0.628` production output was rebuilt, restored to 723 raw assets, synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed. The signed Stack to Six build succeeded with exact final bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`; `dist`, native `Web.bundle`, and final-app entrypoints match. After one recoverable Wi-Fi/RSD transport failure, install-over completed on `iPhone 13 blue` at installation URL ending `13F2B1C4-309F-4E20-B217-A7BA869D2343/Stack to Six.app`, and that exact bundle ID launched successfully. Existing app data was preserved; legacy Kockice Crash was untouched. The exact Round 05 resume feel and saved-board continuity remain `NEEDS PHYSICAL TEST` on this installed build.
- Critical Arcade empty-board incident is fixed from the supplied 11,368-line console trace. Magnet selection and commit previously disagreed about a visible `_ccWildSpawnDropping` target: the caller locked/detached it and marked prospective pull ownership, the handler rejected it, then the caller ignored `false`, skipped rollback/spawn/endgame, and left protected star-flight visuals frozen over an empty logical board. One shared candidate contract now keeps a dropping wild as endgame-blocking presence but never Magnet food; intentionally locked Magnet-owned tiles remain commit-valid. A late commit rejection now clears prospective destination flags, restores every pulled tile/grid/input state, kills the owned timelines/guard, and schedules exactly one centralized endgame check. Wild-meter progress/reset and any resulting spawn are now strictly post-commit, so abort has no speculative gameplay side effects. Confirmed terminal flows force-clean protected star sprites before board exit, Arcade run-summary Exit skips a board exit already completed by the terminal owner, and Arcade `startLevel()` no longer writes Round/score state into Journey progression or sync. Focused incident contracts pass (26/26), `qa:fast` passes (20 suites / 213 tests), `qa:full` passes (89 suites / 553 tests plus type-check, full lint, audits, visual contracts, no-native-sync production build, bundle audit, and native source guard), and the independent post-fix reviewer reports no remaining severity finding with verdict `PASS`. `dist` was rebuilt without native sync; Stack to Six `Web.bundle` and phone remain untouched. Physical reproduction of the exact juice→dropping-star→magnet chain remains `NEEDS PHYSICAL TEST`.
- Arcade Homepage continuation now reads the already-authoritative `cc_arcade_run_state_v1` snapshot and, when its saved `boardNumber`/fallback `level` is Round 02 or higher, presents only the shared pure `Round 0N` number animation as a one-shot resume indicator after the gameplay surface appears. It does not complete a Round, increment progression, reset/rebuild the board, or call `startLevel`; Round 01 keeps the normal entry with no extra cue. Two independent false-advance paths are closed. First, zone teardown previously killed a stage-clear overlay without settling its Promise, then resume cleanup resolved that abandoned owner as `continue`; stage-clear teardown now emits explicit `cancel`, and `endgame-flow` returns without progression. Second, the 1.3s post-load recovery timer previously started while the saved-Round cue still kept every restored tile hidden; it could therefore classify a valid mid-Round save as a clean board and produce ghost exit → Nice/thumb → next Round. Recovery/endgame validation is now scheduled only from the completed saved-board pop-in edge, after the full cue and after tiles are visible, including the pop-in failure fallback. Resume sequencing is therefore deterministic: restore and hide saved cubes → show gameplay/HUD and saved `Round 0N` cue → pop in the exact saved cubes → only then arm recovery validation. The one-shot cue is captured by the load owner and cleared by UI Manager, so it cannot leak into a later load; malformed/missing saves and cue failures fail open into normal resume. Contracts lock pure-visual ownership, cancellation/no-advance behavior, hidden-cubes → cue → cube-pop-in ordering, and recovery ownership after entrance completion. `qa:fast` passes (21 suites / 216 tests plus TypeScript, changed-file lint, diff hygiene, and Journey visual contracts). Web/native build and phone install are pending; timing/feel is `NEEDS PHYSICAL TEST`.
- `v908 polish CTA motion and compact Journey composition` raises the web package version to `2.0.627`. Pre-push `qa:full` passes all deterministic gates: type-check, source audit, full lint, 87 suites / 544 tests, Journey visual contracts, no-native-sync production build, built-bundle audit, and Stack to Six native source guard. The most recent physical install immediately before this checkpoint already contains the same runtime source/CSS behavior; only package metadata and this checkpoint documentation were added afterward.
- Journey Worlds Hub vertical composition is tighter without changing horizontal placement or motion: after final visual tuning, Beach sits at `334px` (8px above the prior pass) and Area 55 at `580px` (4px below the prior pass). Beach's local cloud band follows upward, Area 55's bottom band follows downward, the inter-world cloud band remains fixed, and the Hub/cloud canvas follows Area 55 so no extra paper tail appears. A focused layout contract locks the three World positions, compact canvas, and representative cloud anchors; Journey Hub tests pass 16/16 and `qa:fast` passes (64 tests plus lint/visual contracts). Included in the latest physical install.
- Journey World banner retract is slightly slower (`260ms → 320ms`) so its existing inward spring/bounce-out reads before the World completes its unchanged exit. Banner enter, idle, easing, positions, pivots, and World motion are unchanged. The focused Journey Hub contract passes and `qa:fast` passes. Included in the latest physical install.
- Homepage route exit now handles CTA activation while the carousel is physically between slides. The existing slider lock freezes the live wrapper position; a shared viewport-rect resolver finds every slide currently intersecting the slider viewport, and both the general Settings/Arcade/Collection exit and the dedicated Journey exit animate each visible slide's hero, text, tagline, and master CTA in the same coordinated wave before Homepage hides. At a settled position only the single visible slide is targeted, preserving the accepted benchmark. `qa:fast` and production build pass. Included in the latest physical install; partial-drag visual feel remains a manual QA item.
- Shared primary/secondary CTA shimmer now follows the Journey banner's masked-light language: one `10s` master cycle contains two slow warm/cream sweeps, a short breath between them, then a long idle pause. The former broad bright-white single stripe was removed; the narrower mask and reduced alpha keep both orange and white CTAs legible. Homepage CTA gestures now also retain exclusive ownership from press through captured release: the slider's local touch/mouse handlers reject `.cc-cta` starts, Homepage CTA surfaces use `touch-action:none`, and a sideways hold-drag still activates the intended route on release without exposing an adjacent slide. Other CTA surfaces keep standard drag-away cancellation. The CTA contract locks the shimmer and gesture ownership; `qa:fast` passes (63 tests plus lint/visual contracts), production bundle sync and `qa:ios` pass, and the signed build is installed/launched on `iPhone 13 blue`. Physical shimmer/sideways-drag feel remains `NEEDS PHYSICAL TEST`.
- Active CTA bottom sheets now use the Homepage slider's immediate pointer-release benchmark instead of waiting through the shared `260ms` release-to-idle bounce before beginning their CTA-group exit. Clean Board, Board Fail, End Run, Collectible Reward, and First Play `Got it` explicitly use `activationTiming: 'immediate'`; the pressed visual hands directly into the existing clicked-first paired/group bounce-out. Shared enter/press/hold/exit values, primary/secondary styling, companion stagger, CTA-before-sheet close ordering, actions, drag/outside dismissal, and non-sheet screen/modal CTAs remain unchanged. Focused CTA coverage locks the benchmark across every migrated sheet (11/11); `type-check` and `qa:fast` pass (63 changed-file tests plus lint and visual contracts). Production sync, `qa:ios`, and signed Xcode build passed; the complete Wi-Fi session was held through success and installed only on `iPhone 13 blue` at installation URL ending `BD0C8B15-A083-47F6-9265-28DFBF2252AD/Stack to Six.app`, then launched successfully. This install also includes the explicit Journey World bounce-out and faster springy banner retract. Existing data was preserved and the legacy shell was untouched. Physical bottom-sheet CTA parity remains `NEEDS PHYSICAL TEST`.
- Journey World progress banners now retract faster and with a springy inward snap during Hub exit. Only the banner-local retract transition changes from `440ms` to `260ms`, with `cubic-bezier(0.56,-0.22,0.78,0.34)` providing a tiny outward anticipation before accelerating behind the World PNG. The newly explicit World `120ms` anticipation/`480ms` total exit, banner enter/idle, pivots, positions, counts, and all Journey lifecycle ownership remain unchanged. Focused Journey tests pass (26/26), `type-check` passes, and `qa:fast` passes (63 tests plus lint and visual contracts). Build/install are pending.
- Fixed the Journey Worlds Hub → Homepage exit losing its visible outward anticipation after returning from Forest/Beach/Area 55. The physical trace showed `world-return` completing and handing CSS idle/gyro transforms to exit only ~302ms before Back; relying solely on the subtle implicit `back.in(1.25)` overshoot let WKWebView visually collapse that first phase into a monotonic shrink. Back-to-Homepage now owns an explicit deterministic two-step World exit: `scale 1 → 1.06` over `120ms`, then `1.06 → 0.65` plus fade over the remainder of the existing `480ms` envelope. Reduced Motion keeps zero anticipation. Hub→World, Unit enter/exit, clouds, nav, banners, viewport pinning, stagger, and total duration remain unchanged. Focused Journey motion/ownership tests pass (26/26), `type-check` passes, and `qa:fast` passes (48 tests plus lint and visual contracts). Web/native build and phone install are pending; physical feel is `NEEDS PHYSICAL TEST`.
- Homepage slider CTA activation now hands navigation ownership over immediately on the real pointer release instead of awaiting the full shared `260ms` release bounce. This removes the perceived press → release → dead pause before exit while preserving the approved deep press, stable tap-and-hold, `310ms` route-owned exit, and full release-before-action contract on non-Homepage CTAs. All four Homepage actions use the existing master controller's `activationTiming: 'immediate'`; route guards and screen choreography are unchanged. Focused CTA tests pass (11/11), `type-check` passes, `qa:fast` passes (37 tests plus lint and visual contracts), production sync, `qa:ios`, and signed Xcode build pass. After earlier transfers failed with `IXRemoteErrorDomain 6` and a temporarily unavailable device tunnel, the restored Wi-Fi session was held through the full large-bundle transfer. Install-over succeeded only on `iPhone 13 blue` at installation URL ending `2E4800A0-5DBF-4D28-8A48-5CCE6F7E7176/Stack to Six.app`, and that exact responsive-tap bundle launched successfully. Existing data was preserved and the legacy shell was untouched. Physical direct press-to-exit feel remains `NEEDS PHYSICAL TEST`.
- Fixed inconsistent physical CTA tap-and-hold on WKWebView. The master controller previously treated `lostpointercapture` as a release, but iOS can emit that event while the finger remains physically down, causing the first hold to rebound to idle while a later hold happened to remain pressed. Capture loss no longer releases the visual; actual `pointerup`/`pointercancel` are observed on both the button and `window`, keyed to the active pointer ID, so a held CTA stays at the pressed scale and an off-element release still settles safely. All listeners remain AbortController-owned and disposal-safe. Focused CTA tests pass (11/11), `type-check` passes, and `qa:fast` passes (37 tests plus lint and visual contracts). Production sync, `qa:ios`, and signed Xcode build with the renewed profile pass. The first large Wi-Fi transfer ended with explicit `IXRemoteErrorDomain 6 / Connection interrupted`; after restarting `CoreDeviceService`, the second transfer was held through completion and installed successfully only on `iPhone 13 blue` at installation URL ending `BC3C54DA-3B98-4FE8-BAB3-A939787351BF/Stack to Six.app`, then launched successfully. Existing data was preserved and the legacy shell was untouched. Physical hold consistency remains `NEEDS PHYSICAL TEST`.
- Master CTA motion has a calmer, more tactile global tuning after physical review: press scale deepens from `0.88` to `0.84`, press travel is now a centrally configurable `4px`, press duration increases from `90ms` to `120ms`, release from `220ms` to `260ms`, and bounce-out from `240ms` to `310ms`. Primary and secondary actions inherit the same values; screen/sheet choreography, companion stagger, enter timing, layout, and action semantics are unchanged. Focused CTA tests pass (11/11), `type-check` passes, and `qa:fast` passes (37 tests plus lint and visual contracts). The production build was synced only to Stack to Six, `qa:ios` passed, and Xcode signed successfully with renewed profile `6d49d8d4-e0f8-44ac-b867-1d760121ecab`. The large Wi-Fi transfer was held and polled through completion; install-over succeeded only on `iPhone 13 blue` at installation URL ending `4301DCB0-8667-45C8-BEE8-DC098BA05739/Stack to Six.app`, and that exact bundle launched successfully. Existing data was preserved and the legacy shell was untouched. Physical CTA feel remains `NEEDS PHYSICAL TEST`.
- Fixed the physical first-launch Homepage CTA race across Play, Journey, Shop, and Settings. Startup/finalize/restore paths can legitimately call `reattachHomepageButtonListeners()` more than once; the old implementation disposed and recreated every master CTA controller on each call, which could interrupt the first pointer release or route-exit and leave the visual pressed. Homepage controllers are now retained in an element-keyed map, repeated attachment is idempotent, and only disconnected/replaced DOM buttons are disposed. Focused CTA coverage now forbids the former blanket `splice(0)` recreation; the focused suite (11/11), `type-check`, `qa:fast` (27 tests plus lint and visual contracts), production build/native sync, `qa:ios`, and signed Xcode build all pass. A requested reinstall exposed an expired embedded development profile (`0xe8008011`); Xcode successfully fetched profile `6d49d8d4-e0f8-44ac-b867-1d760121ecab` with `-allowProvisioningUpdates` and rebuilt successfully. The large Wi-Fi transfer continued asynchronously after the initial usage-assertion output; retaining and polling its terminal session returned explicit install success only on `iPhone 13 blue`, at installation URL ending `F8156B82-0807-4CF9-8F08-41EBADD0837E/Stack to Six.app`, and that exact renewed-profile bundle launched successfully. Existing app data was preserved and the legacy shell was untouched. Future large-bundle installs must keep polling the returned `devicectl` terminal session rather than treating the initial yielded output as completion. Physical first-launch CTA feel remains `NEEDS PHYSICAL TEST`.
- Homepage slider CTAs are restored to their accepted pre-master vertical layout on phone. The master phone rule no longer adds `top: 2.25vh`, which had shifted all four buttons too far below their hero art; their existing slide-text/margin layout now places them closer to the main image as before. Widths, master enter/press/release/exit motion, gyro ownership, and iPad/desktop positioning are unchanged. `qa:fast` and the web-only build pass; complete raw assets and the exact `dist` were then synced only to Stack to Six, signed Xcode build and bundle guards passed, and Wi-Fi install-over plus launch succeeded on `iPhone 13 blue` at installation URL ending `427979F9-C636-484C-A3B2-F50ABAC1292D/Stack to Six.app`. No other device or legacy shell was touched.
- Checkpoint `v907` consolidates the accepted master CTA migration across player-facing primary/secondary actions, strict CTA-before-surface exit ownership for active bottom sheets, Tutorial/Reward/Detail/Homepage/Tilt integrations, complete Hearts removal, First Time Run reset correction, canonical paper surfaces, and the proven-unreachable Resume/Pause/duplicate End Run cleanup. Package version is `2.0.626`. `qa:full` passes (87 suites / 543 tests plus TypeScript, release audit, lint, visual contracts, production build, bundle audit, and native source guard). The complete raw assets and exact production `dist` were synced only into the authoritative Stack to Six `Web.bundle`; bundled mode, matching entrypoints, runtime/launch assets, signed Xcode build, and exact bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` were verified. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `B6285A8A-0245-4C42-9D02-3DB5E82B5541/Stack to Six.app`, and that exact bundle launched successfully. No fallback device or legacy shell was touched. This checkpoint supersedes `v906` on `feature/v700-journey-hub`.
- Removed the second proven-unreachable modal cluster after a complete import/global/CSS audit. `pause-modal.ts` had no runtime caller and HUD had already replaced it with the menu screen; its private `pause-ui.ts` and `pause-animations.ts` dependencies are deleted. Duplicate `end-run-ui.ts` and `end-run-animations.ts` had no imports and are deleted; active `end-run-modal.ts` remains authoritative. The inline `showResumeGameModal()` in `app-core.ts` was only assigned to an unused `window` hook and is deleted together with that type/global assignment. Exclusive Pause/old End Run CSS and obsolete docs are removed, plus modal-only state/helpers from `pause-utils.ts`; active `pauseGame()`, `resumeGame()`, score access, save/load migration, End Run, menu, and board lifecycle remain intact. A removal contract prevents all three legacy UI systems from returning.
- First Play Tutorial's active Step 3 `Got it!` action now uses the master primary CTA controller. The former direct click listener and local root hover/active transform owner are removed; the CTA keeps its authored visibility rule and responsive `310px/249px` widths while its inner visual owns enter, press/release, and exit. Both tap activation and the existing tutorial drag-dismiss path await the complete CTA exit before moving the tutorial sheet down and starting the Step 4 wild monitor. Overlay removal disposes the controller. Tutorial board restrictions, pointer choreography, HUD dimming, wild spawn reservation, and completion flow are unchanged. `qa:fast` passes (11 suites / 55 tests plus TypeScript, lint, diff hygiene, and visual contracts).
- The strict CTA-before-surface exit contract now covers every active bottom sheet that contains CTA actions. Clean Board and Board Fail await the complete clicked-first primary/secondary CTA exit before starting their card/content collapse. The active Collectible Reward sheet replaces its parallel legacy action listeners with master primary/secondary controllers, awaits both CTA exits before moving the sheet down and fading its overlay, and defers `View Collection` navigation until close completion. End Run retains the same already-correct strict sequence. Score/Combo is intentionally unchanged because it has no CTA actions. Drag, outside close, keyboard close, rewards, retry/menu decisions, board-save behavior, and navigation targets are preserved. Contract coverage now asserts all active CTA sheets and rejects legacy reward button listeners.
- CTA migration phase 6 moves the actual active End Run sheet onto the master system. Arcade `New Game / Exit Game` and Journey `Restart / New Card / Exit Stage` register shared primary/secondary controllers, use one Pointer Events activation owner, enter with the central stagger token, and exit as one clicked-first CTA group. Button activation now strictly awaits the complete CTA-group exit before the sheet receives its closing state and moves down, preventing its entrance owner from resurfacing the sheet during close. Drag/outside/forced dismissal exits every visible CTA and hard cleanup disposes all controllers; board save, restart, Journey return routing, sheet drag, and the surrounding 400ms bottom-sheet lifecycle are unchanged. A three-or-more-action `exitCtaGroup()` primitive now extends the existing pair API without creating screen-local timing. Focused CTA/paper coverage passes, `qa:fast` passes (11 suites / 53 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped. Native bundle and phone remain untouched; live feel is `NEEDS PHYSICAL TEST` after web approval.
- Corrected the Resume Game audit after tracing actual reachability: `UIManager.checkForSavedGame()` had no caller anywhere, and its `showResumeGameBottomSheet()` reference was the only runtime reference to that sheet. Current Homepage Play already resolves Arcade continuation through `hasArcadeSavedState()` plus `triggerGameStartSequence`, while Journey uses board-scoped saves. Therefore the old `Resume Game? / Continue / New Game` sheet was unreachable legacy, not an active flow. Removed its unused UIManager import/method, `resume-game-bottom-sheet.ts`, `resume-bottom-sheet.css`, the already-duplicate `resume-sheet-ui.ts`/`resume-sheet-utils.ts`, remaining `.resume-bottom-sheet` style/hint hooks, and obsolete guide entry. The shared `resume-sheet-animations.ts` remains because active Score/End Run flows use it. A removal contract prevents this legacy surface from returning. Master root pseudo-element suppression remains as a valid global guard. Focused removal/CTA/paper tests pass (11/11), `qa:fast` passes (11 suites / 52 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped. Native bundle and phone remain untouched.
- CTA migration phase 5 moves the centered launch `Tilt Motion` modal's `Try It` primary and `Later` secondary actions onto the master CTA system. Both now share the canonical 3D visual, shimmer, press/release, staggered enter, paired exit, disabled-color guard, and controller cleanup; their scoped width is `249px`. The surrounding centered paper modal, art, copy, card bounce, launch ownership, seven-day Later cooldown, and Developer forced-preview behavior are unchanged. The master registration API now supports an explicit `activationTiming: 'immediate'` contract so `Try It` invokes iOS `DeviceOrientationEvent.requestPermission()` synchronously inside the trusted user gesture before any animation await; this prevents the shared press/release lifecycle from breaking WebKit permission. CTA exit runs concurrently with the existing card bounce-out. Permission/modal tests pass (10/10), focused CTA/launch contracts pass (19/19), `qa:fast` passes (10 suites / 51 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped. Native bundle and phone remain untouched.
- Hearts has been completely removed from the product source rather than retained behind a disabled flag. Deleted `hearts-system.ts`, `hearts-bottom-sheet.ts`, and the duplicate `lives-manager.ts`; removed the Journey header lives DOM/CSS, initialization/click hooks, refill/storage/timer logic, fail-time heart consumption and retry blocking, bottom-sheet selectors/surfaces, app cleanup imports, and the obsolete `no-hearts` result branch. Board Fail retry now follows its existing direct restart lifecycle with no Hearts gate; Arcade/Journey scoring and progression logic are unchanged. A removal contract prevents the runtime modules or product hooks from returning. Focused removal/paper tests pass (3/3), `qa:fast` passes (8 suites / 29 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes while native sync is explicitly skipped. Native bundle and phone remain untouched.
- CTA migration phase 4 moves all four Homepage slider CTAs (`Play`, `Journey`, `Shop`, and `Settings`) onto the master controller. `UIManager` now registers their original actions as master activations instead of attaching parallel button click handlers; reattachment disposes and recreates controllers safely, while hero-image CTAs remain independent. The Homepage animation owner calls the same controller `prime/enter/exit` lifecycle for startup, normal Homepage transitions, and the dedicated Homepage-to-Journey exit, so legacy root/iPad transforms no longer compete with press/release motion. The master controller now exposes interruption-safe immediate `hidden/idle` priming for screen preparation. Registered Homepage roots drop the legacy `tap-scale/menu-btn-primary` owners, preserve `250px` desktop/iPad and `226px` phone widths, and retain the established phone vertical offset through layout positioning rather than motion transform. Focused CTA contract coverage passes (7/7); `qa:fast` passes (6 suites / 26 tests plus TypeScript, lint, diff hygiene, and visual contracts); and the web-only production build passes with `SKIP_NATIVE_BUNDLE_SYNC=true`. Existing localhost `http://localhost:5174` responds `200`. Native bundle and phone remain untouched; live visual/touch feel is `NEEDS PHYSICAL TEST` after web approval.
- Fixed the master CTA exit color flash: `exit()` intentionally disables input immediately, but the generic disabled-state filter also desaturated/darkened the orange visual before its bounce-out, making it look like a brown active overlay. Exit now publishes `data-cta-state="exiting"` before disabling input, and exiting/hidden CTAs explicitly retain their normal color while scaling/fading away. Genuine idle disabled CTAs keep the subdued treatment. Focused CTA coverage passes (6/6), and `qa:fast` passes (6 suites / 25 tests plus TypeScript, lint, diff hygiene, and visual contracts). Web-only; native bundle and phone remain untouched.
- Fixed the Journey detail primary CTA's missing 3D depth: the legacy `#board-detail-play-button { overflow: hidden !important; }` rule was clipping the shared inner visual's `8px` bottom stroke and outer drop shadow. The registered master CTA now explicitly keeps its layout root overflow visible, while `.cc-cta__visual` continues clipping only its own shimmer/content. No dimensions, color, motion, modal lifecycle, or activation logic changed. Focused CTA contract coverage passes (6/6), and `qa:fast` passes (6 suites / 25 tests plus TypeScript, lint, diff hygiene, and visual contracts). Live on localhost web only; native bundle and phone remain untouched.
- CTA migration phase 3 moves both active Journey detail paths onto the master system: regular Stage cards use the floating `Play/Continue` CTA, while interim Stage cards use their embedded `Continue` CTA. Both register the same primary controller, share global enter/press/release/exit timing, and remove the former parallel `click + touchend` activation and homepage CSS animation classes. Detail modal exit invokes the master CTA exit first while preserving the existing card/stats/header choreography; repeated open/close and hard cleanup dispose controllers, floating buttons are removed, and the persistent interim template button is retained hidden for its next clone. Phone detail width remains `220px`, iPad/desktop `249px`, with identical motion. Focused CTA contract coverage passes (6/6), and `qa:fast` passes (6 suites / 25 tests plus TypeScript, lint, diff hygiene, and visual contracts). Live on localhost web only; Homepage CTA, native bundle, and phone are untouched.
- Fixed the first CTA phase-2 visual regression: Tutorial Complete rendered no visible Continue button because its dynamically injected legacy root selector still applied `opacity:0; transform:scale(0)` after the master stylesheet loaded, while the new system correctly animated only `.cc-cta__visual`. The canonical `.cc-cta` root now forcefully owns layout-only invariants (`opacity:1`, no transform/transition), ensuring old screen selectors cannot hide or transform registered roots; all motion remains on the inner visual. This also protects New Reward and Special Dice from the same injected-CSS ordering bug. Contract coverage asserts the ownership boundary and `qa:fast` passes (6 suites / 24 tests plus TypeScript, lint, diff hygiene, and visual contracts). Web-only; no native bundle or phone changed.
- Developer Settings `First Time Run ON` now prepares a genuinely fresh tutorial launch instead of only arming the tutorial flag. Before arming, it removes the global/transient save, completion marker, legacy game state, Arcade resume state, and stale completion runtime flags that could divert the next Homepage Play tap into Continue/resume. Journey progression, board highscores/stars, collectibles, settings, and other player data remain untouched. The reset policy is isolated in `first-play-tutorial-dev-reset.ts` and called before `armFirstPlayTutorial()`. Focused regression tests pass (2/2), and `qa:fast` passes (6 suites / 24 tests plus TypeScript, lint, diff hygiene, and visual contracts). The fix is live on localhost web only; native bundle and phone remain untouched.
- CTA migration phase 2 moves Tutorial Complete, Journey New Reward, and Journey Special Dice `Continue` buttons onto the approved shared CTA system. Each screen now registers one primary CTA, uses the central press-release activation sequence, calls the same global enter/exit profile, and disposes through the existing screen cleanup. Their former legacy class stack, local click listener, and direct GSAP/CSS CTA transform timelines are removed. On exit, the CTA completes its master bounce-out before the existing hero/text/screen exit continues; reward reveal timing still decides when the shared CTA enters. Reward width remains screen-scoped through `.cc-cta-stack--reward`, while motion stays global and identical. Focused contract coverage passes (5/5), and `qa:fast` passes (5 suites / 22 tests plus TypeScript, lint, diff hygiene, and visual contracts). Changes are live on localhost web only; native bundle and phone remain untouched.
- Tutorial Complete (`Congrats! / You cleared the stage`) no longer owns its old `65%` gray overlay plus white radial background recipe. The full-screen overlay now uses the same canonical `applyAppPaperSurfaceToElement()` paper surface as the other gameplay-occluding screens; thumb, copy, layout, CTA, and all existing animations are unchanged. Focused regression coverage passes (1/1), and `qa:fast` passes (5 suites / 20 tests plus TypeScript, lint, diff hygiene, and visual contracts). The change is live on the existing localhost web server; no native bundle or phone was changed.
- CTA-system migration phase 1 is local, web-first, and visually approved for Clean Board plus Board Fail. New `cta-system.ts` is the single DOM motion/input owner for registered CTA buttons, with primary/secondary roles, interruption-safe GSAP enter/exit/press/release, Pointer Events, double-activation protection, disabled state, and explicit disposal. `CTA_MOTION_DEFAULTS`, mutable `ctaMotion`, `configureCtaMotion()`, and `resetCtaMotion()` provide one global tuning surface for enter, exit, press, release, easing, press scale, and companion stagger. `exitCtaPair()` owns mirrored clicked-first pair exit ordering, so screens no longer carry their own stagger values. Activation finishes the complete press-release bounce before invoking the action; the clicked CTA then exits into itself first and its companion follows `70ms` later. New scoped `cta-system.css` isolates the animated inner visual from screen layout transforms and defines separate phone (`<=767px`) and iPad (`768–1024px`) width tokens while sharing identical motion. Clean Board and Board Fail legacy class collisions and parallel touch/mouse transform handlers are removed without changing their actions or surrounding modal choreography. Focused contract tests pass (3/3), and `qa:fast` passes (5 suites / 20 tests plus TypeScript, lint, diff hygiene, and visual contracts). The existing dev server at `http://localhost:5174` is serving the source for review. No native bundle or phone was changed; physical feel remains `NEEDS PHYSICAL TEST` after the full migration is approved on web.
- Checkpoint `v906` consolidates all accepted work after `v905`: the gentler session-stable independent Journey gyro profiles, correct forced `Try It` activation, per-World ten-Stage difficulty and star thresholds, transient-safe wild/preload-meter credit, and canonical New Reward paper background. Deterministic validation and the exact installed Stack to Six build are recorded below. The checkpoint is intended for `feature/v700-journey-hub`; no legacy shell or fallback device is in scope.
- Journey `New Reward / Tap the card to reveal` no longer owns a separate `65%` gray paper overlay and flat fallback. Its full-screen overlay now calls the canonical `applyAppPaperSurfaceToElement()` helper used by the other gameplay-occluding screens, so paper texture, warm gradient, opacity, positioning, and future global paper changes remain identical. A source-contract regression test prevents the local recipe from returning. The focused test passes (1/1) and `qa:fast` passes (10 suites / 101 tests plus TypeScript, lint, diff hygiene, and visual contracts). This change and the wild-meter race fix below were rebuilt with complete raw assets, synced only to the authoritative Stack to Six `Web.bundle`, passed `qa:ios`, and passed signed Xcode build plus final bundle/content verification. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `53CD42AB-2F4F-40EB-9773-56E2B39BDF53/Stack to Six.app`, and that exact bundle launched successfully. No fallback device or legacy shell was touched. Changes remain uncommitted/unpushed; physical appearance remains `NEEDS PHYSICAL TEST`.
- Fixed a wild/preload-meter race reproduced near Journey endgame: a non-final Juice/Star/magnet merge could be correctly classified from its immutable merge-entry snapshot, then `addWildProgress()` would re-check the transient live board while a backpack wild was still hidden/dropping or before merge-6 replacement tiles spawned. That second check could falsely see a last pair, reset the meter, and discard the earned merge charge. Meter credit now consumes the existing confirmed-non-final decision for regular stack merges, non-final merge-6, and validated magnet pulls; true final merges still reset/skip charge, and unrelated blockers (`busyEnding`, disabled meter, transitions) remain intact. The decision lives in `wild-meter-progress-decision.ts` with focused regression coverage. Targeted wild/final/magnet suites pass (5 suites / 32 tests), `qa:fast` passes (9 suites / 100 tests), and `qa:full` passes (80 suites / 525 tests plus TypeScript, lint, production build without native sync, bundle audits, visual contracts, and native source guard). The exact fix is included in the installed/launched Stack to Six build recorded above. Changes remain uncommitted/unpushed; physical reproduction remains `NEEDS PHYSICAL TEST`.
- Journey gameplay difficulty and completion stars now share one per-World ten-Stage balance contract. Forest `1–10`, Beach `11–20`, and Area 55 `21–30` each reset to the same arc: local Stages `1–3` are very easy (`75%` small-value assistance), `4–6` medium (`40%`), and `7–10` use the normal unassisted `1–5` pool. Journey star thresholds follow those same tiers: `2★/3★` at `2500/6500`, `3000/8000`, and `3500/9500`. Completion modal calculation, saved-high-score World stars, initial tiles, and later regular spawns consume the same modular source; Arcade and tutorial balance are unchanged. Focused balance/spawn tests pass (26/26), `qa:fast` passes (8 suites / 96 tests), and `qa:full` passes (79 suites / 521 tests plus TypeScript, lint, production build without native sync, bundle audits, visual contracts, and native source guard). The exact `dist` and complete raw assets were synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios`, signed Xcode build, final `com.taptapdesign.stacktosix.Stack-to-Six` bundle-ID/content verification, and Wi-Fi install-over passed. Apple confirmed installation URL ending `9F7B71E5-2A47-4B39-BEBD-FAA74D120B51/Stack to Six.app`, and that exact bundle launched successfully on `iPhone 13 blue`. No fallback device or legacy shell was touched. Changes remain uncommitted/unpushed; gameplay feel remains `NEEDS PHYSICAL TEST`.
- Individual Forest, Beach, and Area 55 screens retain the accepted symmetric `270ms` response and all existing per-Unit/cloud direction ownership, but their shared Journey World surface gain is reduced by 18% (`2.2 → 1.804` on both axes) so their gyro travel matches the calmer Hub style. Journey Hub, Homepage, gameplay, detail modal, Round Complete, board transition, sensor range, and maximum low-level strength are unchanged. `qa:fast` passes (6 suites / 70 tests), production build, `qa:ios`, and signed Stack to Six build pass. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `49E56809-94ED-4519-A6F4-2838C50D803A/Stack to Six.app`, and the exact bundle launched successfully. No fallback device or legacy shell was touched. This tuning remains uncommitted and unpushed; physical acceptance remains `NEEDS PHYSICAL TEST`.
- The accepted follow-up direction removes separate acceleration/return timing: every Journey scene now uses one symmetric `270ms` elapsed-time response in both directions for a single gentle follow. Each fresh Journey Hub entry samples three small stable offsets—World path rotation, World depth rotation, and cloud direction rotation—then keeps that profile frozen for the complete Hub visit. Forest/Beach/Area 55 therefore receive different path assignments on later entries while preserving their inward side, and all seven clouds receive a newly rotated set of independent directions/depths. Re-activation on the same mounted Hub reuses the profile; there is no per-frame or per-tilt RNG, new listener, timer, RAF, or cleanup owner. `qa:fast` passes (6 suites / 70 tests), production build, `qa:ios`, and signed Stack to Six build pass. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `9DE619F3-D971-423C-B538-5C8EA2C05C03/Stack to Six.app`, and the exact bundle launched successfully. No fallback device or legacy shell was touched. This refinement remains uncommitted and unpushed; physical acceptance remains `NEEDS PHYSICAL TEST`.
- Physical review accepted the fluid independent Hub model but requested gentler acceleration/deceleration. Journey-only elapsed-time smoothing is softened from `150ms/225ms` to `210ms` intentional pickup and `310ms` return. Sensor range, small-tilt response, maximum travel, three per-World matrices, inward/wander balance, seven cloud directions, listener/RAF ownership, and all non-Journey surfaces remain unchanged. `qa:fast` passes (6 suites / 69 tests), production build, `qa:ios`, and signed Stack to Six build pass. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `E294BA48-1173-4F90-A15D-26CB60F49599/Stack to Six.app`, and the exact bundle launched successfully. No fallback device or legacy shell was touched. This feel refinement remains uncommitted and unpushed; physical acceptance remains `NEEDS PHYSICAL TEST`.
- Physical review of the first truly independent Hub build confirmed separation but found its motion jerky and unnatural. Journey smoothing is now frame-rate independent: the single existing RAF uses elapsed-time exponential response (`150ms` intentional pickup, `225ms` return) rather than a fixed `0.145/0.09` per-frame fraction, so 60/120Hz and isolated iOS frame variation produce the same feel. Hub inward motion removes the noisy radial cusp with a continuous soft-centre magnitude and rebalances from `72/28` to `62%` inward pull / `38%` directional per-World wander, preserving individual trajectories and centre bias while reducing rectified in/out pumping. No listener, RAF owner, timer, or target count was added. `qa:fast` passes (6 suites / 69 tests), production build, `qa:ios`, and signed Stack to Six build pass. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `434226DE-5376-49A8-BD6F-A2F4E5AFE3A9/Stack to Six.app`, and the exact bundle launched successfully. No fallback device or legacy shell was touched. This refinement remains uncommitted and unpushed; physical motion feel remains `NEEDS PHYSICAL TEST`.
- `Try It` on a Developer-forced Tilt Motion intro previously failed whenever the saved 3D toggle was OFF: the modal bypassed the visibility policy correctly, but its callback called the controller while `isEnabled()` was still false, so permission returned immediately and the modal nevertheless resolved as `enabled`. The launch CTA now temporarily enables the runtime setting before the synchronous gesture-owned Apple permission request, persists ON only after a granted result, and restores/persists the previous state on denial or failure. The modal now resolves failed requests as `cancelled`, never `enabled`. `qa:fast` passes (6 suites / 68 tests), production build, `qa:ios`, signed Stack to Six build, exact bundle ID, and matching entrypoints pass. After restarting only Mac `CoreDeviceService`, Wi-Fi install-over finally succeeded on `iPhone 13 blue` at installation URL ending `40DE8A0E-9328-4D14-BEEA-C42291EF092D/Stack to Six.app`, and the exact bundle launched successfully. This installed build includes the Try It correction, three independent Hub World pitch/roll matrices with inward bias, and seven independent Hub cloud directions. No fallback device or legacy shell was touched. The changes remain uncommitted and unpushed; physical motion feel remains `NEEDS PHYSICAL TEST`.
- The first inward-Hub refinement still felt grouped because all three Worlds consumed the same mixed Hub tilt before only depth/polarity differences were applied. Each Hub World now owns a distinct pitch/roll mixing matrix: Forest, Beach, and Area 55 derive different X/Y trajectories from the same physical gesture. Horizontal travel is composed from a `72%` magnitude-driven inward pull plus `28%` per-World directional wander, so Forest/Area 55 independently move right and Beach independently moves left toward the screen centre as tilt grows; releasing the phone returns each along its own path. This supersedes the preceding symmetric `42%` outward attenuation. Cloud independence remains unchanged. Focused tests pass (31/31), `qa:fast` passes (4 suites / 47 tests), production build, `qa:ios`, and signed Stack to Six build pass. The latest install-over attempt was again interrupted by Apple's `IXRemoteErrorDomain code 6`, so this exact correction is not installed, committed, or pushed. No fallback device or legacy shell was touched.
- Physical review of the first sensitive-centre prototype accepted the stronger small-tilt response but rejected the Hub's rigid two-plane motion. Hub Worlds now retain separate session-stable depth/vertical directions while their horizontal polarity points Forest and Area 55 rightward and Beach leftward toward the composition centre; reverse/outward travel remains visible at `42%` amplitude so the islands breathe independently without flying apart. Every Hub cloud now receives its own session-stable two-axis direction plus existing distinct depth, producing seven independent cloud paths without per-frame randomness, new listeners, timers, or RAF owners. Focused tests pass (30/30), `qa:fast` passes (4 suites / 46 tests), production build and `qa:ios` pass, and the signed app is verified as `com.taptapdesign.stacktosix.Stack-to-Six` with matching entrypoints. Two install-over attempts on the available `iPhone 13 blue`, including one after restarting only Mac `CoreDeviceService`, were interrupted by Apple's `IXRemoteErrorDomain code 6`; therefore this refinement is not installed, committed, or pushed. No fallback device or legacy shell was touched.
- Latest local-only Journey gyro tuning replaces the weak linear centre response with a Journey-scene-only ease-out curve: relaxed wrist motion is amplified most near centre while the existing maximum remains unchanged. The normalized sensor dead-zone is reduced from `0.055` to `0.035`, Journey Hub/World scenes pick up outward motion at `0.145` smoothing and settle toward centre at `0.09`, while Homepage, gameplay, modal, Round Complete, and transition surfaces retain their prior response. Hub Worlds now share one movement polarity instead of session-random opposing directions, preserving modest depth variation while preventing the three islands from visually pulling apart; Hub clouds keep their authored counter-depth. Focused tests pass (29/29), `qa:fast` passes (4 suites / 45 tests), the production build and `qa:ios` pass, and the signed app is verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 723 runtime asset files and a matching entrypoint. Install-over succeeded on `iPhone 13 blue` at installation URL ending `B50D0DF4-7DE1-474D-90E4-F02EBD2F1C6D/Stack to Six.app`, and the exact bundle launched successfully. No fallback device or legacy shell was touched. This follow-up is not yet committed or pushed; physical motion feel remains `NEEDS PHYSICAL TEST`.
- Checkpoint `v905` consolidates all accepted work after `v904`: the centered `Tilt Motion` launch modal and seven-day Later policy, shared gyro presentation on Round Complete, Journey banner art/count/handle-pivoted enter-idle-exit motion, Forest cloud composition, and the final Hub progression contract. Every World remains fully authored and previewable, but only the World containing the single interim card shows a progress banner; future Worlds expose locked regular Units without creating interim progression. The complete dirty-tree build passed deterministic QA, was synced only to the authoritative Stack to Six shell, installed over the existing app on `iPhone 13 blue`, and launched successfully. No legacy shell or other device was touched.
- Every Journey Hub World is now navigable for preview regardless of progression: future/inactive World buttons are no longer disabled and the tap handler no longer returns on the internal `locked` state. Opening such a World only renders its existing locked regular Units; it does not create or move interim progression. The existing global single-interim invariant remains authoritative, so only the current progression World has an interim Unit and therefore only that World has a banner. Debounce, drag rejection, Hub exit, World enter, and gameplay card locking remain unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Inactive/locked Journey Hub Worlds and their associated clouds now have no visual treatment at all: full authored opacity, `filter: none`, no desaturation, haze, ghost, overlay, or alternate count color. The only progression distinction is banner presence: a World with an interim card receives the animated banner; every World without an interim card hides it. World interaction locking and standard lifecycle remain unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- The entire rejected locked-World haze/dream-ghost experiment is removed: no duplicate image, pseudo-surface, blend mode, extra keyframes, or related compositor work remains. All World islands and clouds remain visible using their prior full-opacity/desaturation state. Banner visibility now follows product progression directly: each World receives `has-interim-card` only when one of its boards is interim, and CSS hides the banner for every World without that class. Banner enter/idle/exit behavior is unchanged for the single currently progressing World. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- The rejected brown haze pseudo-surface is fully removed after visual review showed its rectangular/oval mask boundary as a brown-black frame over Beach. Locked Worlds retain only the transparent-PNG dream-ghost duplicate, whose effect follows the exact island silhouette and cannot expose a container edge. No replacement clip, mask, gradient, or backdrop/filter layer was added; clouds, desaturation, active Worlds, and lifecycle remain unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Locked-World haze changes from white/cream screen blending to a warm brown Journey treatment built from `#AD8775` and `#CC7D45` gradient stops with `soft-light` blending. Haze amplitude, drift, ghost layer, desaturation, lifecycle, and performance profile are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Locked-World sleeping haze and dream-ghost opacity amplitudes are increased by exactly 80% from the preceding accepted-visible prototype. Haze keyframes now range `0.324–0.9`, and ghost keyframes `0.072–0.54`; drift distance, scale, duration, phase, transform-only motion, lifecycle, and reduced-motion behavior remain unchanged so the stronger look adds no extra animation work. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Because the light-only locked haze remained visually imperceptible against the World art, locked Worlds now also receive one duplicate of their already-decoded World image as a lightweight “dream ghost” layer. The ghost drifts only `2–3px`, scales up to `1.018`, and pulses between very low opacity values to create unmistakable soften/refocus double-vision without animating blur. It shares the haze timing/Area-55 phase, uses only transform/opacity, is removed by normal Hub DOM teardown, and is disabled under reduced motion. Active Worlds do not create the extra image layer. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Locked-World haze visibility fix moves the pseudo-surface from the outer disabled button stacking context directly onto `.journey-v700-world-visual::after`, above the World PNG at `z-index: 4`. Its warm-white gradient and opacity range are strengthened (`0.18 → 0.50`) so the soften/clarify cycle is unmistakable on web, while remaining transform/opacity-only. Clouds stay outside the veil and fully visible; active Worlds remain untouched. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Web-only inactive-World visual prototype adds a performance-safe “sleeping world” haze while keeping locked Worlds and their clouds fully visible under the existing desaturation. One clipped, screen-blended pseudo-surface per locked World slowly changes opacity and drifts/scales asymmetrically, creating a soften/clarify illusion without animating `filter: blur()` or using backdrop filters. Area 55 uses a different duration/phase to avoid mechanical synchronization. The effect is transform/opacity-only, pointer-inert, has no DOM/timer/listener owner, and active Worlds remain unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Locked/inactive Journey Hub Worlds no longer display their banner at all. The existing banner DOM remains structurally available for a future unlock-state refresh, but `display: none` prevents paint, reveal, idle, shimmer, and compositor work while locked. Active World banners and all World lifecycle behavior are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Locked Journey Hub banner counts now use raw `#CC7D45`; the existing locked-World `saturate(0.45)` filter then applies to that number together with the World composition. Active counts remain collectibles orange, and opacity/layout/motion/lifecycle are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Journey Hub banner idle is softened from the rejected high-frequency ±6deg jolts to a slow asymmetric drift: vertical travel stays within roughly `-2.1px/+1.9px`, angular offsets within `+2.5deg/-2.1deg`, and only six irregular turning points occur per cycle. Forest, Beach, and Area 55 use distinct `7.6s`, `8.35s`, and `7.9s` durations, so their phases naturally diverge over time without JS randomness. Reveal/retract, authored base angles, shimmer, lifecycle ownership, and reduced-motion handling are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Banner reveal no longer waits for Hub World/cloud enter completion. A guarded `startBannerEnter` callback is attached to the existing tracked GSAP enter targets and fires on the first actual enter tick, after the tucked-behind state has painted. Directional banner slide therefore runs concurrently with the canonical World enter; exit retract already runs concurrently with canonical World exit. Banner idle still begins after its local reveal delay, and no timeline duration, Promise, timer, RAF, listener, or additional GSAP tween was introduced. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Journey Hub banner reveal replaces the rejected multi-direction spring keyframes with a gentler `720ms` directional slide using one very small soft overshoot and no jerk/back/settle chain. Local idle remains paused until reveal completes. Hub exit now adds a dedicated retracting state and removes presentation at standard exit start: banners glide behind their corresponding World PNG over `440ms` in parallel with the unchanged World exit, while their live idle angle is paused instead of snapping to base. No exit wait, Promise extension, timer, RAF, listener, or GSAP owner was added. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Latest banner composition moves Forest 24px upward from its preceding placement and changes its authored base rotation to `-15deg`; Beach changes to `+8deg`; Area 55 moves 16px upward and changes to `-6deg`. Existing handle pivots, directional post-enter reveal, bounce, idle offsets, shimmer, count alignment, and lifecycle ownership remain unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Journey Hub banners now remain physically tucked behind their World PNGs throughout the complete canonical World/cloud enter. Only after every tracked enter target finishes does the coordinator add one local presentation class: Forest slides/bounces right, Beach mirrors left, and Area 55 follows right with `110ms` per-World flourish staggering. A transform-independent CSS `translate` reveal uses overshoot/back/settle over `820ms`; the existing handle-pivoted idle starts only after each banner settles. The presentation class remains through standard exit so banners never snap behind the World on tap. No Promise duration, GSAP World ownership, timers, RAFs, or listeners were added; reduced motion resolves banners directly to their final position. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Latest banner-count alignment moves Forest/Area 55 another 1px right and 1px up, then makes Beach a true mirrored counterpart: both sides use `top: calc(57% + 5px)`, with right-extending banners at `right: 3px` and mirrored Beach at `left: 3px`. No other banner presentation or lifecycle behavior changed. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Latest web-only count alignment moves only the Forest/Area 55 right-extending banner number 2px right and 2px up from its preceding position (`right: 4px`, `top: calc(57% + 6px)`). Beach and all banner animation/color/lifecycle behavior are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Locked Journey Hub Worlds and their associated clouds now retain full authored opacity and differ only through the existing `saturate(0.45)` treatment, eliminating the dim/inactive fade. Active banner counts stay collectibles orange; locked counts use the established collectibles brown. Count alignment is mirrored by attachment side: right-extending banners move the number 6px left and 8px down, while the left-extending Beach banner moves it 6px right and 8px up. Enter/exit ownership and banner motion are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Journey World banner idle now stays alive throughout its `6.8s` cycle with continuous gentle vertical drift and handle-pivoted sway, plus two larger cartoony breeze/bounce moments that settle through alternating follow-through arcs. Per-World negative phase offsets preserve a pseudo-random ensemble without JS. The flag shimmer now uses the interim-card-style broad masked white reflection at two restrained moments per cycle. Motion remains transform-only CSS, clipped to the flag, and disabled under reduced motion. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Journey World banners replace the stiff multi-jolt idle with a `6.4s` handle-pivoted hanging-sign motion: two restrained breeze impulses each decay through alternating, progressively smaller pendulum arcs back to the authored base rotation. Vertical travel stays sub-pixel, the largest angular offsets are `+6.5deg` and `-5.4deg`, and shimmer/ember timing follows the same cycle. The implementation remains transform-only CSS with negative per-World phase offsets, automatic DOM cleanup, and reduced-motion disablement. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Journey World banner idle rotation now pivots from the physical handle attachment instead of the image center: Forest and Area 55 use the far-left top anchor, while mirrored Beach uses the far-right top anchor. Existing position, rotation range, timing, shimmer, lifecycle ownership, and reduced-motion handling are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Forest Unit 3 now uses a dedicated two-cloud lower composition: one medium cloud and one large cloud sit beneath the island at the bottom of the Unit. They retain the existing `board-3` lifecycle, shared enter/exit, idle, and gyro ownership; no other Unit layout or animation changed. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the web-only production build passes with native sync explicitly skipped.
- Latest web-only banner placement supersedes the placement values in the next banner-history entry: Forest is moved 16px farther right and 10px lower, Beach 16px farther left and 20px higher, and Area 55 20px farther right and 10px higher. Banner animation, shimmer, progress counting, World ownership, and native bundles are unchanged. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and `SKIP_NATIVE_BUNDLE_SYNC=true npm run build` passes; no native sync, Xcode build, install, or phone change was performed.
- Web-only Journey Worlds presentation replaces the detached white progress pill with the user's new `assets/journey assets/natpis.png` / `natpis@2x.png` banner behind each World. Progress counts only truly unlocked regular cards (`board.unlocked && !board.interim`), so two open cards plus the next interim reads `2/10`. Forest and Area 55 sit left with banners extending right and base rotations `-4deg` / `-5deg`; Beach sits right with only the PNG mirrored, extends left, and uses `+5deg`, while labels remain readable. The latest placement moves Forest another 14px right, Beach 14px left, and Area 55 18px right. The rejected radial burn remains removed. The cropped flag surface uses two low-opacity warm-edge/cream-shine moments per `4.8s` cycle; synchronized local motion moves up/down by at most 3px and offsets rotation in both directions up to exactly ±8deg. Different negative delays prevent Worlds firing in unison. This is compositor-only CSS on one existing banner element and two pseudo-elements: there are no GSAP tweens, pools, timers, RAFs, or listeners to retain/kill, and Hub DOM removal ends all work automatically. `prefers-reduced-motion` disables banner motion and both FX layers. `.journey-v700-world-visual` still owns World PNG and banner under the existing World idle/single gyro target, while the outer card solely owns standard enter/exit; tap geometry and locked appearance are unchanged. Focused source/ownership coverage passes. `qa:fast` passes (7 suites / 78 tests plus TypeScript, lint, diff hygiene, and visual contracts), and the latest `SKIP_NATIVE_BUNDLE_SYNC=true npm run build` passes. `http://localhost:5174/` remains the review surface; no native sync, Xcode build, install, or phone change was performed.
- Forest Stage Units 1 and 2 now have deliberately separate cloud compositions below the Forest-main silhouette. Unit 1 was reduced from four overlapping clouds in two cramped bands to three smaller side/lower clouds with more breathing room; Unit 2's explicit empty cloud table was replaced with its own three-cloud group, including the previously missing lower mass. Every cloud remains behind its island and keeps the existing `board-N` Unit ID, shared GSAP enter/exit, idle, and gyro ownership; no other Unit or World changed. A focused source contract prevents Unit 2 from returning to `2: []`. `qa:fast` passes (7 suites / 77 tests plus TypeScript, lint, diff hygiene, and visual contracts); production build, exact Stack to Six Web.bundle sync, `qa:ios`, signed Xcode build, exact bundle-ID/721-asset/entrypoint verification all pass. The first install-over failed with Apple's `IXRemoteErrorDomain 6`; the retry timed out after the iPhone control channel became invalid, and `iPhone 13 blue` then reported `unavailable`. This build is **not** claimed installed; physical composition remains `NEEDS PHYSICAL TEST` after that exact device reconnects.
- A second physical trace isolated the remaining Journey Worlds jerk to the separate Hub → Homepage path after returning from a World. On World return, native Hub scroll moved `0 → 2 → 3px` across the first two idle frames, but `playJourneyV700HubExit('back-to-home')` released the top guard without applying the viewport pin already used by Hub → World. Back-to-home now freezes the elastic owner and atomically pins current Hub geometry through the complete unchanged reverse World/nav cascade; the Promise resolves first so `hideCollectibles()` synchronously hides Journey, then a queued microtask releases the compensating transform/scroll lock before any visible repaint. Hub → World behavior and all motion timings/easings remain unchanged. `qa:fast` passes (7 suites / 76 tests plus TypeScript, lint, diff hygiene, and visual contracts); production build, exact Stack to Six bundle sync, `qa:ios`, signed Xcode build, exact bundle-ID/721-asset/entrypoint verification, and install-over pass. The build was installed only on `iPhone 13 blue` at installation URL ending `78254EF7-BEE7-4A64-8284-1503B0C7DF7B/Stack to Six.app`, and the exact bundle launched successfully. The user physically retested the complete Journey navigation sequence and confirmed that everything now works correctly, accepting both Hub → World and returned-Hub → Homepage behavior.
- Latest physical trace on the installed idle-frame build disproved both prior Hub-exit hypotheses: the custom elastic owner had identity transform/no release tween, and freezing all 16 CSS-idle children changed geometry by only `0.083–0.085px`, while the user still saw the hitch. The trace then captured the real shared movement: native `scrollTop` continued `144 → 141 → 137` over 27ms and every World card moved together `+3px` then `+4px`. Hub → World now atomically pins the Hub to its current screen geometry by compensating with `translateY(-capturedScrollTop)` while resetting/locking the native scroll owner; the compensation is released only in the same synchronous task that replaces Hub DOM with World DOM, with cancel/finally cleanup. The disproven idle-frame freeze was removed. A related Hub opacity conflict was also fixed: locked Worlds previously entered to GSAP opacity `1`, then snapped to CSS opacity `0.48` when cleanup cleared the inline value. Each World now enters directly to its computed base opacity, so locked filter/opacity remains constant and exit fades only once. `qa:fast` passes (7 suites / 75 tests plus TypeScript, lint, diff hygiene, and visual contracts); production build, exact Stack to Six sync, `qa:ios`, signed Xcode build, and install-over all pass. The combined build was installed only on `iPhone 13 blue` at installation URL ending `8799B3D3-BD8E-4D79-94AE-8D9B59DB3215/Stack to Six.app`, and that exact bundle ID launched successfully. Hub pin and locked-opacity behavior remain `NEEDS PHYSICAL TEST` on this build.
- Latest local Journey follow-up replaces the unsuccessful Hub scroll-lock hypothesis with the actual competing owner found in source: the iOS elastic overscroll timeline animates the complete `#journey-boards-container`, and an accepted World tap could leave its touchend/release owner alive while the standard per-World Hub exit began. Hub → World now removes that exact elastic listener/timer/tween at the accepted tap while preserving its current visual transform until the outgoing Hub is replaced; standard World/card/nav exit values and every individual World screen remain unchanged. The same follow-up fixes stale earned-star art after a successful Journey board: board stats were saved while Journey was hidden, but the optimized return preserved the already-mounted World DOM, so it retained empty star PNGs. High-score and board-completion paths now refresh only the mounted left/center/right star sources/classes for that Unit from `boardStatsService`, without rerendering or disturbing the World lifecycle. `qa:full` passes (78 suites / 490 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard). The exact 721-asset output is synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios`, signed Xcode build, exact bundle ID, source markers, and matching `dist`/`Web.bundle`/final-app entrypoints pass. Install-over then succeeded only on `iPhone 13 blue` at installation URL ending `99E0DC4F-743F-4E14-937F-C622B98A3A06/Stack to Six.app`, and that exact bundle ID launched successfully. Both fixes remain `NEEDS PHYSICAL TEST` until the user accepts the Hub → Robo exit and completed-board star return on this build.
- Superseded Hub-exit investigation: a live trace proved the entry scroll-top guard released normally, but the first attempted outgoing viewport lock was never confirmed installed and the user continued to reproduce the jump. That lock has now been removed in favor of the elastic-owner handoff described above. Its two install-over attempts had stalled without Apple's final `App installed` confirmation, so neither attempt is treated as a device install.
- Latest local-only Arcade gyro follow-up adds the existing single `appSpatialMotion` controller to the complete `Round Complete → next Round` overlay. The completed-Round headline, subtitle, and thumbs-up wrapper receive restrained independent depths; the next-Round label and each number wrapper receive their own slightly deeper stable directions. Ownership uses CSS `translate`, so it does not compete with the existing GSAP letter/digit scale, rotation, or opacity animations, and modal cleanup deactivates and clears every target before gameplay resumes. No additional orientation listener, RAF, or parallel motion source was introduced. Focused spatial/launch/modal tests pass (47/47), and `qa:fast` passes (6 suites / 63 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). This gyro addition has not been built, synced, installed, committed, or pushed.
- Latest local-only copy refinement changes the centered motion modal to `Tilt Motion`, `Tilt your phone to add a little motion.`, primary CTA `Try It`, and unchanged secondary CTA `Later`. It deliberately avoids promising a dramatic visual effect while keeping the action short and explicit. Layout, launch ownership, bounce timing, permission request, cooldown, and Developer override are unchanged. Focused modal/launch tests pass (19/19), TypeScript passes, and diff hygiene passes. This copy refinement has not been built, synced, installed, committed, or pushed; the phone still has the immediately preceding centered-modal build.
- Latest local-only 3D Motion presentation follow-up restores the pre-v904 launch ownership and centered modal geometry without reverting the seven-day dismissal policy. TapTap character and logo complete their standard exits, the still-occluded launch lifecycle presents and awaits the centered paper modal, its `0.78s` back-out bounce-in and `0.65s` comic back-in bounce-out complete, and only then can launch removal and Homepage enter begin. Bottom-sheet alignment, drag handle, whole-sheet swipe, bottom-sheet landing keyframe, and Homepage-frozen presentation path were removed. `Later` still writes the seven-day cooldown and Developer `Show/Reset 3D Intro` still bypasses it once without mutating the real timestamp. Focused tests pass (19/19), `qa:fast` passes (3 suites / 22 tests), and `qa:full` passes (78 suites / 487 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard). The complete 721-asset output was synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios`, signed Xcode build, exact bundle ID, centered-modal/cooldown markers, and matching entrypoints passed. The first install-over left Apple LaunchServices with a sequence mismatch; after resetting only `CoreDeviceService`, reinstall-over succeeded on `iPhone 13 blue` at installation URL ending `36B6FEB9-4E16-4853-BC3E-4C7B095003AF/Stack to Six.app`, and that exact bundle launched successfully. No app data, legacy shell, or other device was touched. This follow-up remains uncommitted and unpushed; online checkpoint remains `v904`. Physical modal placement/feel remains `NEEDS PHYSICAL TEST`.
- Checkpoint `v904` consolidates the verified 3D Motion startup bottom-sheet lifecycle, whole-sheet Later swipe, seven-day dismissal cooldown with non-mutating Developer override, and the physically accepted removal of the redundant Journey viewport exit before the Area 55 detail modal.
- A physical `iPhone 13 blue` trace of Area 55 regular Stage → detail modal measured a roughly `1.14s` interval from coordinated World exit start (`64618ms`) to detail-modal spatial activation (`65758ms`). Source inspection proved this was not image or modal preparation: after the canonical clicked-card/Unit/World/nav exit completed, `startBoardAreaThenJourneyExit()` replayed the generic Journey viewport target cascade over the same already-exited DOM and waited for its empty visual tail. The shared Forest/Beach/Area 55 handoff now preserves the standard card/Unit/World/nav timings unchanged, then directly commits the hidden Journey viewport state; the legacy viewport cascade remains only as the non-coordinated fallback. A focused source contract was added. Focused QA passes (16/16), and `qa:full` passes (78 suites / 489 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard). The complete 721-asset output was synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios`, signed Xcode build, exact bundle ID, asset count, and matching `dist`/`Web.bundle`/final-app entrypoints passed. The build was installed over the existing app only on `iPhone 13 blue`, its installed bundle record was verified, and `com.taptapdesign.stacktosix.Stack-to-Six` launched successfully. Apple did not return an installation URL for this transfer. No app data, legacy shell, or other device was touched. The user physically retested the same Area 55 detail handoff on the new build and confirmed it is now smooth; the follow-up console attachment expired before yielding a numerical after-trace, so the UX acceptance is physical while the measured before-trace remains the diagnostic evidence.
- The `3D Motion` intro dismissal policy now treats both `Later` and a successful swipe-down as a seven-day cooldown persisted across app relaunches. After seven full days the intro is eligible to appear again if the native motion permission is still relevant. Developer Settings `Show/Reset 3D Intro` remains a one-shot test override: it bypasses an active cooldown for the next launch but dismissing that forced preview does not create, extend, clear, or otherwise modify the user's real cooldown timestamp. Focused modal/launch tests pass (21/21), TypeScript passes, and diff hygiene passes. This cooldown policy is included in the latest installed `iPhone 13 blue` build described immediately above.
- On 2026-08-01 the complete current 3D Motion bottom-sheet/whole-sheet swipe build passed `qa:full` (77 suites / 487 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard). The full 721-file raw asset tree was restored and synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios` passed, and the signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with identical `dist`/`Web.bundle`/final-app entrypoints. Two Wi-Fi install-over transfers were interrupted by Apple's `IXRemoteErrorDomain code 6`; after restarting the wedged Mac `CoreDeviceService`, the third transfer succeeded only on `iPhone 13 blue` at installation URL ending `CA7E635F-BF39-4C44-9B81-75EE6BE72209/Stack to Six.app`, and that exact bundle launched successfully. No app data was removed, and no legacy shell or other device was touched. Physical paper crop, whole-sheet drag, CTA exclusion, and drag-to-Later continuation remain `NEEDS PHYSICAL TEST`.
- The startup `3D Motion` explainer is bottom-anchored like the End Game sheet with safe-area lower padding, non-overshooting `110% ↔ 0` travel, and `650ms` unlock ownership. The blur/veil is removed; only the End Game-style ambient shadow remains. Its single `inset:0` paper owner overscans the transparent-edged PNG to `calc(100% + 48px) × calc(100% + 96px)` at `center -32px`, so opaque texture reaches every sheet edge; inner paint is disabled and content alone bounces. Downward drag belongs to the complete non-interactive sheet area while CTA/buttons/links/form controls remain excluded. The release decision recomputes total travel from `pointerup.clientY - dragStartY`, covering WebKit's omitted/coalesced final move event; dismissal is easier at `44px`, or `20px` with `0.25px/ms` velocity. `pointercancel` is a separate safe snap-back. Accepted pulls retain their live transform and continue directly into the accelerating `110%` `Later` exit without a reset. Focused tests pass (21/21); complete validation/build/install state is recorded immediately above. Physical launch/sheet feel remains `NEEDS PHYSICAL TEST`.
- Checkpoint `v903` consolidates the current post-v902 dirty tree and the physically traced Area 55 fixes. Area 55 now follows the Forest/Beach sibling-Unit lifecycle with alien beam as a normal Unit target and nested idle-opacity visual. A live `iPhone 13 blue` modal-return trace then proved that Journey cloud idle could retain its last GSAP `x` through exit and snap to zero on the first resumed idle frame; the shared coordinator now clears that stale horizontal drift while the return screen is still hidden, preserving all existing enter/exit timing, easing, layout, and gyro behavior. Focused tests pass (14/14), `qa:fast` passes (9 suites / 74 tests), and `qa:full` passes (77 suites / 484 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard). Exact Stack to Six sync and `qa:ios` passed; the signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 721 assets and matching entrypoints, then installed over the existing app only on `iPhone 13 blue` at installation URL ending `5E806121-5577-4DBC-A00C-990C1C31C55E/Stack to Six.app`. Physical confirmation of the cloud-return feel remains `NEEDS PHYSICAL TEST` after this installed fix.
- The Area 55 sibling-Unit correction was built from the complete current dirty tree, restored to 721 raw runtime assets, and synced only to `/Users/user/Stack to Six/Stack to Six/Web.bundle`. `qa:ios` passed; the signed Debug app at `/tmp/stack-to-six-derived/Build/Products/Debug-iphoneos/Stack to Six.app` was verified as `com.taptapdesign.stacktosix.Stack-to-Six`, with identical `dist`/`Web.bundle`/final-app entrypoints and the new nested beam marker. After resetting a wedged Apple `CoreDeviceService`, install-over succeeded only on the paired `iPhone 13 blue` at installation URL ending `74047FC4-69D6-477D-8762-76105E9B4FBC/Stack to Six.app`, and that exact bundle launched successfully. No legacy shell or other device was touched. Physical Area 55 enter, clicked-Unit exit/modal handoff, and modal/game return remain `NEEDS PHYSICAL TEST` until the user accepts them.
- Latest web-only Area 55 correction removes the rejected Area-55-specific viewport-sized board wrapper and nested crater/beam lifecycle owner. Area 55 now uses the same renderer contract as Forest/Beach: clouds, island, crater, alien beam, stars, and card are sibling targets sharing the same `data-journey-area-id`, and the unchanged shared Journey coordinator owns their Unit enter/exit, stagger, interruption, and completion. The beam wrapper participates in the normal GSAP lifecycle while only its nested image owns the idle opacity pulse, preventing CSS/GSAP competition without adding a separate timeline or modal wait. Forest/Beach renderer and motion values were not changed. Focused Area 55/spatial/transition tests pass (37/37), `qa:fast` passes (9 suites / 73 tests), and `SKIP_NATIVE_BUNDLE_SYNC=true npm run qa:full` passes (77 suites / 483 tests plus TypeScript, lint, audits, visual contracts, production web build, bundle audit, and native guard). `dist` was rebuilt for web testing; no native bundle, Xcode project, app, or phone was changed.
- The rejected Area 55 full-screen board owner has been removed because scaling a viewport-sized wrapper changed the cjelina motion relative to Forest. Current source preserves the canonical Forest composition and coordinator: clouds, island, stars, and card remain the normal synchronized Unit pieces, while only each crater plus its alien beam share `.journey-robo-crater-beam-unit` so WebKit cannot paint the beam before its crater. A second regression was found and fixed: generic Journey preparation and modal-return code overwrote that full-map owner's calculated crater-center transform origin with `50% 50%`, making crater/beam fly vertically instead of scaling into themselves like the Forest stump. The owner now stores one persistent `data-journey-motion-transform-origin`, and every world-enter, auto-prime, active-Unit enter/exit, idle handoff, and 3D path preserves/resolves it. 3D Motion also resolves crater and beam to that single owner and deduplicates it, preventing gyro separation. Focused motion/ownership tests pass (40/40), `qa:fast` passes (9 suites / 73 tests), and `qa:full` passes (77 suites / 483 tests plus TypeScript, lint, audits, visual contracts, production build, bundle audit, and native guard). The exact output was synced only to the authoritative Stack to Six shell, `qa:ios` passed, and the signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 721 assets and matching `dist`/`Web.bundle`/final-app entrypoints. Wi-Fi install-over succeeded on `iPhone 13 blue` at installation URL ending `A836B5F6-8784-4938-87CF-D8999BEF4D33/Stack to Six.app`, and that exact bundle launched. Physical Area 55 initial enter, selected Stage exit, and detail-modal return remain `NEEDS PHYSICAL TEST` until visually accepted.
- Area 55 alien beams now obey the complete Journey Unit lifecycle on detail-card return. The outer `.journey-robo-alien-beam-art` wrapper carries `data-journey-area-id`, participates in the same GSAP enter/exit target array as island/crater/stars/card, and exclusively owns lifecycle opacity/transform; the nested `.journey-robo-alien-beam-visual` owns only the CSS idle opacity pulse. This removes the WebKit one-frame ownership collision that could reveal beams before the rest of their Unit. New focused ownership tests pass (2/2), and `npm run qa:fast` passes (9 suites / 72 tests plus TypeScript, changed-file lint, diff hygiene, and visual contracts). The production web build, exact Stack to Six sync, `qa:ios`, signed Xcode build, final bundle ID/marker/721-asset verification all passed. After two interrupted Wi-Fi transfers, the third install-over succeeded on `iPhone 13 blue`; Apple confirmed installation URL ending `AF4772BC-CC64-43F1-8125-1293DBAE38D3/Stack to Six.app`, and that exact bundle launched. Physical Area 55 detail-card return confirmation remains `NEEDS PHYSICAL TEST`.
- On 2026-08-01 the complete current dirty-tree Stack to Six build, including the post-studio 3D modal sequence, standardized 650ms modal pop-out, and latest contained tilt/star sizing, passed `qa:full` (76 suites / 480 tests plus TypeScript, lint, audits, visual contracts, production build, and native guard). The full 721-file raw asset tree was restored, exact output synced only to the authoritative Stack to Six `Web.bundle`, and `qa:ios` passed. The signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six`, with matching `dist`/`Web.bundle`/final-app entrypoints and bundled mode, then installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `4DC3C1C2-D4F4-4839-A85F-D951346FA7D0/Stack to Six.app`; that exact bundle launched successfully. No legacy shell or other device was touched. Physical sequence/animation feel remains `NEEDS PHYSICAL TEST`.
- Launch sequencing now enforces `studio intro/preload → complete character+TapTap exit → 3D Motion modal enter/choice/complete 650ms comic exit → launch removal → Homepage slider enter`. Modal assets warm in parallel with the studio exit, but the modal DOM cannot be presented until both studio exit promises resolve and the studio container is hidden. The launch/boot occlusion and `isActive` owner remain held through the modal's complete exit, so Homepage cannot flash or start underneath it. Cancellation/dispose still aborts and cleans both launch and modal ownership. Focused launch/modal tests pass (18/18), the source-order contract locks every boundary, and `npm run qa:fast` passes (8 suites / 70 tests plus TypeScript, lint, diff hygiene, and visual contracts). Included in the verified 2026-08-01 `iPhone 13 blue` install above.
- The 3D Motion exit replaces the former partial `scale:.68 / 380ms` close with the exact game-wide V79 comic pop-out contract used by `.animate-exit`: `scale:1→0`, `0.65s`, and `cubic-bezier(0.68,-0.6,0.32,1.6)`, with a small downward tail. The transparent overlay remains visually present for the complete card transform instead of fading it out after 220ms, opacity drops only at the end, and the launch Promise now holds ownership for the full 650ms before cleanup/Homepage handoff. Both `Let’s Move` (after the native permission result) and `Later` use the same single `finishActiveModal()` exit owner; cancellation still removes immediately. Focused launch/modal tests pass (18/18), including a 649ms not-yet-settled assertion, and `npm run qa:fast` passes (8 suites / 70 tests plus TypeScript, lint, diff hygiene, and visual contracts). Included in the verified 2026-08-01 `iPhone 13 blue` install above.
- The latest 3D Motion sizing scales the contained `tilt1/tilt2` artwork up by exactly 20% from the immediately preceding reduced size: `154×193.9 → 184.8×232.68`, and `107.8×135.73 → 129.36×162.876` on short screens. The compact art stages and all three stars expand/reposition with it so the artwork remains centered and contained; copy/CTA spacing and modal bounce ownership are unchanged. The focused launch contract passes (10/10), and `npm run qa:fast` passes (8 suites / 70 tests plus TypeScript, lint, diff hygiene, and visual contracts). Included in the verified 2026-08-01 `iPhone 13 blue` install above.
- Latest local-only 3D Motion art containment follow-up compacts the illustration stage around the reduced tilt artwork (`220×210`, or `180×150` on short screens) and repositions all three decorative stars within that stage. The lower star now uses an explicit contained top position instead of the old bottom anchor, removing the invisible old-height gap that visually pushed the instruction copy downward. Tilt scale, modal copy margin, CTA spacing, and comic enter/exit ownership remain unchanged. The focused launch contract passes (10/10), and `npm run qa:fast` passes (8 suites / 70 tests plus TypeScript, lint, diff hygiene, and visual contracts). This source change is not built, synced, or installed on `iPhone 13 blue`; final composition remains `NEEDS PHYSICAL TEST`.
- Latest local-only 3D Motion composition follow-up reduces only the animated `tilt1/tilt2` frame by exactly 30% while keeping the established art stage, decorative stars, copy spacing, and centered comic modal enter/exit unchanged. The regular frame is now `154×193.9` centered at `left:63px`; the short-screen frame is `107.8×135.73` centered at `left:56.1px`. The focused launch contract passes (10/10), and `npm run qa:fast` passes (8 suites / 70 tests plus TypeScript, lint, diff hygiene, and visual contracts). This source change is not built, synced, or installed on `iPhone 13 blue`.
- Latest local-only 3D Motion redesign is a centered comic paper modal with at least 24px safe-area-aware gutters, a fully rounded 40px paper surface, bounded short-screen scrolling, and the retained exact End Game soft ambient gradient/glow behind it while the viewport overlay stays transparent. The complete card is the sole transform owner: enter uses `0.78s` back-out from `y:88px / scale:.72 / rotate:-2deg`; exit uses `0.38s` back-in toward `y:42px / scale:.68 / rotate:2deg`. The launch Promise now waits 400ms so Homepage handoff cannot cut off the visible bounce-out. Existing art/copy/CTA spacing, two-frame first-paint guard, permission/dismissal/focus cleanup, and desktop developer preview remain unchanged. Focused modal/launch tests pass (18/18), and `npm run qa:fast` passes (8 suites, 70 tests, TypeScript, lint, diff hygiene, and visual contracts). This change is not built, synced, or installed on `iPhone 13 blue`; physical animation feel remains to be checked.
- Latest localhost launch repair fixes the concrete console-proven reason the TapTap/character intro disappeared: `launch-screen.ts` referenced nonexistent `priorityPaperLoadPromise` while building the `[CC_STARTUP_BG]` diagnostic, threw before the studio container became visible, and the inline catch immediately called `dispose('inline-start-error')`. The diagnostic now references its real owner `priorityPaperBgLoadPromise`. A regression test forbids the stale identifier and requires the correct one. Focused launch/permission tests pass (16), `qa:fast` passes (8 suites / 68 tests plus type-check, changed-file lint, and visual contracts), and localhost 5174 was read back to confirm it serves only the corrected identifier. This repair and the centered/original-size 3D Motion composition are not built, synced, or installed; `iPhone 13 blue` still has installation URL ending `725F858B-C070-4E25-93BA-0090B85A6D1F/Stack to Six.app`. The corrected web intro requires a fresh reload because the failed page already disposed its launch DOM.
- Latest local-only 3D Motion composition tuning keeps the installed full-screen paper surface and bottom-sheet lifecycle unchanged, but centers the complete title/art/copy/actions group vertically and horizontally. The tilt artwork is restored from the reduced `176×221.6` frame to its original `220×277` size inside the original `280×285` art stage, with explicit `24px` title-to-art and `8px` art-to-copy spacing; the three decorative wild stars return to their matching original positions. Focused permission/launch tests pass (15) and `qa:fast` passes (8 suites / 67 tests plus type-check, changed-file lint, and visual contracts). This centering/scale follow-up is not built, synced, or installed; `iPhone 13 blue` still has the preceding bottom-aligned full-screen version at installation URL ending `725F858B-C070-4E25-93BA-0090B85A6D1F/Stack to Six.app`. Physical centered composition remains `NEEDS PHYSICAL TEST`.
- Latest 3D Motion surface follow-up keeps the existing launch-owned bottom-sheet enter/exit lifecycle but makes its settled paper surface truly full-screen: `100vw × (100dvh + 32px)` with the existing 32px below-viewport skirt, zero overlay padding, no radius, and no polygon/torn edge. The paper texture therefore reaches every physical viewport edge while the whole surface still travels in from below. The direct title/art/copy/actions composition is one flex column with an auto top margin, so the entire group—not only the CTAs—settles against the canonical 40px-plus-safe-area visible bottom spacing; when content is too tall the auto margin collapses and the paper remains vertically scrollable. The top remains guarded by the iOS safe area plus 44px minimum breathing room (28px on short screens). Focused permission/launch tests pass (15), `qa:fast` passes (8 suites / 67 tests plus type-check, changed-file lint, and visual contracts), the complete raw asset tree was restored, exact output synced only to Stack to Six `Web.bundle`, and `qa:ios` passed. The signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 721 runtime assets and the `100dvh + 32px` marker, installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `725F858B-C070-4E25-93BA-0090B85A6D1F/Stack to Six.app`, and that exact bundle launched successfully. Physical full-screen composition remains `NEEDS PHYSICAL TEST`.
- On 2026-07-31 the full-width 3D Motion bottom-sheet follow-up was rebuilt with the complete raw asset tree, synced only to the authoritative Stack to Six `Web.bundle`, passed `qa:ios`, and built successfully in bundled mode. The signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six`, contained 721 runtime assets and the `width:min(390px,100vw)` bottom-sheet marker, then installed over the existing app on `iPhone 13 blue`. Apple confirmed installation URL ending `3FD1E777-9FE0-4B4F-A349-0D3F53D55287/Stack to Six.app`, and that exact bundle launched successfully. Physical full-width composition, lower CTA placement, and top-only paper tear remain `NEEDS PHYSICAL TEST`.
- Latest 3D bottom-sheet alignment follow-up makes the launch explainer use the same physical geometry as the app's established sheets: the overlay has zero horizontal gutter, the shell is `min(390px, 100vw)` (full iPhone 13 width), and inner spacing is exactly `28px 10% 40px-visible-bottom` after accounting for the existing 32px below-viewport skirt and safe area. This reduces the previous top/bottom padding so the title, artwork, copy, and both CTAs settle lower as one composition instead of moving only the buttons. The polygon no longer cuts either side or bottom; both sides are straight and only the top uses a moderately deeper, sharper irregular `5–16px` paper tear. Permission/launch focused tests pass (15), `qa:fast` passes (8 suites / 67 tests plus type-check, changed-file lint, and visual contracts), and `qa:full` passes (type-check, lint, tests, audits, visual contracts, production build without native sync, bundle audit, and native guard). Physical composition remains `NEEDS PHYSICAL TEST`.
- Latest 3D intro surface correction returns the explainer from a centered modal to a genuine bottom sheet without changing launch/permission ownership, CTA behavior, artwork, or the 260ms resolved-after-exit guarantee. The overlay is bottom-aligned with exact 24px side gutters; the transform-owning shell uses `40px 40px 0 0`, `104% → 0 → 102%` bottom motion, and a 32px below-viewport skirt. Its inner shared paper surface has a straight `100% 100% → 0 100%` lower edge plus enough safe-area/skirt padding that the lower edge remains physically below the screen. Only the top and full left/right edges are organically torn, with deeper irregular insets up to 20px. Focused permission/launch tests pass (15), `qa:fast` passes (8 suites / 67 tests), and `qa:full` passes (76 suites / 477 tests plus type-check, lint, audits, visual contracts, production build, bundle audit, and native guard). The full raw asset tree was restored, exact output synced only to Stack to Six `Web.bundle`, `qa:ios` passed, and the signed app verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 721 runtime assets and the new bottom-sheet markers. The first Wi-Fi transfer produced no install result and was correctly rejected; the retry succeeded on `iPhone 13 blue` at installation URL ending `75113D3F-5745-46E1-9215-F0CD4154DF05/Stack to Six.app`, and that exact bundle launched. Physical iPhone clipping/feel remains `NEEDS PHYSICAL TEST`.
- Latest launch/modal/Journey-transition polish removes the physical one-frame Stack to Six Homepage-logo flash by parsing `<body class="cc-launch-boot-active">` with synchronous CSS that occludes `#ui-root`, `#nav-root`, the game host, and direct canvases until `launch-screen.ts` performs its sole visible handoff; the native launch storyboard remains paper-only. The centered 3D Motion modal now uses 24px viewport gutters, 24px more internal bottom breathing room, and one deterministic organic four-edge paper cut inside the existing hard-clipped 40px shell. Journey detail-card/stat gyro is reduced to exactly 20% of its previous card/stat travel. Journey board transition digits, mountain, clouds, pines/fences, and other scene layers now join the existing single app spatial-motion listener/RAF at full relative strength, while `hill1/hill2` use 30%; activation is Journey-only and cleanup removes all spatial ownership before overlay reuse/removal. Focused launch/spatial tests pass (35), `qa:fast` passes (8 suites / 67 tests), and `qa:full` passes (76 suites / 477 tests plus type-check, lint, audits, visual contracts, production build, bundle audit, and native guard). The full raw assets were restored, exact output synced only to Stack to Six `Web.bundle`, `qa:ios` passed, and the signed app verified as `com.taptapdesign.stacktosix.Stack-to-Six`. The first Wi-Fi transfer produced no install result and correctly was not accepted; a verbose retry succeeded on `iPhone 13 blue` at installation URL ending `7C13D8EA-02B5-4064-AF32-FE550DCCB1B7/Stack to Six.app`, and that exact bundle launched. First-frame flash, four torn edges, weak detail gyro, and transition depth remain `NEEDS PHYSICAL TEST` until the user confirms this run.
- Latest post-physical-test gyro/clip follow-up keeps the existing single orientation listener and RAF owner but makes the requested showcase surfaces visibly more exaggerated: Homepage hero depth is `14×10 → 26×20`, Homepage CTA depth is `6×4.5 → 12×9`, and the one Journey scene master lever is `1.6 → 2.2`, raising Journey Worlds plus Forest/Beach/Area 55 together while preserving their established world/unit/cloud ratios and session-stable directions. The physical trace before retuning proved one owner with exactly 2 Homepage, 16 Hub, and 109 Forest targets; no duplicate sensor path was present. The centered 3D modal's WebKit clipping is also corrected at the paint boundary: the outer 40px-rounded shell now owns only transform and hard `overflow:hidden`, while one inner `.journey-spatial-permission-paper` owns the paper texture, scrolling, and organic top cut. This prevents the polygon clip from overriding the rounded silhouette and prevents paper paint from escaping it. Four focused suites / 40 tests, `qa:fast` (8 suites / 64 tests), and `qa:full` (76 suites / 474 tests plus type-check, lint, audits, visual contracts, and production build) pass. The complete assets were restored, exact output synced only to Stack to Six `Web.bundle`, `qa:ios` passed, and the signed app contains the new `26×20` Homepage and inner-paper markers under bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`. After two interrupted attempts, Wi-Fi install-over succeeded on the unlocked `iPhone 13 blue`; Apple confirmed installation URL ending `7D411098-6A1B-4A58-B8AD-FAD4D15BB3FD/Stack to Six.app`, and that exact bundled app launched with the live console attached. Physical exaggerated-motion feel and modal corners remain `NEEDS PHYSICAL TEST` until the current run is accepted.
- Latest 3D Motion modal polish restores `Later` as the canonical white `exit-btn bottom-sheet-cta`, enlarges the `tilt1/tilt2` animation from 60% to 80% of its original artwork size (`132×166.2 → 176×221.6`), and removes the now-dead underlined-link styling. The blurred launch backdrop now uses a gentle warm paper tint (`rgba(203, 168, 154, 0.22)`) instead of the dark brown overlay. Because `assets/modals/paper.png` has a true alpha-torn bottom but a straight top, the centered modal alone receives a scoped transparent polygon clip at its upper edge; the original alpha bottom remains untouched and no shared bottom-sheet surface changes. Focused permission/launch/clip tests pass (3 suites / 16 tests), `qa:fast` passes (8 suites / 64 tests), and `qa:full` passes (76 suites / 474 tests plus type-check, lint, audits, visual contracts, and production build). The full raw asset tree was restored, the exact output was synced only to the authoritative Stack to Six `Web.bundle`, `qa:ios` passed, and the signed app verified as `com.taptapdesign.stacktosix.Stack-to-Six` with the current CTA marker. Three Wi-Fi install-over attempts then stopped after Apple's usage assertion without a success result or JSON record. A read-only device query proves the phone still has the prior build at installation URL ending `BC2BA74F-CCAE-43DA-AC35-61E6930139FF/Stack to Six.app`; do not claim this modal polish is installed. The signed current app remains ready at `/tmp/stack-to-six-derived/Build/Products/Debug-iphoneos/Stack to Six.app`. Physical modal proportions and torn-edge appearance remain `NEEDS PHYSICAL TEST`.
- Latest local-only bottom-sheet surface correction removes the faint rectangular paper/shadow continuation visible outside rounded upper corners. End/Score, Hearts, Collectible Reward, and Resume no longer paint their paper/white texture on the transform-owning outer shell. One isolated `::after` surface now owns the panel plus the below-viewport safe-area extension, carries the exact `40px 40px 0 0` top radius, and cannot paint outside those corners; the outer shell remains overflow-visible only for bounce/drag ownership. Direct rectangular sheet shadows are removed, including the legacy black Collectible Reward shadow, while existing above-sheet ambient gradients, content, hit areas, transforms, bottom coverage, and modal lifecycle remain unchanged. A new static contract prevents any shared/Resume sheet from restoring an unclipped shell background or rectangular legacy shadow. Focused clip/permission tests pass (3 suites / 16 tests), and `qa:full` passes (76 suites / 474 tests plus type-check, lint, release audits, visual contracts, production build, bundle audit, and native source guard). The production build explicitly skipped native sync. This surface correction and the centered 3D modal redesign are not synced to `Web.bundle` or installed on `iPhone 13 blue`; physical corner/animation confirmation remains `NEEDS PHYSICAL TEST`.
- Latest local-only launch 3D Motion redesign converts the former bottom sheet into a true centered paper modal without changing permission, CTA, dismissal, or launch-handoff ownership. The overlay now centers a fully rounded 40px panel inside responsive `clamp(24px, 10.25vw, 40px)` viewport gutters; the top handle DOM/CSS is removed, the shared bottom-sheet shadow pseudo-owner and RAF entrance helper are disconnected, and the modal uses one local 720ms cartoon scale/translate enter plus the existing bounded 260ms exit lifecycle. `tilt1/tilt2` render at exactly 60% of their previous dimensions (`220×277 → 132×166.2`) with explicit `filter:none`/`box-shadow:none`; the surrounding art/stars were recomposed into a shorter 220×190 stage. Paper texture, typography, primary CTA, `Later` behavior, native gesture timing, focus cleanup, and asset preloading remain unchanged. Focused permission/launch tests pass (2 suites / 14 tests), `qa:fast` passes (7 changed suites / 62 tests), and `qa:full` passes (75 suites / 472 tests plus type-check, lint, release audits, visual contracts, production build, bundle audit, and native source guard). The production build explicitly skipped native sync. This redesign is not synced to `Web.bundle` or installed on `iPhone 13 blue`; physical centered-layout/animation confirmation remains `NEEDS PHYSICAL TEST`.
- Latest Journey Hub correction removes the small one-frame downward jerk affecting Beach and Area 55 after returning from an individual World/Unit screen. Their negative CSS idle delays could previously begin as soon as each child card received `journey-v700-idle-ready`, one style frame before the Hub applied its zero-delay seamless phase; Forest has no negative delay, which is why it appeared stable. The Hub root is now the sole visual play-state owner: it primes the seamless phase, records child readiness without starting motion, then starts all World/cloud idle animations through one root state after GSAP identity transforms are cleared. No duration, easing, position, gyro strength, World enter/exit, or gameplay code changed. A static ownership regression test locks the root selector and handoff order; focused Journey motion/ownership tests pass (2 suites / 19 tests), `qa:fast` passes (7 changed suites / 62 tests), and `qa:full` passes (75 suites / 472 tests plus type-check, lint, release audits, visual contracts, production build, bundle audit, and native source guard). The complete 721-file raw asset tree was restored, the exact current build was synced only into the authoritative Stack to Six `Web.bundle`, `qa:ios` passed, and the signed app verified as `com.taptapdesign.stacktosix.Stack-to-Six`. Apple confirmed Wi-Fi install-over on `iPhone 13 blue` at installation URL ending `81CC9C6D-CBE9-480C-A9F3-925F04AAEA6B/Stack to Six.app`, and that exact bundle launched successfully. Physical Forest → Journey Worlds return confirmation remains `NEEDS PHYSICAL TEST`.
- Latest local-only launch correction fixes the physical `Later → Homepage` path where Arcade's Play CTA could remain hidden. The cause was two ownership races: the 3D sheet resolved `Later` immediately while its 260ms visual exit still owned the viewport, and `initializeGame()` ran after the visible launch handoff, triggering `showHomepage()/forceReady()` through state subscriptions plus a redundant explicit show after the CTA enter had already started. The permission Promise now resolves only after the sheet is removed (abort/cancel still settles immediately), game state initializes once behind the launch surface before asset/handoff work, redundant state writes and the explicit Homepage show are removed, and the sole visible enter marks the Home app zone before animation. Focused launch/modal tests pass (2 suites / 14 tests), `qa:fast` passes (7 changed suites / 61 tests), and `qa:full` passes (75 suites / 471 tests plus type-check, lint, release audits, visual contracts, production build, bundle audit, and native source guard). The production build explicitly skipped native sync. This fix is not synced to `Web.bundle` or installed on `iPhone 13 blue`; physical `Later` confirmation remains `NEEDS PHYSICAL TEST`.
- Latest local-only Journey spatial tuning stays inside the single `journey-spatial-motion.ts` owner with its one orientation listener and RAF. `JOURNEY_SCENE_MOVEMENT_MASTER = 1.6` is now the only product-facing master lever for Journey Worlds plus Forest/Beach/Area 55; derived Hub ratios preserve stronger Y movement, while Homepage, gameplay cubes, preload HUD, and modals cannot be changed accidentally through this lever. All three individual Worlds share one stable per-Unit randomized two-axis direction system and the same `1.35×` relative Unit gain. Hub and individual-World clouds use a dedicated bounded `0.68–1.38` relative depth pattern plus `1.3×` separation, remaining opposite to foreground layers. Random choices are session-stable, never per-frame. Focused tests pass (24), `qa:fast` passes (7 changed suites / 59 tests), and `qa:full` passes (75 suites / 469 tests plus type-check, lint, release audits, visual contracts, production build, bundle audit, and native source guard). The production build explicitly skipped native sync. This work is not synced to `Web.bundle` or installed on `iPhone 13 blue`; physical gyro feel remains `NEEDS PHYSICAL TEST`.
- Local-only follow-up after the latest installed build extends the single app spatial owner without adding a sensor listener, RAF, ticker, tween, or timer. The gameplay wild/preload bar gyro is exactly 50% lower (`11.52×10.08 → 5.76×5.04`). Journey gameplay's `#journey-game-bottom-decor` island uses 60% horizontal and 20% vertical cube depth through independent CSS `translate`, leaving its bottom-anchored GSAP enter/exit `transform` untouched; Arcade returns no decor target. The Journey card-detail modal is now an explicit `journey-detail-modal` surface: the card uses 115% of cube depth and each of the three stat blocks uses full cube depth with different session-stable gameplay direction/depth patterns. Dividers remain stationary anchors. Modal spatial activation starts only after the visible enter frame is primed and deactivates before close/freeze/general cleanup, removing all inline translate/data ownership while preserving the accepted card and exact-reverse stat transform animations. Focused modal/spatial tests pass (2 suites / 27 tests), `qa:fast` passes (7 changed suites / 57 tests), and `qa:full` passes (75 suites / 467 tests plus type-check, lint, source/bundle audits, visual contracts, production build, and native source guard). The production build explicitly skipped native sync. This follow-up is **not** synced to `Web.bundle`, built with Xcode, or installed on `iPhone 13 blue`; the phone still has the preceding build at installation URL ending `C4FDBCC5-9183-476B-8057-E8904C76E2AB/Stack to Six.app`.
- Post-`v902` work implements three requested polish changes. Gameplay cube motion and its preload HUD now use an exact `1.8×` depth profile (`14.625×21.96` and `11.52×10.08`) without adding sensor listeners, RAF owners, or per-frame randomness. Manual Journey Hub → Forest/Beach/Area 55 navigation no longer starts the World timeline during synchronous DOM construction: all Units prime hidden during render, then one tracked RAF starts World and nav motion from a fresh visible-frame clock with no stale active-board delay. The launch 3D Motion sheet now uses the shared gameplay bottom-sheet RAF entrance, configured to `770ms` (`1.4×`) with `0.7×` overshoot, and shares the canonical paper/ambient-shadow layer; the large secondary CTA is replaced by an accessible underlined `Later` text button and reduced bottom spacing makes the sheet shorter. Focused tests pass (4 suites / 40 tests), `qa:fast` passes (7 changed suites / 56 tests), and `qa:full` passes (75 suites / 466 tests plus type-check, lint, release/source audits, visual contracts, production build, bundle audit, and native source guard). The exact current web output plus all `721` raw assets was synced only to the authoritative Stack to Six `Web.bundle`; `qa:ios` passed, the signed app verified as `com.taptapdesign.stacktosix.Stack-to-Six`, and Apple confirmed Wi-Fi install-over on `iPhone 13 blue` at installation URL ending `C4FDBCC5-9183-476B-8057-E8904C76E2AB/Stack to Six.app`. That exact bundle launched successfully. Physical gyro feel, first-frame Journey timing, and bottom-sheet visual cadence remain `NEEDS PHYSICAL TEST`.
- Checkpoint `v902` makes Homepage an explicit hard boundary for gameplay-owned idle overlays and consolidates the navigation ownership fixes after `v901`. The `YOU THERE / TAP TAP / ONE MORE` leak had two owners: replacing `showHomepageShell()` with `markHomeMenu()` correctly removed the duplicate visual enter but also skipped transient cleanup, while already-scheduled board callbacks could later re-arm the endgame hint after cleanup. Every Homepage return path (normal Arcade exit, fast clean exit, and early handoff) now runs `cleanupTransientVisuals()` without showing a second shell. Independently, `endgame-hint` enforces a strict surface policy: it can arm/show only in `board-arcade` or `board-journey` while `#app` is visibly interactive; delayed callbacks in Home, Journey menu, Settings, loader, hidden app, or undefined zones self-cancel. Full QA passes 75 suites / 465 tests plus type-check, lint, release/source audits, visual contracts, production build, bundle audit, and native source guard. Exact Stack to Six sync, `qa:ios`, and signed Xcode build pass. Apple confirmed Wi-Fi install-over on `iPhone 13 blue` at installation URL ending `98CCFEE2-C5FC-4296-A195-5EEDC088BA69/Stack to Six.app`.
- The repeated Homepage/Journey handoff audit found two independent regressions. First, a hidden Homepage container reports `offsetWidth = 0`, so the return owner was marking Journey slide 1 active while physically leaving the wrapper at Play slide 0; the later visible finalize then produced the observed Play → Journey slide. Hidden slide synchronization now uses the physical viewport width fallback and native diagnostics proved repeated returns stayed at `wrapperX = -390` with only slide 1 active before and after reveal. Second, Arcade entry intentionally destroys SliderManager input listeners; the new single-owner return no longer used the old side-effect-heavy `forceReady()`, so the visible Play slide could return frozen. The completed Homepage-enter owner now calls side-effect-limited `sliderManager.ensureReady()` only after visual finalization, restoring swipe/nav/CTA listeners without resetting or reanimating the selected slide. Journey Homepage exit now uses one `back.in(1.25)`-style scale owner instead of plain ease-in. Focused tests pass (32 before the Arcade addition; final changed-file QA: 7 suites / 48 tests), type-check, visual contracts, build, native sync, and `qa:ios` pass. The exact signed Stack to Six build was installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed installation URL ending `50FE9CD1-4C59-4D9D-AA07-258B2F45F710/Stack to Six.app`. The physical live run then completed two Arcade → Homepage returns and immediately accepted repeated slider/nav/CTA interactions into Arcade, Journey, and Settings; Journey returns remained physically fixed at slide 1. Measured Settings-return motion stayed at `16.69ms` average with no frame above `50ms`. User feel for the revised single Journey bounce remains the only subjective confirmation still open.
- A physical v901 reproduction proved the remaining fast Journey Back → Enter duplication was not a second Hub render. Homepage return declared completion immediately after scheduling its 1.06–1.12s hero/CTA/nav callbacks, called the side-effect-heavy `forceReady()`, primed enter twice, enabled input early, and allowed its delayed finalize to mutate Homepage after Journey had already retaken the screen. Homepage return now has one `HomepageEnterTransitionOwner` lease, one side-effect-limited slide sync, one prime, one animation run, and one completion. Input and gyro stay locked until completion; a new Homepage → Journey request atomically cancels the lease and every pending Homepage-enter timeout before Journey exit starts. Focused ownership tests and `qa:fast` pass (5 suites / 23 tests plus type-check, changed-file lint, and visual contracts); production build and `qa:ios` pass. The exact signed Stack to Six bundle containing marker `superseded-by-new-homepage-enter` was installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `664BA653-D094-494A-AD9D-D43DDE62032D/Stack to Six.app`. The first post-fix trace shows one ~1.12s return owner and no former activate→27ms suspend overlap; final user visual confirmation remains `NEEDS PHYSICAL TEST`.
- Checkpoint `v901` removes the remaining Homepage Journey `bom-bom`, duplicated CTA/slide residue, and slow duplicated Journey Worlds reveal at their ownership boundaries. Hub readiness is now view-structural, its renderer is DOM-only, one tracked coordinator owns visible Hub enter, and forward navigation no longer invokes Homepage recovery. Homepage motion is suspended before exit; a dedicated monotonic Web Animations exit owns an independent `scale` longhand without crossing zero or fighting responsive `transform`, then finalizes residue only after Homepage is hidden. Preparation runs concurrently with that single exit and Journey receives one reveal handoff. A static ownership regression suite locks these rules. `qa:full` passes 72 suites / 450 tests plus type-check, lint, visual contracts, production build, bundle audit, and native source guard. The final v901 Stack to Six build is ready locally, but its last Wi-Fi install attempts were interrupted and the user stopped the retry; therefore **do not claim v901 is installed on `iPhone 13 blue`** until Apple returns an explicit install success. The previously installed intermediate build at URL ending `AB524876-1A57-49A8-91AC-CDDA42A94AE5/Stack to Six.app` predates the final monotonic Homepage exit.
- The repeated Homepage Journey `bom-bom` and very slow Journey Worlds reveal were traced past the visible-enter request count to three ownership violations. Hub readiness checked only `.journey-board-card`, so the background-prepared Hub was destroyed and fully rendered a second time immediately before reveal; `renderJourneyV700Hub()` also ran its own untracked enter in parallel with the visible coordinator; and forward `showCollectibles()` called Homepage recovery `forceReady()`, which cleared the transition lock and reacquired Homepage gyro mid-handoff. Journey readiness is now view-aware through one shared structural helper, the Hub renderer is DOM-only, one epoch-based coordinator tracks every per-World/cloud tween for both Homepage and World-return sources, delayed duplicate requests are blocked by owner existence rather than `isActive()`, Homepage gyro stops before exit, and forward navigation performs only a silent hidden-slide state sync. Preparation and the 770ms Homepage exit now run concurrently with one reveal handoff instead of the former serial extra 900ms wait. Focused ownership/readiness/spatial tests pass, `qa:fast` passes 25 suites / 268 tests plus visual contracts, build and `qa:ios` pass, and the verified Stack to Six bundle with 721 raw assets was installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `AB524876-1A57-49A8-91AC-CDDA42A94AE5/Stack to Six.app`. Live console session `47346` is attached; final one-navigation trace and user feel remain `NEEDS PHYSICAL TEST`.
- The idempotent build's physical trace proved there was exactly one visible Hub-enter request, but also proved the replacement tracked timeline itself regressed elapsed time: planned `~820ms` enter completed after `~2330ms`. The exaggerated duration made one `back.out` overshoot look like a duplicate. The Hub now uses one directly tracked GSAP group tween for all three cards (the earlier physically measured fast construction), with a separately tracked cloud tween and the cards tween as the sole completion owner; rerender/exit/cleanup kill both. The same trace also exposed a WebKit idle-handoff jump: negative CSS animation delays and the idle keyframe's initial `-0.5deg` rotation did not match GSAP's identity endpoint. Visible Hub completion now starts a seamless idle phase with zero initial delays, identity rotation, and varied per-world durations instead of phase jumps; gyro still activates only after completion. `qa:fast` passes (23 suites / 252 tests plus visual contracts), type-check/build/`qa:ios` and signed Xcode build pass. After two interrupted Wi-Fi transfers, Apple confirmed install-over on `iPhone 13 blue` at installation URL ending `E23DF333-0169-4526-9859-C2B3D8696FBF/Stack to Six.app`. The app is launched with live console session `45257`; elapsed-time and visual acceptance remain `NEEDS PHYSICAL TEST`.
- Physical testing of installation `BF26...` showed the Hub visibly restarting and therefore appearing very slowly. The visible-enter method itself was not idempotent: any repeated caller killed its active timeline, reset all three World cards to `scale .65 / opacity 0`, and replayed the full cascade. The manager now rejects a repeated request while its tracked timeline is active and also rejects a replay after the Hub has reached idle-ready; it emits native `hub-visible-enter-request`, `hub-visible-enter-ignored-duplicate`, and `hub-visible-enter-single-timeline-complete` markers so physical acceptance proves the exact call count. A pure regression contract covers entering/idle/hidden decisions. Focused tests pass (32), `qa:fast` passes (23 suites / 252 tests plus visual contracts), type-check/build/`qa:ios` pass, and the exact signed Stack to Six build was installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `D855E89C-DC27-4DDF-93F6-E1329E2FC2A2/Stack to Six.app`. It is launched with live console session `85066`; final visual acceptance remains `NEEDS PHYSICAL TEST`.
- The first Journey Hub gyro-reuse correction removed the measured listener reset but the user still saw a double/jerky finish, so the lifecycle was traced one level deeper. The Hub had two enter owners: hidden background rendering could consume the same enter/idle/gyro lifecycle later replayed by the visible Homepage → Journey flow, and spatial ownership could therefore remain live while GSAP still owned the visible card transforms. Hidden Hub preparation now creates stable DOM only and explicitly defers enter, idle, and gyro until presentation. The visible Homepage path deactivates stale spatial ownership, runs all three World cards and the cloud layer through one tracked GSAP timeline, atomically clears its identity transforms, and only then starts CSS idle plus one Hub spatial activation. Exit, rerender, and cleanup kill the tracked Hub timeline. A pure regression contract locks hidden-versus-visible ownership; focused spatial/Journey tests pass (31), `qa:fast` passes (23 suites / 251 tests plus visual contracts), type-check/build/`qa:ios` pass, and the exact signed Stack to Six build was installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `BF26C9CB-F619-4A3E-A19B-6B774453FB1E/Stack to Six.app`. It is launched with live console session `96115`; final Homepage → Journey Worlds physical feel and trace remain `NEEDS PHYSICAL TEST`.
- A physical Homepage slider → Journey Worlds trace reproduced the reported double/jerky finish and isolated it from FPS pressure: the Hub correctly activated its 16 gyro targets during visible enter, then at the final animation boundary the same `activateJourneyHub()` call stopped the listener, cleared every live `translate`, restarted the listener, and rebound the identical 16 targets. The shared app motion owner now reuses an already-active identical target/depth set for both Journey Hub and Journey World instead of performing a destructive deactivate/reactivate cycle; real DOM/surface changes still receive full cleanup and rebinding. A regression test preserves a live Hub gyro offset across repeated activation. Targeted tests pass (20), `qa:fast` passes (22 suites / 240 tests plus visual contracts), production build and `qa:ios` pass, and the signed Stack to Six app with the `surface-activation-reused` marker was installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `DB75AEA6-5F4B-4102-A0D0-7418F42CA281/Stack to Six.app`. It is launched with live console; the same Homepage → Journey Worlds path remains `NEEDS PHYSICAL TEST` for trace and user-feel confirmation.
- The launch 3D Motion bottom sheet now uses the exact same shared `assets/modals/paper.png` surface recipe as the gameplay Exit/Score bottom sheets. Its former `applyAppPaperSurfaceToElement()` call was the contrast mismatch: that helper injected the darker canonical full-screen app paper overlay inline with `!important`. A dedicated `bottom-sheet-paper-surface` class now shares one CSS texture source with `.simple-bottom-sheet`, while the launch sheet retains its own layout, safe-area, animation, and cleanup ownership. Focused permission/launch tests pass (12), type-check and changed-file lint pass, the production build passes, and `qa:fast` passes (22 suites / 239 tests plus visual contracts). Exact native sync and `qa:ios` pass; the signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 721 raw assets and the new surface marker, then installed over Wi-Fi and launched on `iPhone 13 blue` at installation URL ending `6CB99AFC-CDFE-4E41-A46C-B91EB04CCA39/Stack to Six.app`. Final physical color/contrast parity remains `NEEDS PHYSICAL TEST`.
- A physical run reproduced user-visible global jerk after Settings → 3D OFF/ON → Homepage return even though gameplay itself remained near 60fps (`16.67–16.70ms` normal windows; drag mostly `0.29–1.6ms`, one `9ms` input sample; isolated `54ms` frame). Ownership review found the app already had one sensor listener/RAF, but OFF performed a hard deactivate while ON had no controlled reactivation handoff; multiple Homepage refresh points could restart/reset motion while the Homepage enter still owned transforms. The existing controller is now explicitly the single app-level `appSpatialMotion` owner (the historical export aliases the same instance). It provides an activation hold that coalesces all requests during Settings/Homepage transitions and releases only the latest visible surface after Homepage safety-finalize. Settings holds motion before exit; every Homepage enter holds it before animation and releases/rebinds once after completion. A bounded, read-only six-second `[CC_SPATIAL_PERF]` frame audit measures the physical Settings-return window and self-terminates. Focused tests pass (25), `qa:fast` passes (22 suites / 238 tests), type-check/lint/build/`qa:ios` pass, and the exact signed Stack to Six build was installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `EA865C25-0375-445B-A666-51ED6A09561E/Stack to Six.app`. The first post-fix physical OFF→ON→exit run stayed at `16.67–16.70ms` average with zero frames over `34ms`; one transient cleanup sample reached 35 active timelines / 185 GSAP children and returned to zero in the next one-second sample, so the trace shows no sustained animation buildup. The captured tail contains one listener-stop and no duplicate-listener signal. Explicit user confirmation of whether the former visible jerk is gone is still required before extending app-level target registration to transitions and modals.
- Physical diagnostics found the exact reason Developer `Reset 3D Intro` did not reopen the launch sheet: the one-shot flag persisted correctly (`forced:true`), but `shouldShowSpatialMotionPermissionModal()` rejected it first because the player-facing 3D toggle was OFF (`reason:"spatial-motion-disabled"`). The decision order now lets the developer-forced one-shot preview bypass only the disabled/gesture-not-required product gates while still respecting active-dialog ownership, Reduce Motion, and native API availability. Normal launches with 3D OFF remain suppressed. A focused regression assertion covers `forced + spatial disabled + no gesture required`; targeted tests pass (10) and `qa:fast` passes (22 suites / 236 tests). The exact Stack to Six bundle was rebuilt, verified as `com.taptapdesign.stacktosix.Stack-to-Six` with the diagnostic marker and tilt art, and installed over Wi-Fi on `iPhone 13 blue` at installation URL ending `A2746BB9-D0B4-4315-A600-73F194FA73D4/Stack to Six.app`. Final physical reset/presentation and blue-outline confirmation remain `NEEDS PHYSICAL TEST`.
- The launch 3D bottom sheet no longer shows WebKit's default blue focus ring: the dialog panel remains programmatically focused for correct accessibility/focus ownership but now has a narrowly scoped `outline: none`; CTA keyboard focus styles are untouched. Developer Settings `Reset 3D Intro` now truly forces the next launch preview even after iOS motion permission has already been granted: the durable one-shot flag is evaluated before the normal `requiresPermissionGesture()` early return, while disabled 3D Motion, unsupported platforms, and Reduce Motion still suppress presentation. Focused regression tests cover both paths. Targeted tests pass (10), `qa:fast` passes (22 suites / 236 tests), and `qa:full` passes (71 suites / 439 tests plus type-check, lint, production build, release/bundle audits, visual contracts, and native source guard). Exact Stack to Six sync and `qa:ios` pass; the verified signed app with 721 raw assets was installed and launched with a live console on `iPhone 13 blue`. Final physical reset/outline confirmation is in progress and remains `NEEDS PHYSICAL TEST`.
- The launch 3D Motion bottom sheet now gives each wild-star decoration an independent, phase-offset CSS `scale` pulse (`0.96↔1.04`) composed with its existing cartoon bounce, so size breathing is visible without JS timers, RAF work, or new cleanup ownership; Reduce Motion still disables both animations. Its instruction now exactly matches the accepted End Run `Come back anytime` subtitle typography: Baloo2 `20px`, weight `500`, `#CBA89A`, line-height `1.3`, normal style, and centered alignment. A focused source-contract test locks both relationships. Targeted tests pass (9), `qa:fast` passes (22 suites / 235 tests), and `qa:full` passes (71 suites / 438 tests plus type-check, lint, production build, release/bundle audits, visual contracts, and native source guard). Exact Stack to Six sync and `qa:ios` pass; the verified signed app with 721 raw assets was installed/launched on `iPhone 13 blue`. Physical scale/font feel remains `NEEDS PHYSICAL TEST`.
- The launch 3D bottom sheet now matches the End Game title contract exactly: Baloo2 `40px`, weight `900`, line-height `1`; only the `3D` span uses the established orange `#e8744a`, while `Motion` remains taupe. Both title→art and art→instruction spacing are exactly 24px larger than the preceding version, including the compact-height profile. The instruction is now the clearer `Tilt your phone and watch the game move with you.`, and the primary CTA is the friendlier single-line `Let’s Move`; `Later` remains secondary. The former hard 1.2s two-frame swap is now a 2.4s eased crossfade: the outgoing tilt remains as a 20% ghost while the incoming frame becomes fully opaque, producing an approximately 80% motion-blur fade without CSS blur, new JS timers, or RAF work. `qa:full` passes 71 suites / 437 tests plus type-check, lint, production build, release/bundle audits, visual contracts, and native source guard. The exact current `dist` was synced into the bundled Stack to Six shell, `qa:ios` passed, and the verified signed app with 721 raw assets was installed/launched on `iPhone 13 blue`; physical cadence and spacing remain `NEEDS PHYSICAL TEST`.
- The launch-owned 3D permission surface is now a true bottom-anchored paper sheet over the still-visible TapTap/character preloader. Its copy is reduced to title `3D Motion`, one short tilt instruction, and single-line `Enable` / `Later` bottom-sheet CTAs; the former settings fallback paragraph, divider, robo4/robo7 art, and centered-card layout are removed. The sheet uses the supplied `tilt1/tilt2` frames as a CSS-only two-frame continuous tilt loop plus three overlapping shared `wild.png` stars with independently phased cartoon idle bounces. All art variants preload before presentation, the loop creates no JS interval/RAF, DOM removal stops it atomically, Reduce Motion freezes a clean first frame, focus belongs to the sheet instead of visually pressing the primary CTA, and non-overshooting vertical easing keeps its bottom edge sealed to the viewport. `qa:fast` and `qa:full` pass; full QA reports 71 suites / 437 tests plus type-check, lint, production build, release/bundle audits, visual contracts, and native source guard. Exact Stack to Six sync and `qa:ios` pass; the signed build contains 721 raw assets including all tilt variants and is installed/launched on `iPhone 13 blue`. Physical bottom-sheet proportions, sprite cadence, and safe-area feel remain `NEEDS PHYSICAL TEST`.
- Developer Settings now contains a one-shot `Reset 3D Intro` action. It stores a durable next-launch flag, clears only the current session dismissal, changes to `3D Intro Ready` for explicit feedback, and consumes the flag when the launch-owned modal is actually presented; it never opens the permission UI over Settings. Gameplay cube motion in Journey and Arcade is exactly 50% of the preceding `16.25×24.4` profile (`8.125×12.2`). A session-random rotation assigns six stable per-cell two-axis direction patterns, including X/Y inversions, so neighboring cubes can move oppositely without frame-to-frame randomness, additional listeners, or additional RAFs. `qa:full` passes 71 suites / 437 tests plus type-check, lint, production build, release/bundle audits, visual contracts, and native source guard. Exact Stack to Six sync and `qa:ios` pass; the verified app contains 717 raw assets and the new reset/direction markers and is installed/launched on `iPhone 13 blue`. Modal reset behavior and experimental inverted cube feel remain `NEEDS PHYSICAL TEST`.
- The global 3D Motion permission experience now belongs to launch instead of Journey Hub. After critical assets are ready, launch intentionally pauses while the TapTap logo and selected character remain visible, presents one branded paper modal, and only begins character/logo exit after the user chooses `Turn on 3D Motion` or `Not now`. The modal uses the exact existing `primary-button bottom-sheet-cta` and `exit-btn bottom-sheet-cta` button system, has one paper panel/no duplicate shell, robo4 above robo7, a divider, and explicit Settings fallback copy with safe compact-height spacing. Both startup safety timeouts recognize this as intentional user waiting and cannot reveal Homepage behind it; dispose/error cleanup cancels the modal and resolves ownership. Journey Hub presentation and old permission module residue were removed.
- The post-fix physical drag trace confirmed the ownership correction: one deliberately slow live drag lasted `7704ms` with 353 move events and ended normally as `pointerup`, not `runtime-clear`; move handling averaged `0.30ms`, peaked at `2ms`, and had zero samples over `8ms`. A second `1592ms` drag also ended normally. This physically verifies that `checkLevelEnd()` no longer forcibly snaps back a still-owned slow drag. Rapid wild-star/TNT chaining remains a separate physical acceptance case.
- A physical rapid-gameplay trace on `iPhone 13 blue` reproduced involuntary snapback during a slow, still-moving drag. The renderer remained healthy, but DragPerf ended twice with `runtime-clear` instead of `pointerup`; source inspection proved `checkLevelEnd()` was treating any drag deferred for more than three seconds as stale and directly running drag cleanup even while fresh pointer moves continued. Endgame is now a pure observer through `active-drag-endgame-policy.ts`: every live, non-destroyed drag defers endgame without a wall-clock cancellation. Lost-pointer recovery remains exclusively in `drag-core.ts`, whose existing nine-second watchdog is refreshed by actual pointer movement. The duplicate endgame-owned tile/grid reset and listener cleanup path was removed. `qa:fast` and `qa:full` pass; full QA reports 70 suites / 432 tests plus type-check, lint, production build, release/bundle audits, visual contracts, and native source guard. The exact bundle passed `qa:ios`, was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 717 raw assets and the new ownership marker, then installed over Wi-Fi and launched with live console on `iPhone 13 blue`. Slow-drag and rapid merge/wild/TNT behavior remain `NEEDS PHYSICAL TEST` on this new build.
- Gameplay-board cube gyro depth is now exactly 30% stronger than the previous local profile (`12.5×12.5 → 16.25×16.25`) through the existing shared `JOURNEY_SPATIAL_DEPTH.gameplayTile` source of truth. This affects active cube-face wrappers in both Journey and Arcade only; the HUD preload bar, Homepage, Journey Hub/World/Units, sensor normalization, half-point pixel snapping, one-listener/one-RAF ownership, drag coordinates, stack/merge motion, and cleanup remain unchanged. The focused spatial suite passes 16 tests, and `qa:full` passes 69 suites / 430 tests plus type-check, lint, release/bundle audits, static visual contracts, no-sync production build, and native source guard. This change is included in the latest `iPhone 13 blue` install; physical intensity/edge-shimmer feel remains `NEEDS PHYSICAL TEST`.
- The rapid-input plain value-6 residue now has scoped cleanup ownership. Physical evidence showed the residue was dead to touch but still present in board state because a later magnet could pull and remove it. Each non-final regular merge-6 destination now receives an immutable per-tile token, stays visible for impact FX but remains non-interactive across `setValue()` refreshes, and is removed only by its matching cleanup callback. A bounded 1.2s watchdog removes that same tokenized destination if an async early-return skips normal cleanup; it releases without removal if the destination is promoted to a trusted final merge or repurposed. Magnet candidate selection rejects cleanup-owned destinations, tile teardown clears the token, and regular cleanup no longer deletes an arbitrary newer tile merely because it currently owns the same grid coordinate. The shared wild/final-merge logic and normal fast input on other cubes are unchanged. Focused merge/final/lifecycle tests pass (62 tests), and `qa:full` passes 69 suites / 430 tests plus type-check, lint, release/bundle audits, static visual contracts, no-sync production build, and native source guard. This fix is included in the latest `iPhone 13 blue` install; rapid physical regular→regular and regular→wild merge-6 overlap remains `NEEDS PHYSICAL TEST`.
- A five-minute post-install physical stability run on `iPhone 13 blue` exercised Homepage → Journey Hub → Forest/Beach → two Journey boards → world/hub/home returns with active gyro. Two complete 30-second board audits stayed near the 60fps budget: normal one-second windows were about `16.59–16.70ms`; drag processing was usually `0.25–2.22ms` per processed move with a `4ms` maximum and zero moves over `8ms`. One `73ms` special-FX frame temporarily enabled the existing reduced-FX guard, which recovered to full effects about two seconds later; isolated World-return frames reached `69–70ms` (Forest return `916ms`, `17.22ms` average; Beach return `723ms`, `18.32ms` average). Timer/listener/interval counts and active GSAP ownership fluctuated with live effects but returned downward rather than growing monotonically, so this run found no lifecycle buildup signal. Absolute WKWebView memory in MB remains unmeasured: JavaScript memory APIs are unavailable on physical Safari/WKWebView, and a follow-up Apple `Game Memory` trace could not start because Instruments listed the Wi-Fi-connected iPhone as offline even though CoreDevice console capture worked. The measured telemetry is healthy; overall verdict remains `NEEDS PHYSICAL TEST` for user-confirmed feel, sustained memory footprint, and the rare World-return hitch.
- The second spatial-motion polish phase is implemented and installed. Gameplay cube depth is 25% stronger (`10→12.5`) and now uses half-point placement, `roundPixels`, clamp-to-edge linear sampling, generated mipmaps, and low `2×` anisotropy to reduce the physical gyro edge shimmer without enabling renderer-wide antialiasing. The existing wild-meter preload bar is wrapped non-destructively and follows a smaller `6.4×5.6` gyro profile; its own layout/progress ownership remains unchanged. Forest main moved exactly 16px upward (`y -16→-32`), while Forest Stage Units use a 35% stronger, stable five-pattern two-axis direction map so a Unit's island/card/prop/stars/clouds stay grouped without per-frame randomness. Journey Worlds Hub alone is 30% calmer and now mixes a bounded amount of roll into its vertical axis so natural phone movement reads in both dimensions. All ownership remains inside the existing single orientation-listener/single-RAF controller and resets cube/HUD wrappers on exit. `qa:full` passes 68 suites / 425 tests plus type-check, lint, release/bundle audits, visual contracts, production build, and native source guard; `qa:ios` passes after exact native sync. The signed bundle was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 717 raw assets and current spatial/filtering markers, then installed over Wi-Fi and launched on `iPhone 13 blue`; Apple reported installation URL ending `B19CE994-069A-436B-A659-1904F05D8CC6/Stack to Six.app`. Final cube-edge appearance and motion feel remain `NEEDS PHYSICAL TEST`.
- A physical `iPhone 13 blue` Arcade gyro run isolated the reported rigid/jagged cube edges from performance pressure. The live renderer stayed at resolution `2`; drag processing averaged roughly `0.34–1.5ms`, peaked at `4ms`, and recorded zero processed moves over `8ms`; ticker pressure was limited to isolated `42–43ms` peaks. The confirmed cause was illustrated cube textures forced to pixel-art `nearest` sampling while gyro moved their wrappers through fractional coordinates. `gameplay-texture-filtering.ts` now centrally applies Pixi `linear` sampling to ordinary, stacked, locked, wild/magnet/juice/TNT, preload, refresh, fallback, and post-FX recovery texture paths. Global renderer antialias remains unchanged, so this adds no ticker/listener/object lifecycle or renderer-wide GPU cost. Focused filtering tests plus `qa:fast`, `qa:full` (68 suites / 423 tests), production build, and `qa:ios` pass. The verified bundled app contains 717 raw assets, the exact bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, and the linear sampling marker; install-over and launch succeeded over Wi-Fi on `iPhone 13 blue` at installation URL ending `F37C4D5D-44FE-4B13-9691-63048BCE1FCB/Stack to Six.app`. Final edge appearance during physical gyro motion remains `NEEDS PHYSICAL TEST`.
- The Settings header's right-side Developer icon works after real app navigation again. Root cause was lifecycle ownership: `showSettingsScreenWithAnimation()` dispatches `cc-navigation`, while the persistent Settings component previously interpreted that event as full disposal and removed its only delegated click/change handlers. The same UIManager later reattached only Back, which explains why Back worked while Developer did nothing. `cc-navigation` now only resets the local Settings sub-view to main; full listener removal remains exclusively in the explicit `_settingsCleanup` disposal path. A regression test dispatches real navigation before clicking the Developer icon and verifies the Developer title/panel plus main-panel hiding. `qa:full` passes 67 suites / 420 tests plus lint, release/bundle audits, visual contracts, and native source guard. This fix is included in the latest installed `iPhone 13 blue` build.
- The shared `3D Motion` gyro owner now covers live Pixi gameplay cubes in both Journey and Arcade as well as the already-connected main hero image and CTA on all three Homepage sliders. Each gameplay tile owns one lightweight `_ccSpatialG` wrapper around its face/stack/pips while its grid container, x/y target, drag math, scale/bounce/merge timelines, and shadow remain untouched; the stable shadow plus per-cell bounded depth variation creates the parallax read. The controller still owns exactly one device-orientation listener and one smoothing RAF, dynamically reads the current tile array so spawned/merged tiles participate without per-tile listeners, ignores hidden/empty placeholders, and resets every surviving wrapper to `(0,0)` on board exit, rebuild, cleanup, feature disable, or surface handoff. Homepage tests now exercise all three active-slide hero/CTA ownership transitions, and a focused Pixi-wrapper test proves movement plus exact cleanup. This follow-up is included in the latest installed `iPhone 13 blue` build; physical gyro intensity, drag feel, and sustained gameplay FPS remain `NEEDS PHYSICAL TEST`.
- Settings now exposes a player-facing `3D Motion` toggle (default ON) that persists with the existing settings object, immediately deactivates and cleans spatial ownership when switched OFF, and requests native iOS motion permission directly from the toggle gesture when switched back ON. All developer controls were removed from the main Settings list and isolated in a dedicated `Developer` sub-screen opened by the top-right settings icon; one exported `SETTINGS_DEVELOPER_TOOLS_ENABLED` release switch removes both its entry point and rendered panel for App Store preparation. The sub-screen has atomic main/developer ownership, one Back path, hidden footer, independent scrolling, and cleanup returns it to the player-facing screen. The branded Journey permission explainer no longer depends on a permanent localStorage “seen” flag: install-over/relaunch may preserve storage, so presentation is now once per app session whenever the runtime still needs an iOS permission gesture; `Not now` remains session-only and disabling 3D Motion suppresses the explainer. `qa:full` passes 67 suites / 416 tests plus type-check, lint, release/bundle audits, visual contracts, production build, and native source guard; `qa:ios` passes after explicit Stack to Six bundle sync. The final app was verified as `com.taptapdesign.stacktosix.Stack-to-Six`, with `dist/index.html` identical to the built `Web.bundle`, all 717 raw assets, and the new Settings/gyro markers. It was installed over Wi-Fi and launched on `iPhone 13 blue`; installation URL ends in `9CA54359-2D23-41EC-916B-847FD0791C2E/Stack to Six.app`. Settings/modal/stats feel remain `NEEDS PHYSICAL TEST`.
- Journey detail-modal stats ENTER now has a literal runtime relationship with the accepted EXIT instead of a separately tuned approximation. All five direct children (`High Score → divider → Longest Combo → divider → Cubes Cracked`) use the same `detailStatPopOut` CSS keyframes, `0.4s` duration, cubic easing engine, and `0.05s` DOM-order stagger; ENTER sets only `animation-direction: reverse`, so scale, opacity, visibility, and easing are mechanically reversed. The former compact three-group GSAP path, stat-only `0.82` start, and divider-specific duration/ease are gone. One stored restore timer and the existing modal cleanup remove enter/exit classes, delays, animation properties, and compositor hints on completion, close, reopen, or interruption; a pre-start exit guard prevents the enter cascade from being recreated during close. Focused numeric and source-contract tests lock the shared keyframe/reverse ownership. This follow-up is included in the latest installed `iPhone 13 blue` build and still needs physical feel confirmation.
- The shared spatial gyro controller now covers Homepage as well as Journey while retaining one permission state, one orientation listener, one RAF, and the common `JOURNEY_SPATIAL_STRENGTH = 0.6` output gain (exactly 40% below the previous motion). Only the active Homepage slide owns targets: its hero uses the deeper `14×10` profile and its CTA the restrained `6×4.5` profile, both before the shared gain. Slide changes and instant restores atomically clean the previous hero/CTA and bind the new pair; every Homepage reveal refreshes ownership, hidden Homepage state zeroes stale tilt/baseline, slider destruction only deactivates Homepage-owned motion, and CSS `translate` remains independent from swipe/tap/enter GSAP `transform`. Journey Hub, World-main, Unit, accent, and cloud layers keep their existing relative randomized depths/directions under the same 40% reduction, and equal upward/downward pitch remains symmetric. `qa:full` passes (66 suites / 411 tests plus type-check, lint, source/bundle audits, Journey visual contracts, production build, and native source guard), as does `qa:ios`. The exact current Web.bundle was synced, built into verified bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, installed over Wi-Fi, and launched on `iPhone 13 blue`; installation URL ends in `BEDD8EB8-A3FC-45E3-B71E-D5326670CE12/Stack to Six.app`. Homepage/Journey physical gyro feel remains `NEEDS PHYSICAL TEST`.
- Journey spatial motion now covers every world. The Hub independently moves the Forest, Beach, and Area 55 main PNGs plus every world-owned cloud; each individual World screen moves its matching main/cloud layer and every visible Stage Unit's island, card/number, stump/prop/crater, stars/beam, and individually varied opposite-direction clouds. Main Hub/World horizontal art travel remains doubled, while vertical depth is now balanced to the same maximum (`16.8/16.8` and `17.6/17.6`) and the pitch sensor reaches full travel over 9° instead of 12°. Unit and cloud Y depths were balanced the same way. Each app session rotates three distinct diagonal direction vectors across Forest, Beach, and Area 55, while a separate session depth offset varies individual layer spacing; directions remain stable during play so randomization cannot create frame-to-frame jerks. A seven-value organic depth pattern prevents uniform cloud walls, and viewport-near `IntersectionObserver` ownership limits per-frame writes/compositor hints to visible or near-visible PNGs while preserving existing `will-change`. All sensor/observer/RAF ownership stops on transition, rerender, cleanup, background, or Reduce Motion; layout, hit areas, scroll, gameplay state, and GSAP `transform` ownership remain unchanged through independent CSS `translate`. The v3 one-time branded permission explainer now opens automatically only after the visible Journey Worlds Hub finishes entering; individual World taps no longer own or retrigger it. It uses one canonical paper panel with no duplicate outer frame, bottom-sheet typography, the exact shared orange primary and white secondary bottom-sheet CTA classes, and vertically layered `robo4` above `robo7` artwork. Hidden/background-preloaded Hub state cannot open it, duplicate overlays are guarded, dismissal remains session-only, and the native motion permission stays synchronously owned by the orange CTA. `qa:fast`, `qa:full` (66 suites / 408 tests), production bundle sync, `qa:ios`, exact bundle/content verification, Xcode build, install-over, and launch all pass. The latest build is installed and running on `iPhone 13 blue`; its installation URL ends in `1AA7B4BC-D784-452A-B1B9-0E4A0DF94095/Stack to Six.app`. Modal appearance/timing and balanced physical gyro feel remain manual physical QA items.
- Regular merge-6 white smoke now uses an organic radial field instead of either the former upward-biased box or the too-regular four-side flower. Each pooled particle independently randomizes its start radius from the cube core to beyond an edge, angle, travel distance, lateral drift, and visibly varied circle/ellipse silhouette. Particle count, cloud alpha shaping, pooling, timeline ownership, and cleanup are unchanged. This follow-up is included in the latest bundled `iPhone 13 blue` install.
- Checkpoint v815 adds the repository-native QA system: `.agents/skills/stack-to-six-qa` defines the `PASS` / `FAIL` / `NEEDS PHYSICAL TEST` contract and an independent read-only reviewer protocol; `qa:fast` runs staged/unstaged diff hygiene, typecheck, changed-file lint/tests, and selector-scoped Journey visual contracts; `qa:full` adds the source audit, full lint, all 63 suites / 392 tests, a no-native-sync production build, built-bundle audit, and native source guard. `qa:ios` read-only checks the authoritative Stack to Six Xcode PBX target/shared scheme/product/container, exact bundle ID, bundled-mode flag, complete hash-matched raw assets, all eight intro characters, and `dist` versus `Web.bundle` freshness. Dangerous generic Capacitor iOS sync/build scripts and the stale native audit were removed, and former release docs no longer direct agents toward those commands. GitHub Actions runs `qa:full` on PRs and pushes. Both `qa:fast` and `qa:full` pass; `qa:ios` correctly reports `NEEDS SYNC` because this task built a fresh `dist` with native sync disabled, so no Web.bundle, Xcode project, app, or phone was changed. An independent read-only QA agent first found four real coverage weaknesses, all four were corrected, and its second audit returned no findings with `NEEDS PHYSICAL TEST` only for real safe-area clipping and bottom-decor motion feel.
- Journey Worlds Hub now uses the same horizontal decor-canvas ownership as the accepted individual Forest screen: only `.journey-v700-hub-cloud-layer` escapes the shared Journey left/right content padding, so its `overflow: hidden` boundary sits outside the content inset and the physical Journey viewport becomes the visible side clip. A Hub-only cloud coordinate compensation preserves every previously tuned cloud-to-World position exactly; World cards, their hit areas, Hub height/bottom trim, vertical scroll, and all Journey animations are unchanged. This clipping follow-up is included in the latest bundled `iPhone 13 blue` install.
- Journey board gameplay's randomized bottom land can no longer lift away from the physical viewport edge during enter or exit. Its shared motion profile now owns strictly non-overshooting vertical travel (`power3.out` from `y:84→0` on enter and `power2.in` toward positive `y` on exit); the much smaller arrival settle affects only bottom-anchored scale. This preserves a light cartoon landing while keeping the bottom edge invariant, so the canonical paper background cannot flash underneath any of the 12 land variants. Focused motion-contract coverage was added; this follow-up is included in the latest bundled `iPhone 13 blue` install.
- The shared regular-cube stack-contact profile used by both Journey and Arcade now exaggerates squeeze/stretch by exactly 30% (`stack.strength 1.0→1.3`). The responsive anticipation/impact timing is unchanged, while rebound plus final settle lasts `0.31s` instead of `0.24s` (`0.13s + 0.18s`), giving the bounce-back a longer cartoony read without delaying gameplay decisions or changing the separate idle profile. Focused profile coverage, targeted ESLint, type-check, `git diff --check`, and the production web build pass. This tuning is installed and launched on `iPhone 13 blue` in the latest bundled build.
- Journey Hub and individual World screens retain native vertical momentum/touch scrolling but now hide the browser/WKWebView scrollbar completely. The earlier Journey scroll-owner override that zeroed shared horizontal padding variables was removed because it changed the established Forest map/decor coordinate relationship and made the Stage cards read shifted left; the original negative-margin edge-to-edge map layout is restored. On the individual Forest screen, the first upper-left Forest-main cloud moved 30px left and 20px up (`x -14→-44`, `y -18→-38`). Included in online checkpoint v814 and the latest `iPhone 13 blue` install.
- Final merge completion now has one bounded runtime guard shared by regular, star, juice, TNT, and magnet results. The resolver marks the destination before its impact tween begins; `checkLevelEnd` defers while that runtime is active, so the orphan-magnet recovery sweep cannot destroy the live destination and skip its finale. The duplicate inline magnet residue sweep was removed in favor of the guarded centralized cleanup. If bounded recovery is eventually needed, the recent runtime snapshot preserves the exact special finale type and dispatches the matching clean-board handoff instead of the generic reason. Eight focused guard tests, targeted ESLint, type-check, `git diff --check`, and the no-sync production build pass. Included in online checkpoint v814 and the latest `iPhone 13 blue` install.
- Player-facing progression terminology is now consistent across the app: every Journey board is presented as **Stage / Stages**, and every Arcade stage is presented as **Round / Rounds**. `gameplay-terminology.ts` is the single source for dynamic labels used by the HUD, fail/clear/exit flows, and Arcade round transition; Journey World ranges, detail fallbacks, tutorial copy, achievements, settings/dev controls, navigation accessibility labels, and resume copy were updated too. Internal `board` save/state/API names and Pixi `stage` names remain unchanged, preserving storage and gameplay compatibility. Focused terminology tests, targeted ESLint, type-check, `git diff --check`, and the no-sync production build pass. This change is installed on `iPhone 13 blue` in the latest bundled build.
- Regular gameplay cubes now share one controllable cartoon motion family across Journey and Arcade. New `gameplay-tile-cartoon-motion.ts` centrally owns separate `stack.strength` and `idle.strength` plus bounded timings/poses. Every regular stack contact randomly chooses a visible-but-limited vertical stretch or horizontal squash cycle (maximum about 10%) with anticipation, rebound, and exact `1,1` settle; the previous uniform-only scale pulse is removed. After the existing four-second no-input delay, the idle owner uses the same random variants at a gentler maximum of about 8.5%, with a small tilt and the existing one-tile-at-a-time cadence/smoke policy. Both timelines remain AnimationManager-owned, replacement-safe, and cleanup-safe; merge decisions, wild/merge-6 FX, smoke/shards, scoring, and input timing are unchanged. Five focused motion/interim tests, targeted ESLint, type-check, `git diff --check`, and no-sync production build pass. This motion profile is installed on `iPhone 13 blue` in the latest bundled build.
- Journey board gameplay's randomized `bottom1–12.png` foreground no longer begins its enter while the board transition can still cover it or while the selected DOM image is undecoded. `startLevel()` now only primes/decodes the foreground at `y:84`, lightly vertically squashed and transparent; the existing visible HUD/board-enter callback owns one tracked `0.62s` `back.out(1.9)` GSAP pop-in to its final position. Repeated HUD callbacks are idempotent, hide/exit invalidates pending decode work through a lifecycle token, active enter/exit tweens are killed/replaced cleanly, and the old layout-driving `bottom` CSS transition was removed. This applies to all 12 Journey bottom variants. The Journey bottom-decor scope test (3 tests), targeted ESLint, type-check, `git diff --check`, and no-sync production build pass. It is installed on `iPhone 13 blue` in the latest bundled build.
- Paper-background ownership is now centralized and visually consistent. `body` is the one visible viewport paper owner through first frame, TapTap preload, Homepage, Journey, and Settings; the launch container and legacy `#global-bg` are transparent, and the launch module's retired multi-owner `setBackground()` branch was removed. Fail Board, Clear Board, and the short pre-Clear handoff cover still correctly occlude the live gameplay canvas, but all three now render through the same canonical shared helper instead of their darker 65% paper recipes. Targeted ESLint, `npm run type-check`, `git diff --check`, and `SKIP_NATIVE_BUNDLE_SYNC=true npm run build` pass. This paper-owner change is installed on `iPhone 13 blue` in the latest bundled build.
- Journey World image readiness can no longer strand a screen at `opacity: 0`: the already-complete `img.decode()` branch now shares the same hard deadline as load/error listeners instead of returning an unbounded promise. Live Hub → World and homepage-visible World enters do not block their local/cached Unit animation on all decorative image decodes. This addresses the web trace where preparation logged but `world-enter-images-ready`/complete never followed.
- A manual Journey Hub → World tap is now the authoritative start of a fresh visible lifecycle: it clears stale tutorial/game-return suppression markers, and `applyJourneyV700WorldScope` never lets those old flags override the live `__ccJourneyV700Opening` owner. Background and real return renders still prime safely, but a user-selected Forest/Beach/Area 55 can no longer be left hidden by residue from an earlier flow.
- Every current and future Journey Worlds Hub item now uses the selected World card as a first-class target inside the shared Hub exit timeline. It matches the active cjelina-card motion with one continuous visible scale-to-zero over `0.40s` using `back.in(1.7)`; that easing supplies the single initial outward nudge before the World folds into itself. There is no separate `back.out` punch to duplicate the outward lobe, and opacity is removed only after shrinking finishes. Other Worlds, clouds, and nav continue their coordinated standard exit with interruption-safe completion.
- Product-facing Journey world 3 terminology is now **Area 55** everywhere: Hub/header/accessibility metadata and the former `ROBO MAIN` / `ROBOT RIFT` board labels. Existing internal `robo-*` CSS classes, asset paths, and identifiers remain stable implementation details and are not user-visible.
- Journey Worlds Forest now has an additional upper-left cloud explicitly owned by world 1. The complete Journey map canvas—paper, Hub/world content, and clouds—uses zero inherited horizontal content padding, so both sides reach the physical viewport edge and share one clip boundary; header controls retain their own safe-area padding. The Hub's vertical extent and compact Area 55 bottom trim are unchanged.
- Journey visible-enter ownership now distinguishes a live Hub → World tap from a remembered World rendered during homepage background preparation. Background rendering only primes World Units; the visible Journey lifecycle starts their enter once the screen is revealed, preventing Forest from remaining at `opacity: 0` when a stored Forest view is restored on web/iOS.
- The installed v813 follow-up is currently local-only: board intro haptics are packed into six compact grouped `light` beats (45ms minimum spacing) instead of four, still sampled from the actual pop-in wave and lifecycle-owned. Regular merge-6 keeps the same capped pooled particle count and wide/upward travel, but now uses a spatial smoke-cloud alpha profile: opaque/randomized central puffs, progressively softer side puffs, and a short natural fade rather than uniformly solid instant removal.
- Checkpoint `v813 polish Journey idle, merge smoke and grouped board haptics` is online at commit `1d22596`. Its complete 717-file raw asset tree was synced into the bundled-mode Stack to Six shell, the final app verified as `com.taptapdesign.stacktosix.Stack-to-Six`, and install-over succeeded on `iPhone 13 blue` at installation URL ending `A965C28C-8CC0-4167-8A5F-98BB7255EAFC/Stack to Six.app`. The exact bundle launched successfully after installation.
- Board intro haptics are restored as four grouped `light` beats sampled from the actual randomized cube-entry wave, not one native bridge call per/every-other tile. They run only while the real board enter flag owns the pop-in, are tracked with the same delayed-call cleanup as the animation, preserve load/resume suppression, and have focused scheduler coverage.
- Interim landing dust is now 40% more opaque than the prior range, capped at solid white, while keeping the same bounded particle count and replace-on-each-landing cleanup. Regular merge-6 smoke now sells a broken-cube impact through larger solid-white pooled puffs, wider/random outward travel, and an upward bias. Its particle count is explicitly capped below the previous default multiplier, so the stronger visual does not add density/FPS load. The old board-intro per-tile haptics were confirmed removed in v800 when pop-in moved to the centralized frame-budget scheduler; the new grouped cadence above replaces that retired implementation.
- The interim Board card idle was retuned after visual review: it now uses a short five-stage cartoon anticipation, airborne jump, landing, rebound, and clean settle instead of the overly weak 1.045 sine lift. Each repeat safely chooses either a vertical stretch-jump or horizontal squash-pop pose inside the same single owned GSAP timeline. Every completed landing now replaces the prior smoke container with one bounded fresh puff, giving consistent smoke without overlap buildup. Its light treatment mirrors the TapTap launch-logo masked sweep; a strong white-hot/gold screen-blend core lights the question mark while a separate wrapper-owned halo expands outside the clipped card. Neither layer restores the old interval/reflow/filter restart path.
- Journey world idle ownership was rebuilt after a physical-iPhone report that Forest main, Units, and the Board 04 interim card progressively became jerky during a several-minute idle soak. All Forest/Beach/Area 55 Unit motion now shares one GSAP frame ticker instead of one ticker per Unit/cloud, and a single IntersectionObserver pauses transform updates and releases `will-change` for Units more than 240px outside the Journey viewport. Re-entering Units ramp from their current transform to avoid a visibility-edge snap.
- The interim card no longer runs the old 16% scale / 2.5-degree / 240ms punch or emits 25-35 DOM smoke particles roughly every 640ms. Its policy is now a gentle 4.5% scale, 0.7-degree, 3.5px lift with a 0.62s rise and 0.78s settle; smoke is non-overlapping, reduced, and rate-limited to at most once per 5.2s. Smoke absence no longer marks a healthy infinite bounce timeline stale and repeatedly restarts it.
- Interim shimmer/glow ownership is now one persistent `interim-idle-effects-active` CSS lifecycle class. The old 2.9s interval, forced layout reads, nested RAF restarts, filter/drop-shadow animation, stale timeout fields, and retired CSS classes were removed. Render no longer starts interim idle before visible world enter completes. Infinite interim timelines are explicitly removed from `AnimationManager` ownership when killed.
- Focused regression tests cover gentle interim motion/smoke cadence and explicit external-timeline release. Three targeted suites pass (14 tests), targeted ESLint passes, `npm run type-check` passes, and `SKIP_NATIVE_BUNDLE_SYNC=true npm run build` passes. This idle refactor is installed in the latest bundled build but still needs a physical multi-minute soak test on `iPhone 13 blue`.
- A hotspot-era device discovery attempt failed before listing devices because Apple's CoreDeviceService timed out during initialization; no device, native bundle, or app was touched. Vite is running locally on `http://localhost:5174/` (hotspot interface `172.20.10.2`) for browser inspection only; Stack to Six remains bundled-mode and was not pointed at the dev server.
- Journey Worlds hub scroll ownership now follows the updated product decision: every hub render, including `return-from-world`, clears the retired saved hub position and starts at `scrollTop = 0`. The old save/restore property, helper, calls, storage behavior, and stale test expectation were removed; individual Forest/Beach/Area 55 interim-card auto-scroll remains unchanged. A fixed sync/RAF/layout reset alone was physically insufficient because iOS/layout mutated the scroller after that window, so the hub now rejects programmatic scroll until the user's first real pointer/touch/wheel intent, then removes its listeners and gives manual scrolling full ownership.
- Journey world return phase one now reuses the exact pre-reveal preparation instead of repeating `killTweensOf`/`gsap.set` across all 88 Forest targets. `JourneyWorldAnimationCoordinator` owns one finalization pass, and idle begins on the next animation frame with an explicit cancellation path; the v701 duration, easing, scale, position, Unit order, and irregular offsets are unchanged.
- A narrow iOS-only `world-enter-performance` audit now measures the actual game-to-world return window and reports frame count, average, worst frame, and threshold counts. Its summary utility has focused tests. All 35 Journey-focused tests, targeted ESLint, type-check, and the production build passed.
- Arcade wild-star orbit suppression is now owned by `wild-stars.ts`, not only its FX wrapper: attach, async texture/fallback completion, and the shared ticker all reject or clean orbit halos whenever the run mode is Arcade. Journey orbit halos remain enabled, and `run-mode.test.ts` covers the policy.
- Journey Worlds now applies the extra bottom trim to the hub's actual `min-height` owner as well as the cloud layer, and clips cloud overflow at that boundary, removing the residual scroll tail below Area 55 without moving any world.
- The merge multiplier badge now always uses the original brown/taupe disk with light text. The prior intentional white-over-Wild branch was the source of the inconsistent white/brown appearance and has been removed.
- Validation for these three fixes passed: targeted `run-mode.test.ts` (2 tests), targeted ESLint, `npm run type-check`, and `SKIP_NATIVE_BUNDLE_SYNC=true npm run build`.
- Physical installs and tests must always target `iPhone 13 blue`; other visible iPhones and iPads are not fallback targets unless the user explicitly changes this rule.
- Journey Worlds Forest Unit now includes three added framing clouds: one large upper-left, one large upper-right, and one medium at Forest's lower-right edge. Screenshot-guided tuning moved both right-side clouds another 60px left so their visible cloud mass sits close to the Forest island while preserving their diagonal vertical separation. All three are explicitly owned by world 1 so lock, enter/exit, and idle state stay with Forest.
- Journey Worlds hub first trims the paper/scroll tail below Area 55 by 8% of the stable viewport height, then removes another 40% of the measured remaining tail from the absolute cloud-layer box. The cloud layer was the real scrollHeight blocker; world positions, inter-world spacing, visible clouds, and Journey motion remain unchanged.
- Arcade gameplay must not show wild-star orbiting idle decoration; ownership now lives in `wild-stars.ts` across attach, async completion, and ticker cleanup.
- Journey orbiting stars remain enabled.
- Wild-star merge/reward flow remains connected.
- `npm run type-check` and `SKIP_NATIVE_BUNDLE_SYNC=true npm run build` passed after this change.
- This Arcade-only change is included in the latest bundled Stack to Six install.
- The repository now has an automatic continuity layer: `AGENTS.md` requires every new chat to read `PROJECT_CONTEXT.md` and this handoff before acting.
- Global skills `stack-to-six-project-context` and `cube-crash-dev-production-modes` now identify Stack to Six as the only normal iOS target and treat Kockice Crash as explicitly opt-in legacy infrastructure.
- Arcade first-play tutorial HUD cleanup now separates the active tutorial lock from the post-tutorial completion assist. Close/help controls restore immediately after step 4 and no longer remain disabled when entering Journey before an app restart.
- Regression coverage: `first-play-tutorial-hud-lock.test.ts` passes both active-tutorial and completion-assist-only states. Targeted ESLint, `npm run type-check`, and a no-sync production build also pass.
- The current web build, including centralized Arcade orbit cleanup, the corrected Journey Worlds bottom extent, the always-brown merge multiplier, the latest Forest cloud placement, and tutorial HUD lifecycle fix, has been synced into the Stack to Six `Web.bundle` and built successfully as `/tmp/stack-to-six-derived/Build/Products/Debug-iphoneos/Stack to Six.app`.
- On 2026-07-30 the current dirty-tree bundled build was verified with bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 717 runtime asset files, current intro assets, and the new Stage/Round terminology markers. After restarting a wedged local `CoreDeviceService`, install-over succeeded via Wi-Fi on `iPhone 13 blue` at installation URL ending `3575E5A8-314B-44EB-B75F-88B04F6526C6/Stack to Six.app`, and the exact bundle launched successfully. No other device or native shell was touched.

## Latest physical-iPhone state

- On 2026-08-04 the complete latest dirty-tree build passed `qa:fast` (64 tests plus lint/visual contracts), production Stack to Six bundle sync, `qa:ios`, signed Xcode build, and final `CFBundleIdentifier` verification. It includes partial-drag exit for every physically visible Homepage slide, the `320ms` Journey banner retract, and the final Hub composition with Beach at `334px` and Area 55 at `580px`. After one interrupted Wi-Fi transfer, install-over completed on `iPhone 13 blue`; Apple confirmed `com.taptapdesign.stacktosix.Stack-to-Six`, installation URL ending `805AF9A0-25FC-42B7-B169-08910E2E7D61/Stack to Six.app`, and successful launch. Existing app data was preserved; no other device or native shell was touched.
- On 2026-08-04 the approved warm double-sweep CTA shimmer and Homepage CTA exclusive hold-drag gesture build passed `qa:fast`, production bundle sync, `qa:ios`, signed Xcode build, and exact bundle-ID verification. After one interrupted Wi-Fi transfer, install-over completed on `iPhone 13 blue`; Apple confirmed `com.taptapdesign.stacktosix.Stack-to-Six`, installation URL ending `F7CAAE2B-20BF-432F-917B-007F4D282E14/Stack to Six.app`, and successful launch with live console. Retest press-and-hold Settings/Journey/Arcade/Collection CTA, move the finger sideways, release, and require no adjacent-slide exposure plus the normal CTA exit/route handoff.
- On 2026-08-03 the End Run sheet was fully detached from legacy button paint ownership: its active buttons now use neutral `data-end-run-action` roles instead of `.restart-btn`, `.new-card-btn`, and `.exit-btn`, so only the shared `.cc-cta__visual` can paint or animate them. Drag exclusion follows the neutral role and remains functional. A regression contract forbids those legacy classes from returning. Focused CTA tests pass 11/11, `type-check`, `qa:fast` (63 tests plus lint/visual contracts), bundled production build, and `qa:ios` all pass. The signed build installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, installation URL ending `742EC307-4947-4DC8-BE74-B039731E4E16/Stack to Six.app`, and successful launch with live console. Physical `New Game` exit verification is pending.
- On 2026-08-03 the bottom-sheet CTA duplicate-paint regression was fixed at the master CTA boundary: legacy screen-level `:active`/`:focus` rules can no longer paint or transform the `.cc-cta` layout root while `.cc-cta__visual` performs immediate exit. Focused CTA tests pass 11/11, `type-check`, `qa:fast` (63 tests plus lint/visual contracts), bundled production build, and `qa:ios` all pass. The signed Stack to Six install-over completed over Wi-Fi on `iPhone 13 blue` after one interrupted transfer; Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, installation URL ending `24EEB7F3-244E-4240-9AF1-F253CB6DEF95/Stack to Six.app`, and successful launch. Physical confirmation that no static CTA remains behind the animated CTA is still required.
- On 2026-08-02 the complete current dirty-tree Journey Hub build was rebuilt in bundled production mode, restored with the full raw asset tree, synced only to the Stack to Six `Web.bundle`, and passed `qa:ios`. The signed final app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` with 723 bundled runtime asset files. Install-over completed on `iPhone 13 blue` at installation URL ending `20B185C8-A21D-4D5D-861E-FD4B4A0995A4/Stack to Six.app`, and the exact bundle launched successfully. No other device or native shell was touched. This build includes fully visible/clickable future Worlds, banner ownership only for the World containing the single interim card, and locked regular Units remaining non-playable.
- On 2026-07-31 the Journey Hub/World non-destructive motion-reuse build passed `qa:fast` (22 suites / 240 tests), production build, exact Stack to Six sync, `qa:ios`, signed Xcode build, and final bundle/content verification. Wi-Fi install-over succeeded on `iPhone 13 blue`; Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `DB75AEA6-5F4B-4102-A0D0-7418F42CA281/Stack to Six.app`. The exact build is launched with live console. Retest Homepage → Journey Worlds and require one initial Hub activation followed by `surface-activation-reused`, no end-boundary listener restart, and no felt jerk.
- On 2026-07-31 the shared Exit/Score/3D bottom-sheet paper build passed `qa:fast`, production build, exact Stack to Six sync, `qa:ios`, signed Xcode build, bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 721 raw assets, installation URL ending `6CB99AFC-CDFE-4E41-A46C-B91EB04CCA39/Stack to Six.app`, and successful launch. The 3D sheet now uses the same `assets/modals/paper.png` surface as gameplay sheets; physical contrast parity remains `NEEDS PHYSICAL TEST`.
- On 2026-07-31 the first central app-level motion transition build was installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `EA865C25-0375-445B-A666-51ED6A09561E/Stack to Six.app`. The user completed Settings → 3D OFF → ON → Homepage exit and the captured post-transition samples stayed at `16.67–16.70ms` average, zero frames over `34ms`, and no monotonic GSAP/resource growth; a transient 35-timeline/185-child cleanup wave returned to zero in the next sample. The trace therefore passes the measured performance/buildup check. Final acceptance still needs the user's direct visual confirmation that the former jerk was no longer felt.
- On 2026-07-31 live launch diagnostics proved the failed reset was not a storage problem: the app read `forced:true` but returned `reason:"spatial-motion-disabled"`. The corrected decision order passed targeted tests and `qa:fast`, then the exact signed Stack to Six build was installed over Wi-Fi on `iPhone 13 blue`; Apple confirmed installation URL ending `A2746BB9-D0B4-4315-A600-73F194FA73D4/Stack to Six.app`. Next acceptance step: launch this build, use Settings → Developer → `Reset 3D Intro`, relaunch, and confirm the bottom sheet appears without a blue panel outline.
- On 2026-07-31 the focused-sheet outline and forced Developer reset fix passed `qa:full` (71 suites / 439 tests), exact Stack to Six sync, `qa:ios`, signed Xcode build, final bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 721 raw assets, installation URL ending `822FD436-DF3E-485F-BD3E-6D6B5C461F7E/Stack to Six.app`, and successful bundled-mode launch with live console attached. The user should press Settings → Developer → `Reset 3D Intro`, report `spreman`, and let Codex relaunch the connected process to verify the one-shot preview and absence of the blue panel outline. Verdict remains `NEEDS PHYSICAL TEST` until that run completes.
- On 2026-07-31 the latest 3D Motion star-scale and exact End Run subtitle-typography build passed `qa:full` (71 suites / 438 tests), exact Stack to Six sync, `qa:ios`, signed Xcode build, final bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. The first transfer was interrupted by Apple's remote install channel; the immediate retry succeeded. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 721 raw assets, installation URL ending `EF6346F4-A79F-49C3-876D-ADBC7620CE45/Stack to Six.app`, and successful launch. Physical scale/font feel remains `NEEDS PHYSICAL TEST`; use Settings → Developer → `Reset 3D Intro`, then fully relaunch to repeat it.
- On 2026-07-31 the latest 3D Motion bottom-sheet polish passed exact Stack to Six bundle sync, `qa:ios`, signed Xcode build, final bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 721 raw assets including all four tilt variants, installation URL ending `1EBC5CB3-855F-49DB-96DF-54A05B70CBEE/Stack to Six.app`, and successful launch. This build contains the orange `3D` title, End Game title typography, increased breathing room, clearer instruction, `Let’s Move` CTA, and 2.4s 80%-fade tilt crossfade. Physical appearance/cadence remains `NEEDS PHYSICAL TEST`; use Settings → Developer → `Reset 3D Intro`, then fully relaunch to repeat it.
- On 2026-07-31 the new launch 3D Motion bottom sheet passed `qa:full` (71 suites / 437 tests), exact Stack to Six sync, `qa:ios`, signed Xcode build, final bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. The first transfer was interrupted by Apple's remote install channel; the immediate retry succeeded. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 721 raw assets including `tilt1/tilt2`, installation URL ending `2F641F80-DE86-414C-89BC-67D06144C5BE/Stack to Six.app`, and successful launch. Physical appearance/cadence remains `NEEDS PHYSICAL TEST`; use Settings → Developer → `Reset 3D Intro`, then fully relaunch to repeat it.
- On 2026-07-31 the Developer one-shot 3D intro reset and half-strength, session-stable inverted gameplay-cube gyro passed `qa:full` (71 suites / 437 tests), exact Stack to Six bundle sync, `qa:ios`, signed Xcode build, bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, 717 raw assets, installation URL ending `E5EE6E1A-8A16-409B-B708-14E6D05DCDF1/Stack to Six.app`, and successful launch. To retest: Settings → Developer → `Reset 3D Intro`, fully relaunch the app, then confirm the branded modal appears during the TapTap/character launch hold. The experimental per-cell opposite-direction cube feel remains `NEEDS PHYSICAL TEST`.
- On 2026-07-31 the launch-owned 3D Motion modal and stronger gameplay vertical response passed `qa:full` (71 suites / 435 tests), exact Stack to Six bundle sync, `qa:ios`, signed Xcode build, bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `0B009360-E991-49E5-AC26-E3FD93AA3033/Stack to Six.app`; the exact bundle launched in bundled `app://localhost/index.html` mode with live console attached. The next check is visual/interaction acceptance of the modal while TapTap/character remain behind it, followed by gameplay vertical gyro feel.
- On 2026-07-31 the slow-drag ownership fix and all preceding dirty-tree gameplay/gyro work passed `qa:full` (70 suites / 432 tests), exact Stack to Six bundle sync, `qa:ios`, signed Xcode build, bundle/content verification, and Wi-Fi install-over on `iPhone 13 blue`. Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `E3D057D9-C175-4A2C-82DB-B135ECFA1758/Stack to Six.app`; that exact bundle launched in bundled `app://localhost/index.html` mode with live console attached. The next physical acceptance run should hold/move a cube slowly beyond three seconds, then exercise rapid regular merge-6 → wild merge-6 → wild-star/TNT chaining and verify no involuntary snapback or blocked wild-star drop.
- On 2026-07-31 the complete current dirty-tree Stack to Six build passed `qa:full` (69 suites / 430 tests), exact raw-asset sync, `qa:ios`, signed Xcode build, final bundle-ID/content verification, and Wi-Fi install-over on `iPhone 13 blue`. The app contains the tokenized regular merge-6 residue cleanup and exactly 30%-stronger gameplay-cube gyro (`16.25×16.25`). Apple confirmed bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and installation URL ending `6CF4A4FE-FDB6-48F0-B8DB-4A3C21DAC57B/Stack to Six.app`. Automatic launch could not be confirmed because the Wi-Fi CoreDevice control channel became unreachable immediately after the successful install and the phone then reported unavailable; the user can launch the installed app manually. Physical behavior remains `NEEDS PHYSICAL TEST`.
- On 2026-07-31 the spatial-motion polish build passed `qa:full` (68 suites / 425 tests), exact Stack to Six bundle sync, `qa:ios`, signed Xcode build, bundle/content verification, and install-over on `iPhone 13 blue`. The first Wi-Fi attempt was reset by Apple's CoreDevice transport before installation; the immediate retry succeeded and the exact bundle launched. Apple reported installation URL ending `B19CE994-069A-436B-A659-1904F05D8CC6/Stack to Six.app`. This build contains the 25%-stronger half-point-snapped gameplay cube gyro, gyro-owned wild preload bar, phase-two edge filtering, Forest-main +16px placement, stronger varied Forest Unit depth, and 30%-reduced two-axis Journey Hub motion. Physical visual/feel confirmation remains `NEEDS PHYSICAL TEST`.
- On 2026-07-31 the complete current dirty-tree Stack to Six bundle passed `qa:full` (67 suites / 420 tests), was synced with the full raw asset tree, passed `qa:ios`, and built successfully in bundled mode. The final signed app was verified as `com.taptapdesign.stacktosix.Stack-to-Six` and contained the current Settings Developer navigation and exact-reverse detail-stats markers. After restarting a wedged Apple `CoreDeviceService`, install-over and launch succeeded over Wi-Fi on `iPhone 13 blue`; Apple reported installation URL ending in `84080572-7B7A-4A90-839B-AB50AD3218E8/Stack to Six.app`. No legacy shell or other device was touched.
- On 2026-07-31 the current dirty-tree Stack to Six build containing the compact detail-stat enter, player-facing `3D Motion` toggle, isolated Developer Settings screen, and session-correct Journey motion permission explainer was synced, verified, built, installed over Wi-Fi, and launched on `iPhone 13 blue`. The signed bundle ID is `com.taptapdesign.stacktosix.Stack-to-Six`, the app contains 717 raw assets, and Apple reported installation URL ending in `9CA54359-2D23-41EC-916B-847FD0791C2E/Stack to Six.app`. `qa:full` passed 67 suites / 416 tests and `qa:ios` passed before build. Physical animation/permission interaction remains `NEEDS PHYSICAL TEST`.
- On 2026-07-30 the shared 40%-reduced Journey gyro plus active Homepage hero/CTA spatial-motion build was verified, installed over Wi-Fi, and launched on `iPhone 13 blue`. Apple reported installation URL ending in `BEDD8EB8-A3FC-45E3-B71E-D5326670CE12/Stack to Six.app`. `qa:full` passed 66 suites / 411 tests and `qa:ios` confirmed source/bundle freshness before the Xcode build. Physical Homepage slide/CTA depth and revised Journey intensity still require user feel verification.
- On 2026-07-30 the v3 Journey spatial explainer build was verified with bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, bundled `robo7.png`, and the `cc_journey_spatial_intro_seen_v3` runtime marker. Install-over succeeded via Wi-Fi on `iPhone 13 blue` at installation URL ending in `1AA7B4BC-D784-452A-B1B9-0E4A0DF94095/Stack to Six.app`, and that exact app launched successfully. The modal's visual styling, automatic post-Hub-enter timing, CTA interaction, and subsequent native permission sheet still require physical confirmation.
- On 2026-07-30 the post-v814 stack-motion tuning and complete current web tree were synced into the bundled Stack to Six shell, built successfully, and verified inside the final app with bundle ID `com.taptapdesign.stacktosix.Stack-to-Six`, raw `assets/tile.png`, and runtime markers `strength:1.3`, `reboundDurationSeconds:.13`, and `settleDurationSeconds:.18`. Install-over succeeded via Wi-Fi on `iPhone 13 blue` at installation URL ending `28F8138A-4B73-4F44-98A7-F2B101DC9D80/Stack to Six.app`, and that exact bundle launched successfully. The installation also contains the v814 final-merge guard, Journey scrollbar/layout/cloud fixes, and smooth detail-modal stats enter.
- The latest local Stack to Six build is installed and launched on `iPhone 13 blue`. It contains all current dirty-tree changes, including Journey **Stage** / Arcade **Round** terminology, centralized paper ownership, Journey bottom-decor enter, and shared cartoon stack/idle cube motion. The signed app was verified before install as `com.taptapdesign.stacktosix.Stack-to-Six` with 717 runtime assets.
- v813 was the previous online checkpoint installed on `iPhone 13 blue`; the latest local dirty-tree build now supersedes it and also contains the Journey interim idle/performance refactor, stronger gold burn and every-landing white dust, wider regular merge-6 smoke, and grouped board-intro haptics. Physical visual/feel verification remains the next step.
- The v812 hub-scroll guard build was installed successfully on `iPhone 13 blue` at installation URL ending in `52243FE9-49A4-4980-A9EA-46A196598CF0/Stack to Six.app`. Its trace proved the late mutation: after entering the hub at zero, iOS/layout attempted `scrollTop` values `85`, `40`, and `88`; each was immediately corrected to `0`. At the user's first manual touch the guard logged `manual-input`, removed all four listeners, and stopped correcting scroll. The user visually confirmed the screen now stays at the top and manual scrolling works correctly.
- The post-optimization Forest return comparison is physically verified. Both `journey-game-return` and the subsequent detail-modal return reported `reusedPreRevealPreparation: true` for all 88 targets. The measured 11-Unit Journey game return completed in `892 ms` across 50 frames with `17.28 ms` average, `36 ms` worst, one frame over `34 ms`, and zero frames over `50 ms`; the screen ended visible at opacity 1 with no console errors or stuck lifecycle flags. The user reported the return now feels “puno bolje”. Phase one is accepted; do not change its v701 motion values.
- The phase-one optimized Stack to Six build was verified with the exact bundle ID, `717` raw assets, and the new `world-enter-performance` marker, then installed successfully over Wi-Fi on `iPhone 13 blue`. Apple reported installation URL ending in `D072A8F4-8314-4550-B653-9E7B9F712D4F/Stack to Six.app`.
- A follow-up physical Journey trace on the currently installed diagnostic Stack to Six build completed successfully on `iPhone 13 blue`: Homepage → Journey → Forest → board 2 → gameplay → interim-board return. The initial world enter and the board return both primed the screen hidden, completed their full 11-Unit enter, and ended at visible opacity 1 without console errors or a stuck animation flag.
- The 30-second Journey gameplay window averaged `16.74 ms` across its one-second samples. Forty measured drags averaged `1.01 ms` in the move handler, peaked at `4 ms`, and had zero processed moves over `8 ms`; observed regular stack/merge windows peaked at `28 ms`, and `reducedFx` remained false. There was one isolated `78 ms` frame during board enter and two smaller isolated hitches (`49 ms` and `41 ms`); gameplay was not under sustained frame pressure. The user reported the run felt good.
- The completed diagnostic Arcade trace captured 147 ordered gameplay events across Stage 1 and Stage 2: 63 merge entries, 40 merge-6 decisions, 23 non-merge-6 early checks, 8 wild spawns, 7 special merge-6 FX selections, 5 level-end decisions, and one clean-flow dispatch. Every Arcade wild spawn reported `orbitPresent: false`, including core stars and the `cubero` star variant; the Arcade orbit suppression is physically verified.
- Stage 1's final pair was a stack-depth-2 regular value 5 plus `wild-magnet`. The merge-6 resolver saw exactly two active physical tiles, no blocker, and no pull target; it returned `complete / arcade-stage / final_wild_merge6`, selected the magnet FX branch, then dispatched clean flow and advanced to Board 2. The previously reported missing-final-merge handoff did not reproduce in this trace. Stage 2 later correctly resolved a separate `4 + 3` no-moves state as fail. TNT did not spawn in this run, so its final-merge path remains test-covered but not physically exercised here.
- Performance for the diagnostic run remained healthy over 66 drags: maximum average move-handler cost `2.75 ms`, maximum individual processed move `8 ms`, one over-8 sample, one renderer frame over `34 ms`, and worst ticker frame `40 ms`. This is a small isolated hitch, not sustained lag; no 80–120 ms stall occurred.
- The first post-install Arcade physical trace showed consistently cheap touch processing (`0.2–4 ms`, no move handler over `8 ms`) and no severe renderer hitch; observed ticker maxima were about `31 ms`. That build exposed only DragPerf and haptic telemetry, so wild identity, merge-6 FX routing, and final handoff could not be proven from the console.
- A narrow iOS-only `[CC_ARCADE_TRACE]` diagnostic channel was added in `src/utils/ios-arcade-gameplay-trace.ts` and connected in `app-core.ts` at merge entry, early final-pair classification, merge-6 resolver decision, merge-6 FX selection, completed wild spawn/orbit state, level-end decision, and clean-flow dispatch. It does not change gameplay decisions or effects. Type-check, targeted ESLint, and 68 focused final-merge/run-mode tests passed.
- The diagnostic build was synced with the complete raw assets tree, built successfully, verified with the exact Stack to Six bundle ID, 717 raw asset files, and the trace marker inside the final app. Install-over then completed successfully on `iPhone 13 blue`; Apple reported the new installation URL ending in `03B73FF5-645F-4633-81A2-F50D40700763/Stack to Six.app`. The next step is one short Arcade physical trace and analysis of the `[CC_ARCADE_TRACE]` sequence.
- After the user reset the phone's Wi-Fi on 2026-07-28, a long install-over attempt completed successfully for `/tmp/stack-to-six-derived/Build/Products/Debug-iphoneos/Stack to Six.app`. Apple reported bundle ID `com.taptapdesign.stacktosix.Stack-to-Six` and a new installation URL ending in `EBBC07FB-6902-4B94-8019-BA0DF06AFE73/Stack to Six.app`. The exact bundle then launched successfully in bundled `app://localhost/index.html` mode with the native console attached. This is the first confirmed phone install containing the current Journey bottom trim, centralized Arcade wild-star orbit suppression, always-brown multiplier badge, Forest cloud placement, tutorial HUD lifecycle fix, and current final-merge work. Physical visual verification of Journey Worlds and Arcade orbit behavior is the next step.
- On 2026-07-28 the installed `Stack to Six` bundle was launched successfully on `iPhone 13 blue` and attached to the native console, but it did not emit the current `JourneyV700Flow` hub diagnostics during a user-confirmed Journey Worlds reproduction. Combined with the visible old Journey bottom extent and Arcade wild-star orbit, this is evidence that the phone still contains older web content.
- The ready `/tmp/stack-to-six-derived/Build/Products/Debug-iphoneos/Stack to Six.app` was re-verified against the current `dist/index.html`, raw `assets/tile.png`, bundled mode, and exact bundle ID. Three install-over attempts on the unlocked/available `iPhone 13 blue`—including both CoreDevice ID and hardware UDID addressing—failed in Apple's remote install transport with `IXRemoteErrorDomain code 6 / Connection interrupted`. Do not claim the current Journey trim or Arcade orbit suppression is installed until a later attempt returns an explicit install success and the exact bundle is relaunched.
- A freshly built Stack to Six development app was installed successfully under `com.taptapdesign.stacktosix.Stack-to-Six`.
- Before that correction, the phone had the wrong Kockice Crash app; device inspection exposed the bundle mismatch.
- The installed Stack to Six build contains `taplogo.png` and all eight current random `lik-*` intro characters.
- Automatic launch was blocked once because iOS required explicit trust for the development profile. If it remains blocked, the user must approve it under Settings → General → VPN & Device Management → Developer App.
- Do not assume this installed app contains work performed after the most recent native sync/install. Rebuild and install over Stack to Six when the user asks for phone verification.

## Current UX decisions worth preserving

- The startup background is one continuous shared paper surface; no white/gray interstitial frame.
- Intro sequence uses `taplogo.png` plus one random `lik-*` character, not the retired combined preload art or a separate Stack to Six logo phase.
- Journey Worlds hub and individual Journey world scroll behavior must remain distinct as documented in `PROJECT_CONTEXT.md`.
- Journey enter/exit/nav motion must follow `JOURNEY_ANIMATION_CONTRACT.md`.
- Arcade gameplay must not show orbiting wild-star idle decorations; Journey still may.
- Post-tutorial completion assist may stay active until the tutorial board ends, but it must never keep HUD close/help navigation locked.

## How to maintain this file

Update only concrete continuation state: branch/checkpoint, meaningful uncommitted files, latest validated behavior, and what is or is not installed. Move lasting architecture or workflow facts to `PROJECT_CONTEXT.md`.
