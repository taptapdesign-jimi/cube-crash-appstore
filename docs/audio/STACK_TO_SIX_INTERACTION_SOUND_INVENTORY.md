# Stack to Six interaction sound inventory

Updated: 2026-09-15

## Current approved integration

Sound design has been reset to these user-supplied gameplay cues:

- Arcade Nice Job / stage-clear cue: the first visible headline letter starts `assets/sound/Clean board/happy victory sax .wav` once at **0.76 action / 0.456 effective** and the existing `assets/sound/Clean board/applause.wav` once at **0.60 action / 0.36 effective**. The first NN Round digit's exit tween starts a strong **240ms** linear fade of both result voices to silence on decoded/WebAudio and HTMLAudio fallback, then stops them. The thumbs-up image's enter tween starts only `assets/sound/cjelina flip/flip soft.wav` at **1.00 action / 0.60 effective**, the single Journey artwork-return woosh, with no `menu flip.wav` or `flip.wav` layers; its voice is separate from the NN fade. The resumed Round-only cue triggers no celebration. The stage-clear owner preloads all three, obeys Settings Sounds OFF, and stops/rewinds them on completion, cancellation, replacement or Sounds OFF. Source files remain unchanged.

- Shared active-SFX master: `0.60`, reducing every currently used effect by exactly 40%; soundtrack/music is a separate owner and is not multiplied by this SFX gain
- Shared CTA activation cue: every CTA registered through the canonical `registerCta()` owner starts `assets/sound/CTA/deeper.wav` at `0.30 action / 0.18 effective`, plus `pumb.wav`, `cta-splat.wav` and `cta squishy.wav` at the cleanup-balanced `0.50 / 0.30` each. All four start together on first valid pointer-down; keyboard activation starts the same stack once from its accepted path. Replacement touches stop preceding voices, and Settings Sounds OFF stops active or pending playback. Homepage nav icons and Settings toggles retain their independent `poping.wav + wood click.wav` pair.
- Homepage navigation icon cue: only the three small Journey, Arcade and Settings slider-navigation icons play `assets/sound/CTA/poping.wav` at `0.50 action / 0.30 effective` plus the updated `wood click.wav` at `0.70 / 0.42`, starting once on pointer-down. This is independent of the unchanged shared CTA cue; Settings Sounds OFF also stops active or pending navigation voices.
- Settings toggle cue: every Settings switch reuses the same `poping.wav + wood click.wav` pair once on pointer-down while Sounds are enabled, without changing its existing bounce or setting-change ownership.
- Journey Hub World-open cue: an accepted Forest, Area 55 or Beach World-card activation reuses the complete four-layer CTA stack once at the committed Hub-to-World open boundary: `deeper.wav` at `0.30 action / 0.18 effective`, plus `pumb.wav`, `cta-splat.wav` and `cta squishy.wav` at `0.50 / 0.30` each. Drag-rejected, debounced and already-opening/already-open requests do not replay it.
- Journey Worlds ambient loop: the visibly presented multi-World Hub starts the supplied `assets/sound/worlds/crumbleworlds.wav` once. Hub level is `0.75 action / 0.45 effective`; an accepted Forest, Beach, Area 55 or future registry-backed World activation ramps the same still-playing voice linearly for `1.0s` to exactly 30% of that level (`0.225 action / 0.135 effective`) without restarting or rewinding it, keeps it active through the full first `5.0s` after the click, then fades it linearly for `1.0s`, stops and rewinds it. Drag-rejected, debounced and already-opening/already-open requests cannot schedule the fade. A real World-to-Hub return before the deadline cancels the pending fade and ramps the same voice back to 100% over `1.0s`; a direct World entry starts immediately at the reduced level so it cannot flash at full volume. If Play or Continue reaches the Journey Board Transition sooner, that overlay mount cancels the delayed schedule and starts the same immediate `1.0s` fade. Arcade remains excluded; Settings Sounds OFF and ordinary Journey cleanup stop it immediately. Forest's separate World ambience keeps its independently owned Play/Continue fade.
- Homepage manual-swipe cue: an accepted left/right finger or mouse swipe that resolves to a real adjacent Homepage slide starts the updated `assets/sound/UI /sliders/super swoosh.mp3` at the proportionally reduced `0.50 action / 0.30 effective`. The decoded buffer and AudioContext are silently primed from the drag-start user gesture, then playback starts synchronously on accepted finger/mouse release immediately before the slide snap begins. Nav-icon taps, threshold failures and elastic edge snap-backs do not trigger playback.
- Homepage slider hero-image cue: an accepted tap or keyboard activation on the main Journey, Arcade or Settings slider image reuses the complete shared CTA stack (`deeper.wav + pumb.wav + cta-splat.wav + cta squishy.wav`) through its existing bounded owner. Swipe/drag does not trigger it, and the three small navigation icons retain their separate cue.
- Back/X close cue: navigation Back buttons, navigation X controls, HUD X and shared in-game modal X buttons play only `assets/sound/CTA/deppimpact.wav` at the reduced `0.60 action / 0.36 effective` on pointer-down. One delegated owner covers dynamically mounted DOM controls and avoids attaching duplicate listeners; the live Pixi gameplay HUD X calls that same bounded Close owner directly after its terminal guard and tap lock accept the press. The preserved `taptap.wav` is not part of this Close cue or the current CTA pair.
- HUD Exit modal enter cue: after an accepted gameplay-HUD X activation creates the hidden `Exit Game?` / `Exit Stage?` modal, `assets/sound/UI /modal/modal enter.wav` starts immediately at `1.00 action / 0.60 effective`, before the next animation frame makes the modal visible and begins its unchanged 650ms enter animation. Blocked, duplicate and already-open requests remain silent. Journey Units, Journey cards and World/detail modals are explicitly excluded.
- Clean Board result sequence: mounting the accepted Clean Board overlay starts `assets/sound/Clean board/applause.wav` at **0.60 action / 0.36 effective** and the user-supplied `assets/sound/Clean board/happy victory sax .wav` at the reduced **0.76 action / 0.456 effective**. The current **5.0s / 48kHz / 16-bit stereo PCM** victory cue is a dedicated happy-result sax, while the applause owns the longest nominal **10.0s** result layer. At result start, the current theme/adaptive bed fades to silence over **420ms** while its timeline keeps advancing. Restoration is not mount-timed: both applause and sax supply actual decoded/WebAudio or media-element completion receipts, so cold decode, AudioContext resume and media startup delays extend the hold with real playback; the soundtrack resumes only after both owners ended, were unavailable or were explicitly stopped. Natural completion then fades over **1000ms** to Journey/theme **0.68** or Arcade Calm **0.528**. Activating Continue or Play Again first stops every Clean result voice, settles even queued cold starts and restores the Journey gameplay target **0.2244** (Arcade retains Calm); Exit restores the menu target. The destination becomes the sole audio owner, with no end-of-cue overlap or intermediate gain jump. Route changes and Music OFF also settle the bounded owner. The generated v1/v2/v3 victory sources and `crowd.wav` remain preserved but are disconnected from runtime. The initial main-score, Combo and Efficiency drain/count phases use the differently mastered counter files as one perceptually balanced pair: `money count.wav` at **0.95 action / 0.57 effective** plus the approved 1.363646s `fastpointsstack.wav` at its unchanged **0.60 action / 0.36 effective**. `money count2.wav` and `points.wav` remain preserved but are disconnected from preload and runtime. Both active counter layers receive the exact GSAP count duration and, when that duration is shorter than a source file, end with the shared fast 120ms fade instead of a hard cut; decoded and HTMLAudio fallback paths share this contract. Each earned yellow Star now plays `boinb.wav` at **0.80 action / 0.48 effective** and layers one of `harp1.wav`, `harp2.wav` or `harp3.wav` at **0.40 action / 0.24 effective** on its own established staggered bounce frame. The three harp files are shuffled into a no-repeat permutation for each Clean Board presentation, and the immediately previous three-Star arrangement cannot repeat. Only `index < numStars` is audible, so gray empty Stars never play either layer. Each registered Clean Board CTA starts the byte-identical generic `assets/sound/magnet pul lforce/pull1.wav` immediately before its own `controller.enter()` bounce-in; primary and secondary buttons have independent voices and keep their existing 350ms visual stagger. Every cue obeys Settings Sounds OFF and preloads at Clean Board construction.
- Fail Screen result and CTA sequence: mounting the accepted Fail overlay starts the user-supplied `assets/sound/fail board/six fail.wav` at the unchanged dedicated **0.90 action / 0.54 effective** mix, independently of the Clean Board victory melody's current gain. The current nominal **3.0s / 48kHz / 16-bit stereo PCM** cue is the dedicated Six fail result. At sax start, the current theme/adaptive bed fades to silence over **420ms**. Restoration follows the sax voice's actual decoded/WebAudio or media-element `ended` receipt rather than a timer started at modal mount, so cold-start delay cannot make music return before the sax completes. The bed then fades in over **1000ms** directly to the stable non-gameplay target. Forest gameplay ambience is stopped before Fail presentation. Play Again and Exit each retain the same `assets/sound/magnet pul lforce/pull1.wav` used by Clean Board immediately before their actual registered `controller.enter()` calls, at `1.00 action / 0.60 effective`; activating either CTA stops the sax and both CTA voices before the new board or route takes audio ownership. Concurrent Fail show requests join the active modal promise instead of retriggering the result. Every earlier Fail source, including the generated v3 cue, remains preserved but disconnected. Settings Sounds OFF, unavailable playback and explicit full-owner reset remain terminal. Existing CTA activation/click cues and motion remain unchanged.
- Journey Cjelina visible-card flip cue: when the prepainted card begins its real visible grow-and-spin spatial flight into the Journey Card modal, it starts `assets/sound/cjelina flip/menu flip.wav`, `flip.wav` and `flip soft.wav` together once at `0.70 action / 0.42 effective` each. X/Close, white stats/CTA-face drag release, Play and Continue reuse this reduced three-layer flip cue at the start of the return flight. A committed downward drag from the front artwork keeps the illustration facing forward without a flip and starts only the existing manual `flip soft.wav` whoosh at return-flight start (`1.00 action / 0.60 effective`). The former `swoosh back.wav` remains preserved as an asset but is disconnected from runtime playback. The automatic post-game card reminder also reuses the shared reduced entry stack on its first visible flight frame and stops it on interruption. Every later accepted manual front/back flip of the already-open card remains separate at one `flip soft.wav` with `1.00 / 0.60`. A pointer drag may preview or commit the visual handoff while contact remains down, but its manual sound is deferred until the real accepted `pointerup`; `pointercancel` stays silent. Tap and keyboard flips retain immediate accepted-start playback. All cue paths obey Settings Sounds OFF, preload at their Journey presentation boundary, replace their own matching voices and stop on cleanup.
- Forest Journey World ambiance: only the visible Forest World screen layers `assets/sound/worlds/Forest/soft bees ambiance.wav` and `forest nature sounds.wav` as continuous loops. Both retain their `0.85` base action level and receive the requested exact **1.20 local boost**, producing **0.612 effective** gain after the shared SFX master. Opening a Cjelina card leaves the ambience running behind its modal; an accepted Play or Continue handoff fades both layers linearly for exactly `1.5s`, then stops and rewinds them. Forest X/back, a switch to Hub or another World, Journey cleanup, and Settings Sounds OFF stop both loops immediately. Beach, Area 55, Journey Hub, Arcade and gameplay never start this owner. The decoded-buffer path owns native loop and gain-ramp playback after context wake; HTMLAudio owns only the unavailable-context fallback.
- Forest gameplay loop: only committed Journey Forest gameplay for boards 01-10 starts `assets/sound/worlds/Forest/gameplay/forest-gameplay-sound.wav` as a continuous loop, immediately after the prepared gameplay surface becomes visible. Fresh Play and saved Continue share that same entry boundary; Journey Beach/Area 55 and Arcade never start it. The quiet supplied source now uses `0.90 action / 0.54 effective` gain after the shared SFX master. The exact completion of the Cleared residual pop-out owner, after remaining dice and ghost placeholders have been removed, begins one linear `2.0s` fade; the voice then stops and rewinds. Ordinary board replacement/exit and Settings Sounds OFF stop it immediately, while Cleared-route cleanup preserves an already-active bounded fade until it completes. The supplied source is preserved unchanged as 44.1kHz 16-bit stereo PCM.

## Music

- Global runtime sources: the first app launch begins with `assets/sound/soundtrack/theme-loop-v1/SIx-theme-original-intro-bridge.wav`, an exact lossless two-bar extraction from the preserved `assets/sound/soundtrack/SIx theme.wav`. Its first **2.0583125s** are the intended original intro, while the complete second bar is retained as an uninterrupted bridge over the handoff boundary. It then hands off once to `assets/sound/soundtrack/theme-loop-v1/SIx-theme-seamless-loop.wav`. The reproducible generator owns both derived files and never modifies the supplied master.
- Menu/Journey playback: `SIx-theme-runtime-intro-loop.wav` is one **59.6910625s / 48kHz / 24-bit stereo PCM** master containing the exact original first bar followed immediately by the existing 28-bar seamless loop. One Web Audio `AudioBufferSourceNode` starts at frame zero with one soundtrack `GainNode`; `loopStart` is exactly **2.0583125s / frame 98,799** and `loopEnd` is **59.6910625s / frame 2,865,171**. The original launch intro therefore crosses into the loop as adjacent PCM samples inside one buffer, with no second player, crossfade, timer, live seek or transition mix. Later wraps skip the intro and use only the accepted circular loop region. The runtime buffer begins decoding during launch initialization, and the existing pointer-release retry resumes its dedicated AudioContext when autoplay is blocked. Journey Board Transition retains its accepted 20% theme level (`0.136`) and `320ms` soft tail; only after the transition completes does Journey gameplay rise over `320ms` to the 10%-louder 33% level (`0.2244`).
- Arcade adaptive layers: `assets/sound/soundtrack/adaptive-music-v3/gameplay-bed-calm-01.wav` enters at the 10%-louder `0.528` after the Round cue; the first committed ordinary or Wild/Special Merge-6 in each round promotes to the 10%-louder `gameplay-bed-active-01.wav` at `0.594`. Both are 24-bar, 116.6 BPM, 48kHz 24-bit stereo loops derived from the current lossless `SIx theme.wav`. Active inherits Calm's playback position and crossfades on the next musical bar over one full bar (approximately `2.058s`) rather than cutting mid-phrase.
- Arcade result/menu mix: Arcade Stage Clear lowers the current adaptive bed to `0.10` over `420ms`. Clean Board and Fail Screen then fade the still-advancing adaptive bed to silence over `420ms`, hold it silent until their actual result voices have settled, and restore directly to the stable Arcade Calm level `0.528` over `1000ms`. Each new Arcade round retains its existing Calm entry behavior; returning to a menu fades the adaptive voice out and restores the preserved full theme to `0.68` without rewinding the menu theme position.
- Loop boundary: the 57.63275s / 28-bar / 48kHz 24-bit stereo runtime master uses a one-bar (2.0583125s) equal-power circular overlap and native continuous playback. There is no end fade, silence, `ended` restart or loop fade-in; the final crossfaded sample flows directly into the first sample.
- Background policy: the current theme or adaptive Arcade owner pauses while the app is hidden and resumes with a short fade when visible; Music OFF stops and rewinds all soundtrack-owned voices
- Older soundtrack and result-sax files remain preserved but are no longer runtime sources
- Pickup source: `assets/sound/merge 6/woosh.mp3` from the measured `0.045s` perceptual-onset offset at authored `1x`, `0.432 action gain`, now `0.2592 effective gain` after the shared master (8% above its preceding level)
- Pickup trigger: once when drag ownership is successfully acquired for any ordinary, Wild or registered Special die; blocked pointer-down attempts stay silent
- Rejected-return source: the same `assets/sound/merge 6/woosh.mp3` from the measured `0.045s` perceptual-onset offset at authored `1x`, now restrained to `0.40 action / 0.24 effective` so a rejected move settles slightly below the `0.2592` pickup level instead of dominating it
- Rejected-return trigger: once when a released/cancelled dragged die starts its canonical snap-back; successful stack and merge paths stay silent
- Ordinary stack sources: simultaneous `assets/sound/merge 6/wood.wav` at `1.50x / 0.384 action gain / 0.2304 effective gain` (approximately 0.853s wall time; cumulative 40% then 20% reductions) plus restored `assets/sound/merge 6/stack.mp3` at `1.0x / 0.80 action gain / 0.48 effective gain` (20% below its preceding ordinary-stack level)
- Ordinary stack scope: both layers play once on only a committed ordinary sub-six stack; Merge-6 reuses its own separately triggered copy of `stack.mp3`
- Stack trigger: a committed ordinary die-on-ordinary-die stack below six
- Ordinary Merge-6 sources after the shared master remain unchanged: `merge six obicna.mp3` at `1.30x / 0.30 effective gain`; simultaneous longer `merge6 crash.mp3` at `1.30x / 0.1632 effective gain`, shortened to an `892ms` wall-time cutoff with a `120ms` fade-out; simultaneous `merge6 boom.mp3` at `1.0x / 0.36 effective gain`; and a simultaneous Merge-6-only copy of the existing `stack.mp3` at `1.0x / 0.80 action gain / 0.48 effective gain`. Wild/Special owners reuse the same timing and sources at a dedicated **0.65 foundation scale**: effective gains `0.195`, `0.10608`, `0.234` and `0.312`. This keeps ordinary Merge-6 weight intact while leaving headroom for each Special's authored identity.
- Merge-6 trigger: the existing committed `effSum === 6` path, only when both source and destination dice are ordinary
- Core Wild Star Merge-6 reuses the dedicated 65%-level Special foundation: `merge six obicna.mp3` at `1.30x / 0.325 action / 0.195 effective`; `merge6 crash.mp3` at `1.30x / 0.1768 / 0.10608`, with the identical `892ms` cutoff and `120ms` fade; `merge6 boom.mp3` at `1.0x / 0.39 / 0.234`; and `stack.mp3` at `1.0x / 0.52 / 0.312`. Over that base it selects exactly one magic variant per committed event from an even four-way random pool: existing `long_magica_happy_ac_#2-1788980185281.wav` and `long_magica_happy_ac_#3-1788980189939.wav` remain at `1.0x / 0.6667 / 0.40`, while the supplied `star1.wav` and `star2.wav` use `1.0x / 0.80 / 0.48`. `magicle_sparkle_for__#1-1788980027254.wav` remains an additional layer at `1.0x / 0.51 / 0.306`, beginning exactly `200ms` after the event. Sparkle is therefore proportionally 15% quieter than its preceding `0.60 / 0.36` setting. The former Star-specific `big_tnt_boom_explosi_#1-1788980586466.wav` remains preserved on disk but is no longer a runtime layer. Literal filename hashes remain unchanged and are referenced as `%23` in runtime URLs so browser fetch/media decoders receive the complete WAV path.
- Core Wild Star trigger: the same committed `effSum === 6` branch, but only when at least one merge-entry tile is the unregistered core `wild` Star and neither tile carries a registered Special-die variant; Bee, Kanta and every other registry-backed Special die remain excluded even when they share the `wild-star` archetype
- Robo Cube Merge-6 adds the three supplied Robo-folder layers simultaneously at the committed `robo-cube` Merge-6 event: `bibiribi.wav` at `1.00 action / 0.60 effective`, `robo1.wav` at `1.00 / 0.60`, and `outro zing.wav` at the requested `0.70 / 0.42`. The dedicated Robo owner preloads for Journey and Arcade, queues cold decode/context wake, obeys Settings Sounds OFF, and stops on board cleanup or replacement; other Special variants remain excluded.
- Fish Merge-6 reuses the 65%-level four-layer Special foundation, then starts the supplied `fish0.wav`, `fish1.wav`, `plomp.wav` and `fish speaks.wav` immediately. `fish2.wav` starts at exactly `+1.8s`, the 50% point of the 3.6-second Fish finale contract. Fish 0 and the peak-0dBFS Fish Speaks layer use `1.0x / 0.40 action / 0.24 effective gain`; Fish 1, Fish 2 and Plomp each use `1.0x / 0.6667 / 0.40`. All five supplied WAV byte streams remain unchanged.
- Fish sound trigger: the committed `effSum === 6` branch only when merge-entry variant provenance identifies `fish`; core Wild Star, Bee and every other registered Special remain excluded. Sounds OFF, board cleanup and a replacement Fish event cancel the pending midpoint layer and stop all Fish-owned voices.
- Every committed core Wild or registered Special Merge-6 starts the shared `assets/sound/merge 6/poof1.wav` and `poof2.wav` pair exactly once at commit, both at the restrained `0.55 action / 0.33 effective`, while ordinary Merge-6 remains excluded. This covers core Wild Star/Juice/Magnet/TNT and every registered visual variant. The shared pair owns preload, Sounds OFF, replacement and board cleanup independently from feature-specific sound families, but now sits beneath rather than masks feature-specific voices.
- Core Juice Merge-6 adds only the five supplied `assets/sound/Wild and special kockice/juice/` cues over the unchanged shared foundation and poofs. The first visible upward bubble burst starts `bubble.wav` at **0.55 action / 0.33 effective**. `introbubble.wav` enters **0.8s after that first visible burst** at **0.70 / 0.42**. `mini2.mp3` starts with the straw flight at **0.65 / 0.39**, `mini3.wav` with the lid at **+0.26s / 0.65 / 0.39**, and `mini4.wav` with the cup at **+0.55s / 0.50 / 0.30** relative to the prop-flight start. These mini cue assignments follow the three supplied filenames in flight order. The dedicated owner preloads on board entry and runs only for generic Juice props/default upward bubbles. Natural success lets committed one-shot tails finish; replacement, interruption, board exit and Sounds OFF stop all five voices.
- Juice `bubble.wav` runtime tail: the supplied **4.56s** source is unchanged; only this long cue now ends at **3.56s**, one second early, with a linear fade through its final **0.5s** (3.06–3.56s). Decoded Web Audio schedules the gain ramp and stop from its actual start; HTMLAudio fallback follows media `currentTime`, reaches zero gain and rewinds at the same endpoint. `introbubble.wav` and the three mini prop cues retain their source lengths, triggers and gains.
- Bee sound trigger: the committed `effSum === 6` branch only when merge-entry variant provenance identifies `bee`. The 65%-level Special foundation starts at merge entry; `bee start.wav` starts immediately at `0.75 / 0.45`, `BEE.wav` at `0.70 / 0.42`, and `brushing.wav` at `0.80 / 0.48`. At 40% of the four-second Bee finale (`+1.60s`), only `happy bee.wav` starts at `0.20 / 0.12`. The shared main-folder Wild/Special `poof1.wav` and `poof2.wav` pair also starts at merge entry. `fishy wee.wav`, Bee-folder poof copies, `smile.wav`, `smile bee.wav`, `leaves trail.wav`, and `leaves.wav` are excluded from Bee-local runtime and preserved on disk. The dedicated owner obeys Sounds OFF and stops on board cleanup/replacement.
- Beach Ball Merge-6 reuses that same 65%-level four-layer Special foundation and has one fixed Ball sequence with no random variants: `ball1.wav`, `ball2.wav` and `ball4.wav` all start immediately at `1.0x`, followed by the derived `ball5-60pct.wav` at exactly `+100ms` and **1.8x playback speed** (80% faster). The derived Ball 5 retains the first **60% / 1.20s** of the original two-second `ball5.wav`, with an 8ms anti-click fade at the cut; the supplied original remains preserved and is not used at runtime. All four Ball layers retain `0.6667 action gain / 0.40 effective gain`. `ball impact.mp3` is an additional invariant immediate layer at authored `1.0x / 0.50 action gain / 0.30 effective gain`.
- Beach Ball sound trigger: the committed `effSum === 6` branch only when merge-entry variant provenance identifies `beach-ball`; Bottle, core Wild Star and all other registered Specials remain excluded
- Beach Ball four cube impacts: the existing exact `onImpact` contact on the four bonus targets now plays the shared core-TNT/Flower shuffled mini-impact pool, choosing four different sources from `mini crash1.wav` and `mini2`–`mini5`. Ball keeps its 0/260/560/900ms impact stagger and its separate authored Merge-6 entry package. Position action gains are 0.48/0.56/0.63/0.56 (effective 0.288/0.336/0.378/0.336 after the 0.60 SFX master); shuffled `mini2` is half its position gain and `mini5` stays fixed at 0.25 action / 0.15 effective. The shared owner preloads, obeys Sounds OFF, and stops on Ball replacement/board cleanup.
- Core TNT Merge-6 reuses the 65%-level four-layer Special foundation, including its `1.0x / 0.52 action / 0.312 effective` `stack.mp3` layer. TNT's own dense entry package has a separate **0.65 custom-bus scale**: the `0.56` authored-base `tnt3.wav` crash starts exactly `+500ms` at **0.364 post-bus action / 0.2184 effective**; immediate `horn.wav` moves from `0.80` authored base to **0.52 / 0.312**; immediate `wood break.wav` and the `+100ms` `wood ssplinter.wav` each move from `0.90` authored base to **0.585 / 0.351**. The foundation, delays, playback rates and sources are unchanged. Each of the up to four later core-TNT bonus cube impacts chooses one unique source from the five-file `mini crash1.wav`, `mini2.mp3`, `mini3.wav`, `mini4.wav`, `mini5.wav` pool. Those later impact gains remain unchanged at `0.48 / 0.288`, `0.56 / 0.336`, `0.63 / 0.378`, and `0.56 / 0.336` (action/effective); `mini5.wav` retains its fixed `0.25 / 0.15` override and `mini2.mp3` retains its half-level override. One candidate remains unused per four-impact event. The mini cue fires at the canonical cube-impact boundary shared with haptic, smoke/shards, the 5% Wild progress award and the HUD-star handoff.
- Core TNT sound trigger: the committed `effSum === 6` branch only when exactly one merge-entry tile is the unregistered core `wild-tnt` and the other is an ordinary die; Beach Ball, Flower, Cubero, LaserGun and every other registry-backed TNT-archetype Special remain excluded
- Barrel (`barell`) Merge-6 sound trigger: committed `effSum === 6` with Barrel variant provenance at merge entry. It shares the reduced Special four-layer foundation, but Barrel alone raises its `merge6 boom.mp3` layer by **30%** from **0.39/0.234** to **0.507 action / 0.3042 effective**; ordinary Merge-6 and other Specials keep their gains. It also shares the Wild/Special `poof1/poof2` pair and the shuffled core-TNT four-impact mini pool. For Barrel alone, the four target-cube impacts and their selected mini sounds begin **+200/+400/+600/+800ms** from bonus-target scheduling, keeping their **200ms** stagger and the visual/sound/haptic contact together. Its own folder supplies TNT-timed `horn.wav` and `wood break.wav` immediately at **0.52/0.312** and **0.585/0.351** action/effective; `wood ssplinter.wav` at **+100ms, 0.585/0.351**; and `tnt3.wav` at **+500ms, 0.364/0.2184**. Barrel adds `planks.wav` **200ms after the visible Barrel TNT overlay starts** at **0.70 action / 0.42 effective**, `puf1.wav` immediately at **0.55/0.33**, and the hotter `puf2.wav` at **+100ms, 0.35/0.21**. The Barrel-local `poof.wav` starts once at **50% of the visible smoke-frame sequence** measured from first cloud enter to final cloud exit (about **+0.91s** for the 12-frame TNT smoke), at **0.35 action / 0.21 effective**. All are `1.0x`, independently bounded, prewarmed on Arcade/Journey entry, and stopped on replacement, board cleanup or Sounds OFF. The eight live Barrel sources remain separate from the unregistered core TNT owner.
- Flower Merge-6 reuses the 65%-level four-layer Special foundation, while its four simultaneous local entry layers pass through a separate **0.65 custom-bus scale**. `bush0.wav` moves from `0.80` authored base to **0.52 post-bus action / 0.312 effective**, `bush2.wav` from `1.00` to **0.65 / 0.39**, `bush3.wav` from `0.35` to **0.2275 / 0.1365**, and Flower-local `boom.wav` from `1.00` to **0.65 / 0.39**. `bush1.wav` is explicitly excluded from runtime and remains preserved on disk. The later milestone voices remain unchanged: `leaves.wav` plays once at exactly 90% of the real Flower sprite-sequence duration at `1.0x / 0.80 action / 0.48 effective`, and `spark.wav` starts only from the real Flower sprite-sequence completion callback at `1.0x / 0.60 / 0.36`. Flower's up to four later bonus-cube explosions retain the exact same event-shuffled five-source mini-impact pool, per-impact gains, `mini2` half-level override and fixed `mini5` level as core TNT, without the leaves layer.
- Flower sound trigger: the committed `effSum === 6` branch only when merge-entry variant provenance identifies `flower`; all Flower voices and the shared mini-impact sequence obey Settings Sounds OFF, preload at Arcade/Journey entry, and stop on board cleanup or replacement.
- Bottle Merge-6 reuses the 65%-level four-layer Special foundation at the committed Bottle `effSum === 6` event: `merge six obicna.mp3`, `merge6 crash.mp3`, `merge6 boom.mp3` and `stack.mp3` retain the ordinary Merge-6 rates, crash cutoff/fade, preload and cleanup with only the dedicated Special gain scale changed. The ocean-finale sounds below are added over this shared foundation rather than replacing it.
- Bottle ocean finale: `water waves.wav` begins with the ocean/bubble scene at `1.0x / 0.40 action / 0.24 effective gain`. When the five hero bottles begin their authored fall 300ms later, `bottle1.wav` starts at `1.0x / 1.00 / 0.60`. At 20% of the 3.2s reference fall path (`+640ms`), `bottle 2.wav` starts alone at `1.00 / 0.60`. At 30% (`+960ms`), the user-replaced `bublesi.wav` starts at `1.0x / 0.64 action / 0.384 effective gain`. Both glass accents now occur only after the bottles pass the midpoint: `cling.wav` at 55% (`+1.76s`) and `cling2.wav` at 75% (`+2.40s`). Both Cling cues are proportionally 10% quieter than their preceding `0.42 / 0.252` setting, now `0.378 action / 0.2268 effective gain`. The dedicated Bottle owner obeys Settings Sounds, preloads on Arcade/Journey entry, and stops all active or pending layers with finale cancellation, board cleanup or Sounds OFF.
- Shared NN number-transition sound: Arcade Round and the themed Journey Board Transition use the identical supplied cue map and mix. `bum bum zvuk.wav` and `exit.wav` now start together from the first displayed N/digit enter timeline's exact `onStart`; `bum bum 2.wav` starts from the second digit enter timeline's exact `onStart`, currently 300ms later through each presentation's authored digit-enter stagger. `exit.wav` has no remaining activation in the digit-exit phase and plays only once. All three retain authored `1.0x`, use `0.70 action / 0.42 effective gain`, obey Settings Sounds and preload at their presentation boundary. Arcade Round and Board Transition own separate bounded voice IDs and cleanup, so a retired presentation cannot stop the replacement presentation's cue. A third digit receives no added enter cue.
- Forest World Board Transition ambiance: only a resolved Journey Forest Board Transition starts the scalable Forest-folder mix when its overlay is mounted. Both existing layers receive the requested proportional **1.20 local boost** after the shared SFX master. `assets/sound/Board transitions/Forest/Forest.wav` therefore rises from `0.60` to **0.72 effective** gain and retains its committed natural 6.48s ending. `assets/sound/Board transitions/Forest/bees small.wav` starts simultaneously and rises from `0.12` to **0.144 effective** gain, stays level for 3.0s, then fades linearly through its final 1.0s before stopping and rewinding at 4.0s. The decoded and HTMLAudio fallback paths share that fade contract. Arcade, Beach, Area 55 and scene-less transitions remain silent. Transition completion, overlay cleanup, interruption and gameplay handoff still do not truncate committed environment cues. The dedicated owner obeys Settings Sounds OFF before activation and preloads both Forest sources at the accepted transition boundary.
- Shared Magnet-archetype Merge-6 routing: core Magnet and the registered Bottle, Honey and Spaceship variants all start the 65%-level canonical Special foundation (`merge six obicna.mp3`, `merge6 crash.mp3`, `merge6 boom.mp3`, and `stack.mp3`) on their first committed Merge 6. `magnetpulls.wav` and `planks break.wav` are preserved but disconnected from runtime. When their selected cubes reach the shared 75% convergence/new-Merge boundary, the second merge replays that same reduced foundation and layers the supplied `boiing.wav`; core Magnet and Spaceship retain **1.00 action / 0.60 effective**, Honey uses **0.60 / 0.36**, and Bottle alone uses **0.40 / 0.24**. Bottle additionally starts `bubsplat1.wav` and `bubsplat2.wav` together through its dedicated **0.72 custom-bus scale**: each `0.88` authored base becomes **0.6336 post-bus action / 0.38016 effective**. This moves the measured splat-pair peak from about `-3.5dBFS` to about `-6.4dBFS` before the foundation and boing. `sparkle.wav`, the former custom seven-layer pull stack and Happy sustain/loop are absent. The shared Wild/Special `poof1.wav + poof2.wav` entry accents remain independently layered on the first Merge 6 at the cleanup-balanced **0.55 action / 0.33 effective** each. Bottle's separate ocean-finale `water waves`, `bottle1/2`, `bublesi` and `cling1/2` cues are unchanged. Successful merge cleanup lets committed one-shots finish; interruption/failure, board cleanup and Settings Sounds OFF stop them.
- Bottle second pull Merge-6 addition: only when every selected pull target reaches the existing 75% convergence boundary, `glass crack.wav` now starts once alongside the Bottle-only `bubsplat1.wav`/`bubsplat2.wav` layers before `tryMergePulledTiles()`. Its rate is 1.0× and gain is **0.80 action / 0.48 effective** after the 0.60 SFX master, without the bubsplat custom-bus trim. Both bubsplats retain **0.6336 post-bus action / 0.38016 effective** each; Bottle `boiing.wav`, first committed Merge-6, poofs and ocean finale retain their existing owners and gains. The existing Bottle pull owner preloads, obeys Sounds OFF and stops all three layers on cleanup.
- Honey-specific finale sweeteners: Honey's simultaneous local merge sweeteners pass through a dedicated **0.60 custom-bus scale**. The first registered Honey Merge 6 adds the user-shortened `honeysplat.wav` at **0.60 post-bus action / 0.36 effective** over the shared first-stage Magnet package. At the second merge, `hapr.wav` and `supersplat.wav` each move from `0.80` authored base to **0.48 post-bus action / 0.288 effective**, taking their measured pair peak from about `-0.4dBFS` to about `-4.8dBFS` before the foundation and Honey's unchanged `boiing.wav`. Bee-folder `smile.wav` and Honey `twinkle1.wav` remain preserved on disk but disconnected from runtime, as are `honey1.wav` through `honey4.wav`, `bees1.wav` and `bees2.wav`. The later `BUZZING!` bee-flight `BEES3s.wav` remains unchanged at **0.336 action / 0.2016 effective** because it is not part of the simultaneous merge collision. The source filename `hapr.wav` is preserved as supplied. These bounded voices preload at Arcade/Journey entry, obey Settings Sounds OFF and stop on board/finale cleanup; core Magnet, Bottle and Spaceship do not receive the Honey-local layers.
- Wild/Special landing source: `assets/sound/sfx/bag drop.wav` at authored `1.0x / 0.50 action gain / 0.30 effective gain` after the shared master
- Wild/Special landing trigger: exactly once at the committed grid-contact `onImpact` shared by every meter/backpack/crate-spawned core Wild and registered Special reward; initial board population and merge/finale sounds remain separate owners
- Arcade crate sequence: all four layers now play at `1.40x` and are another proportional 5% quieter; `crate 1.wav` starts with the crate entrance at `0.4256 action / 0.25536 effective gain`, `crate2.wav` follows at `+300ms` at `0.3192 / 0.19152`, `crate 3.wav` at `+600ms` at `0.2128 / 0.12768`, and `crate 4.wav` at `+800ms` (200ms after layer 3) at `0.3724 / 0.22344`. They remain exclusive to the Arcade crate.
- Journey backpack sequence: both layers are 20% louder than their preceding settings. `backpack1.wav` starts with the backpack entrance at `1.40x / 0.96 action / 0.576 effective gain` (approximately 1.429s wall time); `backpack2.wav` starts at `+200ms` at `2.10x / 0.30 action / 0.18 effective gain` (approximately 0.952s wall time). Neither layer plays for the Arcade crate.
- NO MOVES source: `assets/sound/grandpa - no moves left.mp3` at authored `1.0x / 0.70 action gain / 0.42 effective gain`; it plays once only after the real `.cc-no-moves-overlay` mounts and then continues naturally through overlay removal, Fail presentation and board cleanup until its approximately 3.997s file ends. Settings Sounds OFF remains the explicit interruption owner.
- Playback: pickup and rejected-return woosh cues start from `currentTime = 0.045`; ordinary, core Wild Star, Fish, Beach Ball, Flower, core TNT and Bottle Merge-6 start the shared four-layer base from zero with the primary/crash pair at `1.30x`; Wild Star starts one magic layer immediately and sparkle at `+200ms`; Beach Ball always starts impact plus `ball1.wav`, `ball2.wav` and `ball4.wav` immediately, then `ball5.wav` at exactly `+100ms`; Flower starts bush0, bush2 and bush3 immediately and starts spark only at the real visual-sequence end.
- Ownership: bounded decoded/fallback voices remain split by exact interaction or Special owner; shared ordinary source buffers and the intentional TNT/Flower mini-impact pool are decoded once, while each Wild/Special event adds only the layers owned by its exact variant.
- Cold-start/idle reliability: when a committed cue arrives while its shared buffer is still decoding or the Web Audio context is not yet running, the newest request for each bounded voice is retained and starts immediately after decode/context resume instead of being discarded. Settings OFF and route/board cleanup cancel retained starts, so a retired event cannot leak into the next screen.
- Settings: `gameSoundsEnabled` gates preload and playback; switching OFF or board cleanup stops and rewinds all bounded sources and cancels every delayed fallback owner.
- Exclusions: `pick up drag.wav` is preserved but no longer used at runtime; `bush1.wav` is preserved but excluded from Flower playback; each registered Special keeps its own authored finale layers even when it intentionally inherits the default Merge-6 foundation.

All other user-supplied audio remains preserved but is not newly wired.

## Removed generated work

The assistant-generated Gameplay, Navigation and prior Merge-6 candidate/runtime packages and their runtime modules were removed permanently.

Beyond the integrations listed above, no other sound family is currently approved for a new integration. Future work should begin from a separately auditioned cue and keep Gameplay, Navigation/CTA, Journey, Settings, Wild/Special and Board Transition ownership independent.
